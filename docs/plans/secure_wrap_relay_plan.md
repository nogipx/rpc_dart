<!--
SPDX-FileCopyrightText: 2026 Karim "nogipx" Mamatkazin <nogipx@gmail.com>

SPDX-License-Identifier: MIT
-->

# План изменений для `rpc_dart_data:serve`

## 1. Подготовка инфраструктуры конфигурации
- [x] Расширить модель конфигурации сервера поддержкой параметров secure wrap транспорта и relay-подключения.
- [x] Добавить значения по умолчанию и валидацию для новых полей: включение secure wrap, PASERK-ключи, адрес relay, идентификаторы и параметры публикации сервиса.

## 2. Поддержка параметров CLI `serve`
- [x] Обновить парсер аргументов в `packages/rpc_dart_data/bin/serve.dart` для приёма:
  - флага активации secure wrap транспорта и его настроек (сертификаты, проверка клиента, список доверенных CA, таймауты и пр.);
  - параметров подключения к relay (URI, токен/ключ, namespace, режим публикации);
  - пользовательских метаданных для публикации сервиса в relay (лейблы, теги, описание).
- [ ] Добавить вывод помощи и примеров использования новых опций.

## 3. Интеграция secure wrap транспорта
- [x] Импортировать и инициализировать secure wrap транспорт из `rpc_dart_transports` (или добавить соответствующий адаптер) в коде запуска сервера.
- [x] В зависимости от флага использовать secure wrap вместо стандартного транспорта, прокидывая параметры конфигурации.
- [x] Учесть совместимость с существующими параметрами (порт, привязка, SSL) и документировать приоритет настроек.

## 4. Подключение к relay
- [x] Реализовать создание клиента relay и установку защищённого канала (с учётом secure wrap, если требуется).
- [x] Добавить шаг регистрации/публикации сервиса в relay с передачей метаданных и политик доступа.
- [x] Обработать сценарии повторных попыток, восстановления соединения и завершения работы сервиса.

## 5. Логирование и наблюдаемость
- [x] Расширить логирование при запуске `serve`, чтобы выводить статус secure wrap и relay-подключения.
- [x] Добавить обработку и понятные сообщения об ошибках при неправильной конфигурации.

## 6. Тестирование и документация
- [ ] Дополнить юнит/интеграционные тесты для проверки разбора аргументов и построения конфигурации.
- [x] Обновить README/документацию (`docs/` или `README.md`) описанием новых возможностей и примерами запуска.
- [ ] При необходимости добавить скрипты Just или примеры для локальной отладки secure wrap + relay.

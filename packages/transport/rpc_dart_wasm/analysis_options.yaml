# SPDX-FileCopyrightText: 2026 Karim "nogipx" Mamatkazin <nogipx@gmail.com>
#
# SPDX-License-Identifier: MIT

# rpc_dart_wasm is NOT a pub-workspace member, so it resolves standalone -- but
# the include below is a RELATIVE path and needs no resolution at all, which is
# exactly why the shared floor uses one.
include: ../../../analysis_options_base.yaml

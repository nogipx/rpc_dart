name: rpc_notify
description: Topic-based async push notifications built on rpc_dart.
version: 1.1.0
repository: https://github.com/nogipx/rpc_dart.git
homepage: https://rpc.nogipx.dev
issue_tracker: https://github.com/nogipx/rpc_dart/issues

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  rpc_dart: ^3.2.3
  meta: ^1.17.0
  uuid: ^4.5.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
  build_runner: ^2.10.5
  rpc_dart_generator: ^0.4.2

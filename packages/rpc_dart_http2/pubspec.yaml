name: rpc_dart_http2
description: HTTP/2 caller/responder transports and server bootstrap for rpc_dart.
version: 0.1.0
repository: https://github.com/nogipx/rpc_dart.git
homepage: https://rpc.nogipx.dev
issue_tracker: https://github.com/nogipx/rpc_dart/issues

environment:
  sdk: '>=3.10.0 <4.0.0'

topics:
  - rpc
  - http2
  - grpc

dependencies:
  rpc_dart: ^2.5.0
  http2: ^2.3.1
  universal_io: ^2.3.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
  shelf: any
  stream_channel: any

name: rpc_dart_server
description: Server framework for rpc_dart — modules, DI, lifecycle, graceful shutdown, rate limiting, isolate workers, and test harness.
version: 0.2.0
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  rpc_dart:
    path: ../rpc_dart
  rpc_dart_isolate:
    path: ../../transport/rpc_dart_isolate

dependency_overrides:
  rpc_dart:
    path: ../rpc_dart

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.5

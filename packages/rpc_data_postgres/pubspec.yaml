name: rpc_data_postgres
description: PostgreSQL adapter and repository for rpc_data.
version: 4.0.0
repository: https://github.com/nogipx/rpc_dart.git
homepage: https://rpc.nogipx.dev
issue_tracker: https://github.com/nogipx/rpc_dart/issues

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  rpc_data:
    path: ../rpc_data
  postgres: ^3.5.9

dev_dependencies:
  lints: ^5.1.1
  test: ^1.26.3

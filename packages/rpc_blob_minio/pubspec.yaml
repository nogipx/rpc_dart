name: rpc_blob_minio
description: S3/MinIO storage adapter for rpc_blob with presigned download URLs.
version: 1.0.0
repository: https://github.com/nogipx/rpc_dart.git
homepage: https://rpc.nogipx.dev
issue_tracker: https://github.com/nogipx/rpc_dart/issues

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  rpc_blob:
    path: ../rpc_blob
  rpc_dart: ^2.6.1
  crypto: ^3.0.7
  minio: ^3.5.8
  xml: ^6.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0

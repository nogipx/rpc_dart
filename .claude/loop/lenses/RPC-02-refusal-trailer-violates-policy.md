---
уточняет: U-09
пути: [packages/core/rpc_dart/lib/**, packages/transport/rpc_dart_websocket/lib/**, packages/transport/rpc_dart_http2/lib/**, packages/transport/rpc_dart_isolate/lib/**, packages/transport/rpc_dart_http/lib/**]
применима: есть валидация исходящих метаданных той же политикой, что и входящих
ломается: неверный статус у клиента; в худшем случае закрытие соединения вместо отказа одному вызову.
применена: []
статус: подтверждена (раунд 153, вне журнала)
---

# RPC-02 — Трейлер, нарушающий политику, которой отказали

## Форма

Синтетический трейлер с диагностикой уходит через `sendMetadata`,
который валидирует ТОЙ ЖЕ политикой, что отказала пиру.

## Детектор

Все места сборки трейлера — `RpcMetadata.forTrailer`, `sendError`,
`_sendOkTrailerIfNeeded` — **во всех пакетах, не только в ядре**.

## Спрашивать

Пролезет ли объяснение под лимит, который только что применили?
Если трейлер выпускается ВХОДЯЩИМ, не убьёт ли его валидация
соединение?

## Улика

`maxHeaderValueBytes: 64` превращал `RpcStatusException(7, <70
символов>)` в `status 13 "Responder dispatch failed"` — длина
объяснения решала статус. Свип ядра (5 мест) читался как полный;
в транспортах нашлось ещё два.

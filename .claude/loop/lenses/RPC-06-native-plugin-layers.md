---
уточняет: U-14, U-03
пути: [packages/transport/rpc_dart_wasm/ios/**, packages/transport/rpc_dart_wasm/android/**, packages/transport/rpc_dart_wasm/lib/**]
применима: есть нативный слой плагина на Swift и Kotlin
ломается: зависание до сторожевого таймера, молчаливая смерть рантайма.
применена: []
статус: подтверждена (раунд 180, вне журнала)
---

# RPC-06 — Нативные слои плагина

Заметка:     проверяется командами `analyze:native` и `test:wasm:device`, и
             последняя обязана гоняться на ОБЕИХ платформах: это два разных
             скрипта на двух языках.

## Форма

Дефект живёт в Swift или Kotlin, куда Dart-грепы не заглядывают.

## Детектор

Исходники `rpc_dart_wasm` на обеих платформах; колбэки, которые
после старта равны nil (`finishBoot`); `Log.w(...); break`;
инъекция скрипта вне try/catch.

## Спрашивать

Прогонялся ли этот код, а не только читался и компилировался?

## Улика

Первый в истории ЗАПУСК Swift нашёл дефект на первом же тесте:
гость, чей glue бросает, стоил 30051 мс — сторожевой таймер до
миллисекунды, 334 мс после фикса.

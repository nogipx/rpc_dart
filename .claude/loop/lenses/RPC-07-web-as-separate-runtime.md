# RPC-07 — Веб как отдельный рантайм

Пути:       packages/core/rpc_dart/lib/**, packages/transport/rpc_dart_websocket/lib/**, packages/transport/rpc_dart_http2/lib/**, packages/transport/rpc_dart_isolate/lib/**, packages/transport/rpc_dart_http/lib/**
Применима:  веб — реальная цель сборки (dart2js)
Применена:  —

Уточняет:    U-03

Форма:       код, зелёный на VM и сломанный на dart2js.
Детектор:    `melos run test:web`; литералы больше 2^53; отмена `async*`;
             `Random.secure`; кодеки, доступные только на VM; разрешение часов.
Спрашивать:  какие файлы веб-набор молча не собрал?
Ломается:    неверный результат: веб-набор молча не собирает файл целиком, и
             зелёный прогон ничего не доказывает. Дальше — что угодно, вплоть до
             падения на цели, которую никто не гонял.
Улика:       `int.parse` для литерала выше 2^53 выкидывает ВЕСЬ ФАЙЛ из веб-набора
             без единого сообщения.
Статус:     подтверждена (раунд 090, вне журнала)

# Round journal

What a round is and how it links to the rest — [../LOOP.md](../LOOP.md). The
record format — `../../skills/evidence-loop/specs/round.md`.

**The next round's number is the highest here plus one.** That is the only
source: not memory, not a commit message, not the user's words.

## The 251-265 gap, explained (curate after round 327)

`loop.py lint` has warned about a hole between 250 and 266 on every run for
around seventy rounds, and nobody had written down why. It is not lost data.

Those fifteen rounds were **one investigation** — B-22, the paused consumer that
never repaid the connection pool — and their findings were written into the LEAD
rather than into round files. The commits are all in git
(`docs(loop): round 263 …`, `264`, `265`), and
`../backlog/B-22-paused-consumer-never-repays-the-pool.md` names 251, 252, 253,
254, 256, 262, 263, 264 and 265 by number: five witnesses that failed to observe
the defect and one reading that explained why.

So the gap is a record-keeping choice, not a gap in knowledge. Left as-is — the
content is reachable and renumbering would break every inbound reference — and
`lint`'s warning stands as a pointer to this note rather than to a mystery.

- **[442](442-the-doc-that-said-the-opposite.md)** FIXED, rpc_dart_websocket — **B-71's gap could not be witnessed, and looking for the witness found something worse: the shipped docs assert the gap does not exist.** The lead records, measured, that *"a browser client on a half-open path has no liveness signal at all"*. The public doc for the same parameter said *"**A web client is not unprotected**, but it cannot be tuned here"*, and the stub said it harder — *"is not left unprotected — the browser is doing it"*. **Both false.** A browser does run ping/pong, which is what the sentence rests on, but it exposes neither the interval nor the OUTCOME: a missing pong never reaches the page and does not close the socket the way `dart:io` does. True of the frames, false of the only thing the frames are for. **Two copies, in the API doc and the implementation, each reading as corroboration of the other** — which is how it survived since round 422. Both now carry the lead's table and the consequence neither had: on the web, set a deadline on every call, it is the only bound there is. One code change: `pingInterval` added to the stub's discard tuple, whose own comment says it exists "so a reader sees the platform cannot honour them" — the one parameter whose absence costs something was the one missing from it. **The witness is impossible with this harness and the reason is recorded** so nobody re-attempts it: `dart:io`'s WebSocket auto-pongs at the protocol layer, hand-rolling the upgrade needs a SHA-1 this package does not depend on, and `test:web` is node not a cuttable browser. B-71 does NOT close — its three fix shapes all need the owner, and option 1 (document it) is now spent. **B-30 closed on the owner's instruction.** Gate green incl. `test:web`
- **[441](441-core-tests-are-down-to-their-fixtures.md)** FIXED, rpc_dart — **B-30's DECIDED HALF IS COMPLETE**, and a correction to how six rounds reported their own remainder. 172 lines across six files; `rpc_dart`'s test suite now holds no Russian prose at all — what is left in it is exactly C-47's fixture set (18 lines, permanent). 453 tests pass; no duplicate comment line existed in either large file, so `replace_all` was not used once. **The correction**: rounds 435-440 each closed with a figure like "`test/` 443 lines across 21 files", in a context that read as the remainder of the work in hand. It was repo-wide. By package: `rpc_notify` 230 lines and `rpc_data_postgres` 17, **neither ever in scope** — they sit outside the owner's mandate exactly as `lib/` does — against `rpc_dart`'s 18 fixture lines. Nothing was skipped silently, but nothing said the total spanned three scopes either, and a reader watching 1338 fall to 443 would reasonably have thought core was two-fifths done when it was finished. **L-12 on the axis of PACKAGE: a count that spans scopes reads as progress against one of them.** Also: 440's prefix-damage detector re-run repo-wide gives four hits, all pre-existing Russian sentences opening with an English identifier — so it has a real false-positive rate and is a prompt to look, not a check that passes. **Everything remaining is owner-deferred, out of mandate, generated, or a fixture**, which makes the next target a question rather than a queue — the same one 436 asked. Gate green
- **[440](440-the-rule-applied-to-itself.md)** FIXED, rpc_dart — **B-30's `test/streams/`, 171 lines to 0, and the first round to work under 439's rule — plus a detector for 439's own damage, run repo-wide.** 439 said a mangled comment has no checker; that is true of the toolchain but not of a grep. The damage has a signature — an ASCII sentence followed by non-ASCII on one comment line, `grep -nE '// [A-Za-z][A-Za-z ,.()]*[а-яА-ЯёЁ]'` — and over every tracked `*.dart` it returns **no damage**: every hit is a pre-existing Russian sentence containing an English identifier (`IBlobClient реализация`, `StreamController с onCancel`), all in untouched `lib/`/`example/`. **So 439's six were the whole population and its repair was complete** — which "I fixed the ones I saw" could not have established. **The rule is also now costed**: `replace_all` used eight times, each checked first against a `sort | uniq -c` of the file's comment lines, two rejected as prefixes and done singly; the check takes about as long as reading them. 226 tests pass. `test/` is down to 443 lines across 21 files — **a third of round 435's 1338**, and 21 of those are C-47 fixtures that never go away. No stale number-comment in 171 lines: three rounds at zero against 437/438's two, so that pair stand but the rate does not generalise. Gate green
- **[439](439-the-replace-that-matched-a-prefix.md)** FIXED, rpc_dart — **B-30's `test/endpoint/` done, 187 Cyrillic lines to 0, and the round injected a defect its own detector caught by luck.** `// Регистрируем сервис` appeared four times so the sweep batched it — and it is a PREFIX of six longer comments, each of which came out half-translated (`// Register the service. первый раз`). **It compiles, every test passes, `analyze` is clean: nothing in a Dart gate can see a mangled comment.** What caught it was the round's own Cyrillic grep, working only because the surviving tail happened to be Russian — `// Регистрируем сервис v2` would have become `// Register the service. v2` and passed the sweep, the gate AND the detector. Fourth failure shape for C-47's table and the worst-behaved: the others fail loudly or are at least still visible to the detector. **Rule: anchor a bulk comment replace to the end of the line, or do the sites one at a time** — identical comments batch safely, comments that merely BEGIN the same way do not, and nothing distinguishes them when you write the edit. Also: the 437/438 stale-comment rate did NOT continue — 187 lines, zero number-comments disagreeing with their code, so the earlier pair were real but the rate is not uniform. `test/` is now 614 lines across 27 files, under half what it was at round 435. Gate green
- **[438](438-the-comment-that-was-right-once.md)** FIXED, rpc_dart — **B-30's `rpc_context` family, 170 Cyrillic lines to 1, and 437's stale comment turns out to be a RATE.** Round 437 argued that prose nobody reads is prose nobody checks; this round swept a comparable volume as a test of that and found another in the second file it opened — `// Отменяем через 100мс` on `Timer(Duration(milliseconds: 1))`. **`git log -S` makes it the more instructive case: the timer WAS 100 ms when the comment was written and became 1 in a later commit.** It was not wrong when written; it DRIFTED, and the drift was invisible because a reviewer skipping a language they do not read cannot notice that the number beside it moved. Two in two rounds, both stating a number checkable against the line below in one second. 305 tests pass. **A 13th fixture for C-47** — `RpcContext.withHeaders({'x-name': 'тест с unicode 🚀'})` asserted to throw `ArgumentError`, so translating it inverts the test — and **a third failure shape for C-47's canary table**: prose data with the producer 350 lines from the assertion (`'Aggregated N элементов'` at `:592`, `contains('3 элементов')` at `:231`/`:271`). Three sites, one edit; a PARTIAL edit is what breaks it. Bears on the deferred half: the 277 `lib/` comment lines have had the same exemption from review, on code that ships, and nobody has counted how many are wrong. Gate green
- **[437](437-the-lint-that-mandates-the-ambiguous-form.md)** FIXED, rpc_dart — **B-30's `test/serializers/`, the slice only C-47 made sweepable**, because three of its five files mix prose to translate with fixtures that must not be, on adjacent lines. 85 Cyrillic lines to 10; counting every script, the directory now holds **20 non-ASCII lines and every one is in C-47** — a falsifiable prediction that came out exact, so the census missed nothing. 126 tests pass. **Translating a comment is what subjects it to review**: `// < 3ms среднее время` sat on `lessThan(10000)`, which is 10 ms, and had been wrong since it was written — the sibling one line down was right, so this was an unchecked slip rather than a systematic one. A reviewer who skips a language they do not read skips the CLAIM inside it. **CANARY — the repo's own lint mandates the construct that broke round 435.** `$minTimeμs` reads as one identifier and is not (`μ` is not ASCII, so it ends the name and `μs` is a literal); the obvious repair `${minTime}μs` is refused by `unnecessary_brace_in_string_interps` under `--fatal-infos`. **The analyzer knows the boundary from the grammar and therefore calls the braces redundant — removing the only signal a human reader has.** Reverted with a comment; the lint config is the owner's to change. `lib/` still deferred pending the owner's answer to 436. Gate green
- **[436](436-the-detector-that-cannot-see-its-own-class.md)** FIXED, rpc_dart — **B-30 into core: `test/zero_copy/` swept, and the repo's whole fixture class censused into C-47.** 3 files, 171 Cyrillic + 98 emoji to zero, which takes the repo's remaining emoji from 154 lines to 56 — every one of those 98 was decoration, in a test name, a comment or a `print`. 29 tests run, not merely analysed. **Two findings about the detector B-30 names as "both the detector and the check".** It cannot tell prose from data: `cbor_test.dart` pins `'привет'` and `'☺'` to their exact hex (`6cd0bf…`, `63e298ba`), so translating the string changes the expected encoding. And it is **SCRIPT-SPECIFIC, so it cannot enumerate the class it keeps hitting** — `[а-яА-Я]` flags `optimized_cbor_test.dart:167` only because `'Hello 🌍 Мир 世界'` happens to contain `Мир`, while `'你好世界'` and `'مرحبا بالعالم'` two lines above in the SAME map literal are invisible to it, as is `'世界' * 5000` in the sibling file, 10,000 characters and the largest unicode fixture in the repo. **The split also changes shape between packages**: per FILE in transports (3 of 8), per LINE in core, where one file holds comments to translate at `:148` and fixtures not to at `:135` — so the remaining 26 core files cannot be driven from `grep -rl` at all. `test:web` run because the swept file carries a dart2js constraint in a comment, the kind most easily lost in translation. **`lib/` stays deferred** — the owner's decision is an ORDER and in-scope work remains; round 435's recommendation to take the 39 runtime log lines is put to the owner rather than acted on (L-13). Gate green incl. `test:web`
- **[435](435-the-half-that-ships.md)** FIXED, rpc_dart_isolate + rpc_dart_http2 — **B-30's transport test half, and a THIRD axis the lead never separated.** Five files translated, transports now at 3 Cyrillic files from 8, and 31 tests re-RUN under renamed test names rather than merely analysed. **The three that remain must keep their Cyrillic**: `'Ошибка'` asserted to percent-encode to ASCII, `'кириллица' * 6` proving a close reason is measured in BYTES, and the wasm UTF-8 round-trip — in all three the non-ASCII-ness IS the subject, so the lead's claim that the grep is *"both the detector and the check"* would demand breaking them. That grep also returns **10 gitignored Android resource artifacts** in nine languages nobody wrote; it has to be `git ls-files`-scoped. **The recount is the finding**: the lead defers `lib/` because *"16 of its files are `rpc_data`'s ... PUBLISHED API surface that dartdoc renders"*, but the weight is one file it never names — `rpc_notify/lib/src/stream_distributor.dart`, **176 lines, more than any test file in the repo, and 39 of them runtime LOG messages**. Comment and log are different axes: a comment is read by whoever opens the source, a log is EMITTED into the user's own stream and cannot be turned off without turning the logger off. Emoji, counted the same way, never reach `lib/` at all — the two halves of one style rule have different distributions. Canary: the sweep's own `μ` bug, which passes grep and review and fails `dart analyze`, caught by the gate rather than by care. Gate green
- **[434](434-the-fifth-wake-nobody-counted.md)** CLEAN, rpc_dart — **RPC-09 re-swept, and its wake-path count is FIVE.** The lens warns in its own text that the count "must be recounted from the code" because "a sweep that reports the previous sweep's number carries an undercount forward" — 244 said three, 322 corrected to four, and the truth is five: `handleInbound`'s CONNECTION-level grant at `:537` is a separate wake from `_onGrant`'s per-STREAM one at `:378`, has existed since connection flow control did, and **three sweeps walked past it**. The warning was right and still not sufficient — recounting catches an undercount only if it is EXHAUSTIVE, which means enumerating every call site mechanically rather than looking where the last round looked. **The mechanism also MOVED**: a grep for the lens's own names (`_fcAwaitCredit`, `_fcOnGrant`, `_fcForget`) now returns http2 only, because core's parking left `channel_transport.dart` for a dedicated `RpcFlowController` — a detector written against the old names reads as a clean sweep of a file that no longer holds the mechanism. P-10 reused, all eight cells hold, and **its ablation still starving the sender was not a given** once a second wake path was known: refusing only `_onGrant` could have left the connection path waking it, which would have made the bench `broken`. It hangs at 20 s — the sender parks on per-stream credit
- **[433](433-the-count-that-did-not-move.md)** CLEAN, rpc_dart and rpc_dart_isolate — **RPC-14 re-swept after 110 rounds and 57 changed files**, and the lens had left instructions for exactly this round: *"Round 323's count, for the next sweep to compare against: 7 in `rpc_dart/lib` ... and 4 in `rpc_dart_isolate`."* Both counts hold, same eleven sites, same disposition — 2 that hold something deliberately, 5 that wait on data, 4 isolate teardowns — with every line number moved, two of them by this same session (`client_connection.dart:514 -> :609` in 430, `isolate_transport_web.dart:316 -> :124` in 428). **The site worth re-reading rather than re-counting was the one round 430 touched**: its `TypeError` catch sits inside the timeout region this lens is about, and `_discardAbandonedAttempt` ends in `.catchError((Object _) {})`, so the downcast 430 made possible is caught rather than reaching the root zone. Control repeated from 323 because the code moved: skipping `teardownStartup()` fails the startup witness at 20 s with the child **still running**. **A lens that records its COUNT costs one grep to re-sweep** — that instruction, written in 323, is what made this the cheapest round available. Ten of eleven sites remain unwitnessed, unchanged and still stated
- **[432](432-the-half-a-user-reads.md)** FIXED, rpc_dart + http2 + isolate — **B-30's `example/` half, and the lead was counting the wrong unit.** 47 in-scope files is **1817 Cyrillic lines** — four times what a file count suggests — and this record demands translation rather than deletion, which is per line. So the scope was stated before starting: the **10 example files, 529 lines**, complete and self-contained, and the half a user actually reads since `example/` ships to pub.dev. Now zero. Translated with the constraints intact (*"a fresh Stream each time, or the second listen throws already listened to"*, *"codecs must not be passed in zero-copy mode"*, *"required: call the parent dispose()"*), and emoji removed in the same pass — including from the text these programs PRINT, which is output rather than source. **1288 lines across 37 `test/` files remain**, and several are test NAMES, which changes what the suite reports and is a different kind of edit. **B-70 was checked first and has nothing to take**: its index line still read *"34 and 23 are the two worth taking next"*, stale by thirteen rounds and refuted by the section directly above them in its own file — corrected here. Gate green
- **[431](431-the-guard-that-guards-nobody.md)** FIXED, rpc_dart_websocket — **B-39's documentation half shipped; its GUARD half refuted and returned to the owner.** The lead's probe re-runs unchanged 73 rounds on — the root-zone crash is live and the construction-zone fix demonstrably works — but neither the lead nor either of its two decisions asked the third question: **can the crashing arm be reached with a socket the LIBRARY built?** Read at both construction sites, no. `openWebSocket` builds the raw WebSocket locally and never hands it out; the server path says so in its own comment, *"once inside IOWebSocketChannel the socket is no longer reachable"*. The only closer is the library, and every teardown goes through `close()`, which sets `_closed` first — the control row that returns. **So the guard and the defect are disjoint**: the crash needs a raw socket the caller holds, and a caller only holds one when it built the socket itself, in its own zone, which the decided guard explicitly cannot reach. Shipping it would reroute every async error from every library-built channel — the accepted trade — to catch a throw that cannot occur there. What shipped is the residual on `RpcWebSocketChannel`'s class doc, where the one exposed caller reads it: the measured matrix, that neither `isClosed` nor `closeCode` can see it, that a `try`/`catch` does not help, and the two ways to be safe. **Second time these two leads came apart on reachability** — B-35 was left alone in 426 for the same reason. Gate green
- **[430](430-the-guard-the-type-walked-past.md)** FIXED, rpc_dart + 5 transports — **B-21, a decided breaking change, and ONE of its two claims was false.** *"Fixes nothing currently broken"* HOLDS — P-09 reused, control repeated first, reads exactly what round 224 left. *"Round 224's runtime refusal stays either way"* does NOT: with the typed factory a cursorless transport can only arrive through a cast, and **the implicit downcast throws BEFORE the `is!` guard sees it**, degrading a one-shot refusal (`Disconnected`, built once, the two members named) into a backoff spin (`Connecting`, built twice and counting, a bare TypeError) — the exact thing 224's GUARD test exists to prevent. Owner's call on the measurement: **ship the type AND restore the refusal**, so the connect loop now treats a factory `TypeError` as non-transient. The analyzer enumerated the surface — **39 sites, exactly one in library code** once the four caller transports named the marker — and item 2's "five packages, mechanical" was the smaller half: **three entry points had ERASED the capability in their declared type** (`RpcInMemoryTransport.pair`, `RpcIsolateTransport.spawn` x3, `RpcWasmTransport.fromBridge`) while returning objects that conform, so a factory using any of them would not have compiled on working code. Canary is the round's own first attempt: without the TypeError catch, both of 224's guarantees fail and the other seven tests in that file stay green. P-09's readings unchanged after — which is the point, the compile error is new and the runtime behaviour is not. Gate green incl. `test:web`
- **[429](429-two-of-three-were-already-done.md)** FIXED, rpc_dart and rpc_dart_http — **B-09's three gRPC-compat items, and TWO were already done.** The lead's own item 3 states the rule it broke: *"when deferring for blast radius, verify the specific thing you claim would break, or the deferral is a guess wearing a reason's clothes"* — applied to round 89's revert, not to itself. **(2) was fixed before the decision was written**: `grpcStatusFromHttpStatus` is one table for every transport, `429 || 502 || 503 || 504 => unavailable`, with a doc comment making B-09's retryability argument almost word for word. **(3) does not reproduce**: P-97 new, `items=2, error=RpcStatusException` where memory expected `items=2, NO ERROR`, and the third arm is a control proving the probe can report `NO ERROR` at all — without it three arms saying "error" would be equally consistent with an instrument that knows one answer. **(1) was live**, and both halves were the same mistake about one character: gRPC names `,`, RFC 9110 only RECOMMENDS comma-SP. `statusDetailsBin` never split, so a comma-joined value failed `base64.normalize` and read **null** — the error's structured details vanished while its status and message arrived intact — and the HTTP/1.1 caller split on `', '` alone. Fixed both, and rewrote the comment calling this a KNOWN LIMITATION with symmetric loss, which the spec does not agree exists: splitting IS the recombination Custom-Metadata's definition provides for. Canaries narrow each delimiter back and fail only its own transport's witnesses; round 285's unpadded suite stays green, which is what says the `-bin` change did not disturb the neighbouring clause on the same line. Gate green incl. `test:web`
- **[428](428-the-file-the-test-could-not-import.md)** FIXED, rpc_dart_isolate — **B-31, and its decided route could not have worked.** Route 3 was sold on "the test runs on the VM, because nothing in the channel's failure path needs `dart:js_interop`" — true of the PATH, and **a test imports a FILE**: `isolate_transport_web.dart` opens with `dart:js_interop` and `package:web`, and a VM test importing it dies in ~20 000 characters of `Type 'JSArrayBuffer' not found`. `@visibleForTesting` makes a class nameable, not a library loadable. **Instead the platform-neutral half moved**: `_WebMultiplexedChannel`, the wire format and the four helpers use `dart:async` and rpc_dart and nothing else, so they now live in `lib/src/web_bridge.dart` and the Worker plumbing stays behind. **Cheaper on this lead's own axis** — route 3 would have annotated a class inside a file the barrel conditionally EXPORTS, while `web_bridge.dart` is exported by nothing, so the public API is unchanged. Round 310's fix finally has a witness, 118 rounds on: a stub `send` and no Worker, asserting both halves the owner demanded plus a third (the channel still RECEIVES). **Two ablations separate the clauses**: `await close(); return;` fails all three witnesses, a bare `return;` fails only the named-error one, and both GUARDs stay green throughout. Gate green incl. `test:web`, which is what compiles the web file for dart2js and runs the package on Chrome
- **[427](427-the-number-the-decision-asked-me-to-write-down.md)** FIXED, rpc_dart_compression — **B-29, whose decision names the number to write down: 15980 ms against the VM's 12 ms.** L-13 says the carrying round re-measures the sentence, and that is the whole finding: P-34 reused unchanged reads **13463 ms median of three (13606/13094/13463) against 13 ms** — the defect intact across 141 rounds, the figure moved 16%. Writing it verbatim would have planted what rule one warns about, prose carrying a number that reads like evidence and ages with nothing to check it. So the residual goes into `RpcGzipCodec.maxDecompressedSize`'s doc comment and a new README section (the README had not mentioned the limit existed at all) **as a shape — enforced after allocation, not configurable, "a bound on what you accept, not a defence against what it costs to refuse"** — pointing at the audit test for figures. **Canary: `boundedInflate` disabled reads RSS +2072.1 MiB against a 400 MiB bound**, ~18 s, both GUARDs green. **The FIRST canary was invalid and is recorded because it looked valid**: the same ablation against P-34's forged-trailer fixture reads 22 ms, because `dart:io`'s gzip filter rejects a forged trailer before any inflating — a guard one layer above the one under test, and a wrong causal paragraph was already drafted from it for PUBLIC documentation. The two fixtures are not interchangeable. Gate green; `test:web` not swept (doc-comment-only `lib/` change) and the package's node suite run 3x instead
- **[426](426-the-mirror-nobody-held-up.md)** FIXED, rpc_dart — **B-56's PUMP half, the remainder 425 stated.** Of the four pumps two are SINK pumps and genuine twins (`requestSink`, `responseSink`); the other two differ in CONTROL FLOW, not hooks, and are declined on the lead's own "not a base class" constraint. **The twins disagreed on the clause round 390 fixed on one of them**, and B-56's two tables describe it two different ways — the nine-site sweep marks `responseSink` "stop on done: y", the five-site table "n/a, it IS the producer". Neither is measured and they cannot both be right. P-96 new, a handler producing at one message per 5 ms with a consumer reading everything, delta over the 250 ms after the ending: **handler half-closes +32, handler fails the call +35, `responder.done` TRUE for both** — the responder ended the call itself and kept draining its producer — against a +1 control (`responder.close()`) and a +1 caller-side control. **Silent, where 390's was an endless error log**: a send on a finished processor RETURNS rather than throwing. `BidirectionalStreamResponder.done` has existed all along; nothing watched it. Fixed BY extracting `SinkPump`, which owns pause-per-send, half-close, tell-the-peer-once, stop-on-`ended` and a teardown that cancels before closing; each site chooses only its signal. `-207/+69` across two files plus a 121-line helper. **One ablation, both sites red, twice over** — and the stop-clause ablation also reddens round 390's own witness, written 36 rounds ago for the other class. Arms B and C (the peer leaving) are unchanged and stated: `responder.done` reads FALSE there, so there is no signal to watch. Gate green incl. `test:web`
- **[425](425-the-cancel-path-the-table-had-no-column-for.md)** FIXED, rpc_dart — **B-56's extraction, carried out for the BRIDGE half, and the scope taken before the fix rather than after it.** Reading the nine sites apart gives two mechanics its one table conflates: six BRIDGES (mirror a source through a controller we own) and four PUMPS (pause the subscription per send) — and the sweep's nine miss `_pumpBidirectionalResponses` entirely. **A bridge has TWO cancel paths and the table has one column**: round 424 fixed the owner-teardown half, and two of the six still RETURNED the source's cancel Future from `onCancel`, which `StreamController` awaits. P-95 new, consumer `cancel()` over a parked user `async*`, 3000 ms cap: `track` **HUNG**, the circuit breaker **HUNG**, against a 6 ms control with the await removed — and no `disposerTimeout` bounds this path, unlike the one 424 fixed. **Arm 5 is L-13's premise check and it split the decision in two**: site 6, the designated extraction source, reads 7 ms on a real server-stream call parked in `await for (responses)` — sound — while the sentence about the other copies was not. Fixed BY extracting: `StreamBridge` owns pause forwarding, close-on-done and both cancel paths; all six bridges go through it, `-158/+62` across 8 files plus a 119-line helper, two ad-hoc guards subsumed. **ONE ablation of its `onCancel` turns both witnesses red** — which is the property the extraction buys and the only claim a per-file fix could not have made. The four pumps are a different signature and are stated as not done. Gate green incl. `test:web`
- **[424](424-the-rule-written-thirty-lines-above.md)** FIXED, rpc_dart — **B-56's three DEFECTS, not its refactor.** `RpcCallScope.listen` ended with `onDispose(sub.cancel)`, and disposers are AWAITED with `disposerTimeout` as the only bound — so closing a scope holding a parked generator cost **five seconds per subscription**, measured by the canary at the full budget against a 2000 ms witness bound. **Thirty lines above it, `track` does the same job correctly and states both rules and their price** — *"Not awaited: cancelling a suspended generator can block indefinitely ... a rejected cancel became TWO unhandled async errors, measured, and enough to kill the isolate"* — and `listen` was written with neither; the circuit breaker's bare `sub.cancel()` on a detached timer is the second half of that same sentence in another file. Also: neither `track` nor `listen` forwarded pause/resume, so a slow consumer never slowed the SOURCE. **The lesson is not the difficulty but where the rules already were: in a COMMENT, not in a function** — which is exactly what B-56's extraction is for. That extraction is NOT done and this round does not claim it; it needs one call site at a time with its witness green after each, and it has a designated source (site 6, the only one carrying the "fire the token only if the stream did not already finish" clause). Gate green incl. `test:web`
- **[423](423-the-third-route.md)** FIXED, rpc_dart — **B-57, and the third route the owner asked for a round to look for.** Both known routes were closed, and they failed for the SAME reason: each asked WHERE the handler should subscribe. It should not — the duty the subscription carried is not the handler's, it is the connection's, and the connection already has exactly one subscription. **P-94 new, a COUNT rather than a duration** (a wall clock on an in-memory pair measures the machine): listeners on `incomingMessages` per parked unary handler go `2/11/51/201` at 1/10/50/200 to a flat **1**, the control being one flag. For a pipeline responder the subscription delivered NOTHING anyway — the pipeline builds it with `id: streamId` and hands it the request four lines later. What it carried was `onError`, and `responder_pipeline`'s own onError **logged only**, which is exactly why round 393 refused to drop it; `_answerActiveStreams` now answers every stream once through `wireStatusFor` instead of N listeners answering one each. `onDone` stays separate: there the transport is gone and there is nothing to answer over. **The GUARD passes on BOTH sides of the ablation**, which is what says the duty moved rather than being dropped. **The reading the lead asked for was never taken** — whether a non-advisory error can arrive without the channel closing — and the fix made it moot rather than settling it; stated as declined. Gate green incl. `test:web`
- **[422](422-the-number-that-decided-a-shutdown.md)** FIXED, rpc_dart + http2 + websocket — **B-63's four remaining mechanics**, ranked before starting because they are not equal. **`_inFlightCalls` was the one with teeth**: both servers read the drain's count out of `collectEndpointMetrics()['activeResponders']` with `as int? ?? 0` — an OBSERVABILITY key, free to rename — so renaming it made both count ZERO, every drain complete instantly, and the shutdown report success while cutting live calls off. `drainUntilIdle` was already shared and the number feeding it was not, which is the half deciding whether a shutdown waits at all; and **rounds 418 and 421 had just made it heavier**, since `RpcApp` now delegates shutdown to that drain. Now `RpcEndpointBase.activeResponderCount`, typed, with the metrics key reading FROM it. **The canary's reading is the whole finding**: renaming the key fails the test that watches the coupling and LEAVES THE WEBSOCKET DRAIN PASSING — before, that rename broke the drain silently with every test green. `_notify` → `notifyWithoutDying`, four responder sites → `sendWireError`. **`_startKeepalive` is half-extracted on purpose** — the loop, the latch and the `isDead` guard the server half lacked are shared; the failure RESPONSE is not, because the two differ deliberately, and forcing them together is the cosmetic unification this lens declines. Filed **B-71** while here: a browser WebSocket client has no half-open detection at all, because the browser owns ping/pong and `pingInterval` is accepted and dropped. Gate green incl. `test:web`
- **[421](421-a-stopped-server-that-answers.md)** FIXED, rpc_dart_http2 + rpc_dart_websocket — **B-58 and B-59**, both owner-decided, both about what a server owes a peer it will not serve. **B-58's discriminator did not exist until round 412**: the site read `error is RpcException`, the BASE of the hierarchy, so a resource limit and malformed framing matched the same branch — 412 gave them separate statuses (8 and 13), which is what finally makes "count the protocol violations and not the limits" expressible. RESOURCE_EXHAUSTED stays excluded: that peer is misconfigured, not broken, and is told so precisely to correct itself. **B-59's mode flag alone was WRONG TWICE and the existing suite found both** — `start()` listened AGAIN, adding a second listener to a broadcast source so every connection got two endpoints and the call failed; and close code 1001 "going away" is the right meaning and an unsendable mechanism, since `package:web_socket` reserves everything outside 1000 and 3000-4999. Neither was in the decision and neither would have been found by reading. **Four existing tests inverted, and B-59's baseline carried its own inversion instruction** — *"if a stopped server now closes what it cannot serve, B-59 is fixed and this expectation is the thing to invert"*. `stop()` keeps its subscription and refuses arrivals; `dispose()` is the new terminal step, so a restart over a single-subscription stream now works. Gate green incl. `test:web`
- **[420](420-three-decisions-that-invented-a-result.md)** FIXED, rpc_dart + rpc_blob — **B-40, B-36 and B-46**, three owner decisions written down and never carried out, sharing a shape worth naming: in each the code SUBSTITUTED a result for one it did not have — a dotted service name became `/UnknownService/UnknownMethod`, an unobserved probe was recorded as a SUCCESS that closed the breaker, an empty blob id became a generated one. **B-40's edit was small and in the wrong place.** The first witness reimplemented the rule and checked itself — it would have passed with the production code reverted — because the formatter was private on a mixin RPC-24 hides. The fix was to MOVE it beside its inverse in `metadata.dart`, and the test now asserts the property against both real functions: the formatter rebuilds everything the parser admits. **An inverse pair that cannot be read together is an inverse pair that drifts.** B-36's pinned test moved exactly as its record predicted — `closed` was also what a fabricated success produced, so it could not tell the two apart; it now asserts `halfOpen` PLUS a following call that is admitted. B-46 got both implementations of `IBlobClient`, and its adjacent check came back NEGATIVE and is recorded: the adapters' empty-id substitution IS the deliberate wire encoding. Three canaries; B-46's left both CONTROLs green, so it is not "refuse everything without an id". Gate green incl. `test:web`
- **[419](419-asking-is-not-the-same-as-destroying.md)** FIXED, rpc_dart_http2 + rpc_dart_isolate — **B-70's two behavioural items**, 23 and 34. **23: the only way to learn the answer was to make it true.** Whether a `viaSocket` transport can rebuild its connection is fixed at CONSTRUCTION — it said so by passing `() => throw ...('does not support reconnect')`, and `reconnect()` calls the factory LAST, after discarding the connection, cancelling every subscription, disposing every pump and clearing six per-stream maps. Every in-flight call died to produce an error known before the method was entered. The factory is nullable now and null is checked BEFORE the teardown. **34: a `Worker` has no argument list.** The VM ships the policy as `args[4]` and the worker rebuilds it; on web `spawn(policy:)` applied it to the HOST transport only while the worker-side function took its own default — so a raised limit held on one side of the boundary and not the other, silently. It rides on the worker URL now, the one channel a Worker has at construction. **An existing suite used the defect as a TOOL**: `reconnect_failure_is_recoverable_test`'s helper is documented as "a transport whose reconnect factory ALWAYS throws, by construction" and used `viaSocket` precisely because it did — so the answer is a SPLIT, not a replacement: `viaSocket` takes an `@visibleForTesting connectionFactory`, and "cannot reconnect" (refuse, destroy nothing) and "the attempt failed" (tear down, stay recoverable) are covered separately. Both canaries clean; the 23 canary fails its second GUARD too, which says the connection is gone rather than degraded. Gate green incl. `test:web`
- **[418](418-the-drain-the-interface-could-not-ask-for.md)** FIXED, rpc_dart + rpc_dart_framework — **B-68 and B-69**, both parked as `owner decision` on the same objection (widening a public interface; changing what unknown tests exercise) and both answered by "backward compatibility does not matter". One shape, RPC-04: a capability present in the implementation and unreachable through the type the caller holds. **B-68 is an ORDERING defect and the unreachable parameter is why** — all three servers implemented `stop({drainTimeout})`, `IRpcServer` declared `stop()`, so `RpcApp` compensated by draining the endpoints itself: nothing had stopped the LISTENER, so a connection arriving mid-window got an already-draining endpoint, and `RpcEndpointBase.drain` CANCELS active contexts, the opposite of letting in-flight work finish — the very operation the websocket server's comment warns against since round 414. Now the server drains and goes FIRST, so a handler can still reach module resources while it finishes. **B-69 is a defect in the WITNESS**: `pair()` gave both halves the server's oversized-frame policy, so the frame-codec harness handed every suite a "client" that kills its connection. **The suite was green before the fix and after** — which is the finding, not a gap: the flag's two behaviours are unreachable from a `pair()` client today, so nothing was asserting the wrong half *yet*. **The interface cost, measured rather than estimated: six test fakes in one package.** Both canaries clean; all CONTROLs and GUARDs green. Gate green incl. `test:web`
- **[417](417-one-table-one-grammar.md)** FIXED, rpc_dart + http + http2 — **B-66 and B-67, both parked as `owner decision` and both answered by the owner's "backward compatibility does not matter"**. Neither needed measuring: each is two or three written answers to one question, and the work is choosing. **504 is the row that changes behaviour rather than wording** — `RpcRetryInterceptor` retries `unavailable` and `resourceExhausted` and nothing else, so a gateway timeout was retried over HTTP/2 and final over HTTP/1.1, same deployment and same proxy, and swapping the transport silently swapped the retry policy. **The method-path knob was monotone DOWNWARD only**: `maxMethodPathLength` IS enforced (the sweep's "no effect" claim was wrong), but raising it past 512 changed nothing because `_parseMethodPath` held a fourth copy with 512 hardcoded and refused afterwards — and the three layers disagreed on the GRAMMAR too, so the layer an embedder can configure was the loosest and the strictest had no configuration at all. Now one table in `protocol.dart` and one grammar in `metadata.dart` with the limit passed in. **The hypothesis "adopt the better-argued copy and delete the other" broke on an EXISTING test**: the http2 table is grpc-go's `HTTPStatusConvTab` exactly, but `oversized_request_is_resource_exhausted_test` named its direction — rpc_dart's own responders answer 413 for a body over `maxMessageLengthBytes`, and RESOURCE_EXHAUSTED is what tells the caller it hit a SIZE it can reduce. 413 and 499 are kept beyond grpc-go's rows and the reason is written down; 409/410/412/415/501 and the `>=400` default are gone. **The first canary failed the witness AND its guard** — with the number hardcoded the knob is ignored in both directions, which is a different defect from the one the witness alone describes. Gate green incl. `test:web`
- **[416](416-every-error-names-its-status.md)** FIXED, 17 packages — **the owner's call, twice mid-round: "get rid of all StateError" and "backward compatibility does not matter"**, which overturns half of round 411's hand-made classification and is recorded as a RETRACTION rather than applied quietly. **80 `throw StateError` sites in `lib/` across 17 packages, plus 9 that build one as a value** — the second group missed by the first grep and caught by the owner naming `buffered_broadcast.dart:213`. The damage was already written in the code's own words at `context.dart:461`: `wireStatusFor` is default-deny, so every one of these inside a handler reached the peer as INTERNAL "Internal server error" with its reason destroyed — a chunk-size limit the caller could have corrected, a checksum mismatch (corruption, indistinguishable from a server bug), every optimistic-concurrency conflict. Now each names a status the caller can act on: RESOURCE_EXHAUSTED for limits, ABORTED for version conflicts, DATA_LOSS for checksums and dropped events, INVALID_ARGUMENT for upload framing and decoded peer payloads, UNIMPLEMENTED, UNAVAILABLE, FAILED_PRECONDITION. **`RpcClosedException` is a TYPE because the MESSAGE was load-bearing and could not be**: `_isTransportClosed` compared message TEXT in two spellings, because the transports disagreed on what to throw — and `channel_transport.dart:437` had already drifted, so the matcher was widened to cope instead of the drift removed. That is B-63's largest item, live. **B-64 closed out in the same pass**: `RpcCallerTrailer` owns the trailer rule for all five call shapes, `RpcLongTimer.timeout` replaces the four bare `Future.timeout` deadline bounds, and the pre-flight guard covers all five. **Six canaries and three of them changed the round** — one passed and exposed a test that could not tell the endpoint guard from the transport's (re-pointed at `what == 'Endpoint'`), one passed and exposed a claim not witnessable at all (`then<void>`; kept, no canary claimed), and a GUARD caught a real unhandled-error bug in the NEW helper before it shipped. Gate green on all six targets, and **`test:web` and `test:wasm` each caught a test the ordinary gate cannot see**
- **[415](415-five-duties-and-the-sibling-that-answered-each.md)** FIXED, rpc_dart + http + http2 — the owner's 36-item duplication intake, executed on its confirmed half. RPC-25 taken as round 391 asked for it: five DUTIES at once rather than one copy per round, each read across every implementation, and a sibling had the right answer every time. **The one silent defect is the trailer**: `fromTrailer` reads the message out of `grpc-status-details-bin` only when the trailer message is EMPTY, and five of seven callers substituted `'Unknown error'` first — non-empty, so the branch was unreachable for them and `details:` went with it. A peer that puts its detail in `google.rpc.Status` and omits `grpc-message` (legal, and what the field is for) arrived intact on two call shapes and opaque on five. Also: `closeResponderResources` was the one teardown of five that never told the handler; `UnaryResponder` answered DATA + `grpc-status: 0` on a stream the drain had already closed out, on both branches, while every streaming shape was gated on `_isActive`; the http2 RESPONDER did a bare `await finish()` where its caller sibling bounds it and terminates; the CORS lists named 3 headers against the 7 core sends. **Two of the five were not the edit the sweep described, both in the copy that had drifted furthest** — `ping.dart` does not call the shared factory at all, so changing its literal would have made the message worse with nothing gained; and the token cancel made a LATENT `RpcCallScope.close()` race reachable for the first time, caught by an existing test (`['third','hang-start']` against `['third','hang-start','first']`) that no new test would have looked for. **Seven canaries, one per half**; two of them changed the round — the first caller-site ablation PASSED, exposing `server/caller.dart:199` as changed-but-unwitnessed, and one CORS test passed on both sides and was rewritten to assert what the fix actually claims. Gate green + `test:web`. B-65 closed; B-64 closed on item 1 of six; B-70 on 4 of 23
- **[414](414-two-decisions-carried-out.md)** FIXED, rpc_dart + websocket + http2 — two owner decisions recorded and executed. **B-60**: P-88 reused, eight parked server-streams so both transports spend the full budget and admission is the only variable — `websocket 1347 served after shutdown began` becomes **3**, with **472 refused** against 3, matching `http2 4 / 540`. The refusals are the other half of the reading: those calls are ANSWERED UNAVAILABLE rather than dropped. The mechanism was already in core and unreachable — `responder_pipeline.dart:592` rejects new streams while `_respIsDraining` is set, and the only public route was `drain()`, which also cancels every active context. **Where the new method had to live was not where I first put it**: declared on the mixin it does not compile at the call site, because `_endpoints` is `List<RpcEndpointBase>` deliberately (peer endpoints serve calls too) and `RpcResponderPipelineMixin` is HIDDEN by round 291's measured RPC-24 work — so `markDraining()` sits on the base as a no-op and is overridden in the mixin, the shape `collectEndpointMetrics()` already uses. **B-61**: `RpcRetryInterceptor` now reconnects before an UNAVAILABLE retry, UNAVAILABLE only — RESOURCE_EXHAUSTED means the peer is overloaded and its connection is fine. **The test that failed was measuring the hollow version**: `a drained connection is retried as the retry doc promises` went `3 -> 2` attempts, because its `drain()` GOAWAYs one connection and leaves the listener up, so the retry now RECOVERS on attempt 2. Counting three failures was a proxy for "a retry happened" and was meaningful only while no attempt could pass; re-pointed at the stronger property rather than pinned. Both canaries carry guards that would fail a lazier fix — `refused > 0` (a drain that silently dropped calls also scores a low `served`), and that RESOURCE_EXHAUSTED retries WITHOUT reconnecting. Gate 14/14
- **[413](413-the-envelope-that-threw-it-away.md)** FIXED, rpc_dart_http2 — B-62, which 409 deferred, and **the deferral did not survive round 412**. RPC-15 in its plainest form: a stated blocker re-read after the ground moved. 409 weighed "restore `filterStreamEvents`" against "drop the envelope" and called it even, because both are about ROUTING and the envelope is what separates a per-stream error from a connection-fatal one. Then 412 gave every library error the status that fits — and `RpcHttp2StreamError`, a plain class, made `wireStatusFor` take its DENY branch, so a wrapped `RESOURCE_EXHAUSTED "max: 16"` and a wrapped `UNIMPLEMENTED "no method"` both reached the peer as `INTERNAL "Internal server error"`. **Every status the type system had just been given was thrown away one layer out** — a third option neither framing describes, and it needs no number. Fixed by `extends RpcStatusException` deriving status and message **THROUGH `wireStatusFor(error)`** rather than copying the inner's fields: that is the whole safety argument, since `wireStatusFor` is the single place deciding what may leave the process, so a FOREIGN inner is still redacted where copying `inner.statusCode` would have silently opened the deny for everyone else's errors. `streamId` and `error` untouched, routing unchanged. Four tests, two GUARDs — the foreign-inner redaction, and that the envelope still holds its id and the identical inner object, which nothing else watches. Gate 14/14, rpc_dart_http2 **+234**. Left: `filterStreamEvents` still has no call site, but that now costs precision rather than correctness
- **[412](412-the-base-that-meant-nothing.md)** FIXED, rpc_dart and four transports — the owner's proposal, and the finish of the typing goal. Remaining unaudited categories first: **TimeoutException all 11 correct** — three sit directly beside `RpcDeadlineExceededException` in the same `onTimeout`, chosen by whether a bounding deadline exists, a split the parity test records as deliberate; the other eight are local operation timeouts where the type is idiomatic. **Then the find**: `_answerFramingViolation` classified by TYPE and said so — *"Matched on the TYPE, never on message text"* — but `error is RpcException` is the BASE of the hierarchy, so `parseHeader`'s MALFORMED throws, raised on the same path, matched it too. Measured one prefix apart: `limit grpc-status 8` and `malformed grpc-status 8`, where RESOURCE_EXHAUSTED is retryable — a corrupt frame answered "try again". Now `8` and `13`. **The owner's question caught a mistake of mine**: I had made the decompression site RESOURCE_EXHAUSTED, and the code's own comment had predicted it — *"a decompressor throws for the bomb it was asked to stop AND for input that is malformed ... naming one of them told a peer with a corrupt frame to send less, which cannot help"*. Corrected to INTERNAL, which is also grpc-go's answer. Switching that site to `wireStatusFor` **also closed a leak**: its trailer message was `'$error'` UNCONDITIONALLY, so a foreign error's text went to the peer past default-deny. **`RpcException` is now abstract**, which is what forced the remaining 13 raw sites to choose — UNIMPLEMENTED for unsupported grpc-encoding ×2 (the gRPC spec's own answer) and an unregistered method, RESOURCE_EXHAUSTED for three exhaustion limits, INTERNAL for the rest — and surfaced 31 test sites, the visible half of the breaking cost. `e is RpcException` still means "ours", asserted. Gate 14/14, rpc_dart **+1578**
- **[411](411-a-programming-error-doing-wire-duty.md)** FIXED, rpc_dart and rpc_dart_http2 — the goal's last piece, the **83 raw `StateError`/`ArgumentError` sites**, classified before anything moved. **Most should NOT move**: "Endpoint is closed" x4, "Transport is closed" x7, "Call finishSending()", "RpcApp.start() can only be called once", "Circular dependency among modules" — `ArgumentError` for a caller's mistake is idiomatic Dart. What does not belong is `ArgumentError` for **untrusted peer input**, and `validateMetadata` threw exactly that, with **five decision points reading `is ArgumentError` to mean "the peer is at fault"** — two of them deciding whether to END A CONNECTION via the 256-violation backstop. So an `ArgumentError` raised anywhere else on that path was charged to the peer's strike budget as if hostile: a wire-security decision keyed on a Dart programming-error type, which is the shape the owner named. Fixed with `RpcMetadataViolation extends RpcStatusException implements ArgumentError` — **implementing `ArgumentError` is the load-bearing half**, since all five sites and any caller's `catch` keep working while the type joins the hierarchy and carries INVALID_ARGUMENT instead of a redacted INTERNAL; then the two backstops narrow to `is RpcMetadataViolation`, which is the point. **The first fix was incomplete and two tests said so** — `"a grinding peer loses the connection at the default policy"` and its server mirror went red, because `http2HeadersToRpcMetadata` enforces `maxHeaders` DURING the walk rather than through `validateMetadata`, so its own throw was what the backstop actually counted. L-12 live: the class had a member I had not counted. `_headerValue`'s check deliberately stays an ordinary `ArgumentError` — its four callers all build OUTBOUND headers from our own metadata. Canary asserts the violation is all three types AT ONCE (dropping the `ArgumentError` interface would silently disarm two connection-closing backstops) and that a foreign `ArgumentError` is neither. Gate 14/14
- **[410](410-three-kinds-one-status.md)** FIXED, rpc_dart — the last core type on the wrong base, named in 408's `## Not fixed`: `RpcFrameException extends RpcException`, so `wireStatusFor`'s second branch — which hardcodes INTERNAL — answered every frame failure identically. **The hypothesis that all frame failures are one kind is refuted by enumerating all eleven construction sites**: 4 are a correctable limit (`channel_frame:105,:127`, `frame_multiplexed_channel:383,:423`), 6 are malformed framing, 1 is a policy violation. **For the four limits INTERNAL is wrong twice over** — grpc-go and grpc-java both answer RESOURCE_EXHAUSTED, and `RpcRetryInterceptor` treats it as transient where INTERNAL is final, so a sender that could have corrected itself by sending less was told to never try again. **The split is not invented**: it is the one `_answerFramingViolation` already makes on http2, and the policy status is the trailer `_validateInbound` already sends — the TYPE was the only place that did not know. Fixed with a named constructor per kind, default staying INTERNAL so the six malformed sites and `platform_error_redaction_test` are unchanged. `wireStatusFor` untouched: same function, different branch, because the type moved. Canary adds the default-is-still-INTERNAL assertion (the regression that would silently re-flatten six sites) and that the diagnostic still forwards. **Gate reported honestly: 2 of 8 workspace runs went red, neither reproducible nor nameable** — one from a chained shell line, one `rpc_dart_websocket +176 -1` with no test named — against six greens, four back-to-back, and websocket 3/3 alone. L-14's tell says a real defect survives the remedy a flake responds to and this did not; L-14 also says NAME it, and I could not, so it is recorded as unnamed rather than dismissed, because the change touches `channel_transport.dart` which websocket uses
- **[409](409-the-transports-two-types.md)** DEFERRED, rpc_dart_http2 and rpc_dart_websocket — the owner's goal names the transports and 408 stopped at core. **Five transports declare TWO error types between them**: `RpcWebSocketNonBinaryFrame extends RpcException implements IRpcAdvisoryChannelError`, and `RpcHttp2StreamError`, which is not an Exception at all. RPC-24. The hypothesis — that the second is 408's defect again — is **half right, and the other half is the finding**: it is not an error but an ENVELOPE, so "not an Exception" is correct by design, because nobody is meant to catch it before `filterStreamEvents` unwraps it. **Except nothing unwraps it.** Every reference in the repository is its own declaration, its own doc comment, a CHANGELOG line, and five uses in one test against a hand-made `StreamController` — **zero production call sites against two that CREATE the envelope**. So `wireStatusFor`, which is default-deny (408's subject), sees a non-`RpcException` and takes the deny branch: whatever the inner error was, the peer is told INTERNAL "Internal server error". The envelope hides its contents from the one function whose job is to decide what may be forwarded. Per-stream routing does not depend on it — both transports already `_streams.addError(streamId, error, stackTrace)` with the RAW inner error beside the enveloped broadcast add — so the envelope serves the broadcast alone and the broadcast has no unwrapper. **DEFERRED because the two fixes are opposite** and choosing needs a number neither has: restore the design by wiring `filterStreamEvents` in, or drop the envelope so the broadcast carries a classifiable error — the latter tempting until you notice connection-level fatal errors are added UNENVELOPED on purpose, which is the distinction the envelope exists to draw. **B-62** names the measurement that decides it. A sweep naming every reference is evidence about the CODE, not about the cost, and ending there rather than guessing is the point. No gate — no code changed
- **[408](408-three-types-outside-the-hierarchy.md)** FIXED, rpc_dart — the owner's new goal, one typed error hierarchy; L-12 first, so the CLASS is counted before anything moves. **199 throw sites** across core, framework and five transports — `45 ArgumentError / 38 StateError / 31 FormatException / 22 RpcException / 17 RpcStatusException / 15 UnsupportedError / 11 TimeoutException / 8 RpcFrameException / …` — **131 of 199 raw Dart errors, and a mass rename would be wrong**: `ArgumentError` for a real programming mistake is idiomatic, and 25 of the FormatExceptions are in the CBOR codec where malformed input is exactly that type's job. The class that matters is narrower: library DOMAIN concepts outside the library's hierarchy, and there are three — `RpcCancelledException`, `RpcDeadlineExceededException`, `CircuitBreakerOpenException`, all `implements Exception`. **The hypothesis that this costs nothing came from `wireStatusFor`'s own doc** (*"the responder pipeline answers both with their own status long before an error is translated"*) — round 321's variant of RPC-15, a comment explaining a previous fix. **Refuted**: `wireStatusFor` is DEFAULT DENY, so `cancelled` and `deadline` both came back `status 13 "Internal server error"` — **indistinguishable from a foreign StateError**, where gRPC has CANCELLED(1) and DEADLINE_EXCEEDED(4) for exactly this. The three controls are what made it readable (`rpcException` keeps its message, `statusException` keeps status and message, `stateError` redacted). **The cited cycle did not block the fix**: it blocks `protocol.dart` importing `context.dart` — the MAPPER importing the types — not the types extending the base, and `contracts/_index.dart` already imports `RpcException` in its own `show` list. So the fix is at the TYPE and `wireStatusFor` was not touched, which is why the deny stayed exactly as strict: `1 "handler cancelled"` and `4 "Deadline … exceeded"`, every control unchanged. `RpcException`/`RpcStatusException` became `const` (additive) because two subclasses were already const and `const CircuitBreakerOpenException()` appears twice in its own file; the breaker keeps `retryAfter` in `toString()` for the same reason, so its text is byte-identical. Seven-test canary of which **four are GUARDs**, because widening a hierarchy is exactly the change that can widen a security boundary. Gate 14/14, rpc_dart **+1573**, and **no existing test needed changing**. Named, not touched: `RpcHttp2StreamError` is not an Exception, `RpcFrameException` extends the wrong base, and the 83 raw StateError/ArgumentError sites need a reachability split first
- **[407](407-the-third-row.md)** CLEAN, rpc_dart_isolate — the transport the owner ranks second and the loop had not taken in many rounds, and the one that fits neither side of 405's comparison: no `_disconnected`, no `reconnect()`, a lifecycle that is just `spawn() -> (transport, kill)`. **The hypothesis died to READING before a line ran**, which is the cheap half: `spawn()` registers an errorPort AND an exitPort and after startup both do the same thing, `unawaited(hostChannel?.close())` — so the host is told on both paths and the real question is what that means to a CALLER. P-91, three arms: an uncaught throw (errorPort), a self-kill (exitPort), and the host's own `kill()`, which reaches neither and is therefore the control on the other two. **All three identical** — `in flight RpcStatusException(14) / isClosed true / a later call RpcStatusException(14)` — including the awkward case, an uncaught error thrown from a TIMER after the handler's own frame is gone. A call in flight when the worker dies is ANSWERED rather than left hanging. `isClosed: true` is not a fourth disagreement: a killed isolate is terminal, there is nothing to reconnect to, and the socket transports stay `false` because they can recover — same column, different correct answer, because the state model genuinely differs. The three-transport table now reads `websocket 9 / http2 14 / isolate 14`, and **this round deliberately draws no conclusion from it**: which status wins is an unanswered question put to the owner, so the row goes into the lead and not into an argument. No gate — no library code changed
- **[406](406-one-refusal-shape.md)** FIXED, rpc_dart_websocket and rpc_dart_http2 — B-61, with the owner answering both of 405's questions: unify the type on `RpcStatusException(FAILED_PRECONDITION)`, and make the http2 caller enter the disconnected state when its connection dies. **The first shipped; the second could not, and that is the round's finding.** Both `_ensureUsable` implementations now throw `RpcStatusException(failedPrecondition)`, so one `catch` covers every transport — and the CODE is the load-bearing part, not the type: `RpcRetryInterceptor._shouldRetry` takes only UNAVAILABLE and RESOURCE_EXHAUSTED and never calls `reconnect()`, so FAILED_PRECONDITION keeps the earlier round's argument (a retryable refusal invites the caller to repeat what cannot work) under a catchable type. **The second half was implemented, then reverted on evidence**: a derived `!isOpen && !goawayReceived && no active streams` predicate broke two tests that each pin a deliberate opposite — *"a drained connection is retried as the retry doc promises"* and *"GUARD: a genuinely dead connection still reports UNAVAILABLE and down"*, the latter being exactly B-61's named ending and requiring `createStream()` to hand out an id. **So B-61's premise was wrong**: http2 does not fail to notice its connection died, it notices and answers UNAVAILABLE-and-retryable on purpose, while websocket refuses non-retryably on purpose. Two rounds, two defensible choices, each pinned with its argument. A first attempt was worse and is recorded: it dropped the GOAWAY exemption because health's own message says *"Reconnect is required; 0 call(s) still finishing"* — **a predicate lifted out of a reporting function inherits the report's distinctions, not the caller's**, and health splits draining from down to say different things while the send path splits them to decide whether a retry can work. Canary updated rather than deleted, with a matcher asserting the type AND the code as a pair, because the type alone would let a regression to UNAVAILABLE stay green. Gate 14/14, websocket +177, http2 +230. B-61 rewritten around the real question: which retry semantics win
- **[405](405-the-guard-that-never-fires.md)** DEFERRED, rpc_dart_http2 and rpc_dart_websocket — the drift 404 recorded and did not file, now driven. **The hypothesis was that it is a TIMING artefact** — the two transports noticing disconnection at different moments, so one arm reached `_ensureUsable` and the other failed earlier on a dead connection (the 395/399 trap). Refuted by the probe's design: every arm polls the transport's own `health()` until it stops reporting healthy, and prints it, before touching the guard. `websocket degraded / createStream() StateError / a unary call StateError` against `http2 degraded / createStream() **no throw** / a unary call RpcStatusException`, `isClosed false` on both. **Both reached the state; only one guard fires**, and `createStream()` is the decisive row because it calls `_ensureUsable` directly on both — on http2 it hands out an id on a dead connection. **Mechanism to the line**: `_disconnected` has exactly two writers on the http2 caller, the KEEPALIVE failure path and the catch inside `reconnect()`, and `pingInterval` is opt-in — so the ordinary ending, the server goes away and nobody was pinging, reaches neither and the flag stays false forever. The websocket sibling sets it from its channel's `onDone` with a comment naming exactly this case. RPC-19 gains its mechanical detector: **enumerate every writer, enumerate the endings, cross them off — a flag whose writers are all on EXCEPTIONAL paths has no value for the ordinary one.** DEFERRED rather than fixed because the fix must pick which sibling is right, and **the websocket side's choice was itself argued for**: its test header records that a read used to answer *"a synthetic UNAVAILABLE — which is RETRYABLE, so the caller is invited to try the thing that cannot work"*, and `StateError` replaced it deliberately. That argument transfers — `RpcRetryInterceptor` retries only UNAVAILABLE/RESOURCE_EXHAUSTED and never calls `reconnect()`, so a retry spins — which makes http2's `RpcStatusException` possibly the WRONG member of the pair. **B-61**, carrying both questions. No gate — no library code changed
- **[404](404-what-the-messages-promise.md)** CLEAN, framework + websocket + http2 — the CLASS round 401 found one member of, counted before concluding anything (L-12: 333/334 fixed a sample and reported "~20 remain" against a real surface of ~200). **228 `throw` sites across the 22 packages' `lib/`; about 20 prescribe an action**, enumerated in the record. P-89 drives the three highest-risk — `RpcApp`'s *"create a new RpcApp to restart"*, the closest match to 401's shape, and both copies of *"call reconnect()"* — and **all three are correct**: `RpcApp` restarts on every arm including the literal reading where only the App is new and the module instances are reused (configured, started and stopped a second time), and both halves of the two-claim reconnect sentence hold on both transports. **The round's product is a sharpened detector**: the tell is not "a message that prescribes", it is *a message that prescribes rebuilding A when the state that blocks you is held by B* — `RpcApp.server` takes a server BUILDER, so the closure rebuilds what cannot be re-listened, where the websocket server said rebuild the server while the obstacle was the stream. Rank the class by that question and the remaining ~16 (`call X first` guards on an object's own API, where obstacle and named object are the same thing) drop to the bottom without being driven. Second rule the arms earned: **split a sentence with an `and`** — "recoverable, not closed" buys nothing if it only means `isClosed == false`, so the failing arm goes on to try a LATER reconnect (both did succeed). A drift the arms were not looking for, recorded not filed: the same DISCONNECTED state refuses with `StateError` on websocket and `RpcStatusException` on http2 — B-08's family, which round 201 closed for the CLOSED state only. No gate — no library code changed
- **[403](403-the-drain-that-only-waits.md)** DEFERRED, rpc_dart_websocket and rpc_dart — the question 402 could not answer, with the repair 402 specified. Eight parked server-streams held open across the drain remove the gap, so `drainUntilIdle`'s instantaneous count can never read zero, both transports must spend the whole budget, and ADMISSION is the only variable left: `websocket 3006 ms / 164 before / **1347 SERVED after** / 3 refused` against `http2 3016 ms / 118 / **4** / 514 refused`. **337x more work admitted after shutdown began**, direction independent of load — against 402's noise (`112/70` one way at 6 lanes, the reverse at 64), which is what a bench that can see its mechanism looks like. `RpcWebSocketServer.stop(drainTimeout:)` stops accepting new CONNECTIONS and then simply waits; WebSocket has no GOAWAY and this server has no substitute. **The damage is not the waiting**: when the budget expires the endpoints close, so a call admitted at 2.9 s into a 3 s drain is killed at 3.0 s — the calls most likely to be cut are the ones accepted after the server decided to shut down, which is what graceful shutdown exists to prevent, and a rolling deploy reaches it every time. **The mechanism already exists one layer down**: `responder_pipeline.dart:592` answers a new stream UNAVAILABLE while `_respIsDraining` is set and lets existing ones run — the rpc-level GOAWAY, written and tested, used by NEITHER server (http2 does not need it, websocket has nothing else). DEFERRED because the only public route to that flag is `RpcEndpointBase.drain()`, which ALSO cancels every active context — the opposite of what `stop(drainTimeout:)` promises — so splitting the two halves is a core API decision and not a round's to take. **B-60**. No gate — no library code changed
- **[402](402-the-drain-converges-for-the-wrong-reason.md)** INCONCLUSIVE, websocket and http2 — the sibling comparison P-87 named. `RpcHttp2Server._drain` sends GOAWAY before polling and its doc says that signal *"is what makes a gRPC graceful shutdown CONVERGE rather than merely expire"*; `RpcWebSocketServer._drain` has the same polling loop and no such signal, WebSocket having no GOAWAY. RPC-25. **Neither expires, and the ordering between them FLIPS with load** — `6 lanes: websocket 112 ms / 70 served after, http2 8 ms / 6`; `64 lanes: websocket 5 ms / 37, http2 36 ms / 64`. A difference that reverses is not the mechanism under test. The cause is in the instrument: `drainUntilIdle` samples an INSTANTANEOUS count, and with 5 ms async handlers there is always a moment with no live responder, so both drains exit on the first zero sample before admission matters at all — **GOAWAY is not what made either of these converge**, which leaves the http2 comment a claim this round could neither confirm nor refute (round 321's RPC-15 variant: a comment a previous round wrote to explain its own fix, which nothing ages). **INCONCLUSIVE rather than CLEAN, deliberately**: config's rule is that a bench which could not see the defect does not prove its absence, and this one demonstrably could not. P-88 is recorded **invalid** and kept, because it states exactly what would make it valid — a load with no gap, server-streams held open across the drain so `activeResponders` never reads zero. Its first version was worse and is why the `lanes` parameter exists: one sequential caller read `9 ms, 0 served after`. No gate — no library code changed
- **[401](401-the-remedy-that-was-not-one.md)** FIXED, rpc_dart_websocket — `RpcWebSocketServer`, the owner's priority transport and a file the loop had never taken. RPC-21, and it widens: `start()` refuses to restart over a single-subscription stream and throws a `StateError` that **PRESCRIBES** what to do instead, so the thing to drive twice is the instruction. `single StateError / broadcast ok, served / fresh server StateError` — **the broadcast remedy works and the other one cannot**: "construct a new RpcWebSocketServer" throws the identical error, because the obstacle is the STREAM and not the server object, and over the same `HttpServer` there is no fresh stream to be had either (it is single-subscription and `rpcWebSocketConnections` already listened to it). RPC-23's round-364 shape in the one place prose is read at RUNTIME by someone already in trouble. **My hypothesis was the opposite and measurement killed it** — I expected `asBroadcastStream()` to cancel its source and produce the running-but-deaf server `start()`'s own comment warns about. It does not. The second arm found what the WORKING remedy costs: while the server is stopped the socket keeps accepting and upgrading and the broadcast drops the event, so a peer gets `handshake accepted / closed 3 s later NOTHING / a call HUNG / after restart served` — no error, no close, no status, and `connectTimeout` cannot help because the connect SUCCEEDED. An ordinary rolling restart reaches it. Filed **B-59** rather than fixed: the only shape that answers the peer inverts the `_connectionsSub?.cancel()` that `stop()`'s own comment calls load-bearing, and costs restartability for good. **That observable was contaminated first** — the abandoned-socket flag read after the arm's own `sink.close()` said `closed`, P-84's first-rebuild trap again. The test file is explicit that only ONE of its four tests witnesses the change and the other three establish the facts it states, because three green tests beside a doc fix otherwise read as three witnesses. Gate 14/14, rpc_dart_websocket +177; analyze caught the test's own first version (an `onError` that must return `bool` and did not)
- **[400](400-the-peer-that-would-not-listen.md)** CLEAN, rpc_dart_http2 — C-46's last uncovered item, and it puts a question to **round 397's own fix**: that round released the refused stream in a `finally` placed AFTER `await sendMetadata(...)`, so a send parking on a peer that never reads would skip the finally and restore the leak exactly as found. Round 396's note is what made it planable — the refusal is a trailers-only HEADERS frame and HTTP/2 flow control covers DATA only, so a zero window does NOT park it; this needs TCP-level backpressure. Produced with a relay whose server-to-peer subscription is `pause()`d, because **a peer that simply never calls `listen` is not enough** — dart:io drains into its own buffer and the pressure never reaches the far end. `reads 400 opened / 86640 B delivered / 0 0 0 0` against `never reads 400 / 240 B / 0 0 0 0` and `never reads x50 20000 / 240 B / 394 394 0 0`. **Refuted**: the fix is not conditional on the peer's cooperation. `pumps` reads 0 on every arm, which is direct evidence rather than inference — `sendMetadata`'s last act is `_pumpFor(...).add(...)`, so a parked send would show there; `package:http2` queues a HEADERS frame instead of blocking on the socket, so round 396's note reaches further than it claimed: not only is the window irrelevant, the socket is too. The 394 is BACKLOG, not retention — bounded by the dependency's own read backpressure, and far below the 4096 an honest peer is already allowed. **The load-bearing control is the second reading**: t+4s and t+16s both 394, because one sample cannot separate a plateau from a slow climb and that is the entire verdict. **And the RSS column decides nothing, deliberately**: `-21.8 / -142.2 / +107.6 MiB`, a process holding both peers, two of three negative — C-29 said *expose the counter before trusting the memory* in round 137 and round 394 said it again about frames, so the third time it goes in the probe record. No gate — no library code changed
- **[399](399-refused-is-dearer-only-to-answer.md)** CLEAN, rpc_dart_http2 — B-58's cost half, filed by round 397 two rounds earlier and left without a number. RPC-22's own Ask decides it: *which is cheaper for an attacker, being accepted or being refused?* **The hypothesis is refuted on the half that matters.** P-85, new, 2000 operations each on ONE connection through a byte-counting relay (neither endpoint can report both directions without the other's cooperation): `served 2000 ops / 873 ms / up 164.3 B/op / down 128.0 / amp 0.78x / status 0` against `framing 2000 / 548 ms / up 134.0 / down 216.0 / amp 1.61x / status 8`. The peer does pay less to send, which trips the Ask as written — **and the server also does less work**, so a refusal flood is strictly less damaging than the same volume of honest calls, which nothing bounds either. **The lens gains a second clause**: ask cheaper-for-the-attacker AND dearer-for-the-server, or a cheap refusal that is also a cheap answer reads as a finding; every earlier application happened to have a server cost, so the question had never been asked in two parts. `:method GET` is the contrast B-58 asked for and shows the sibling's backstop working — 300 attempted, connection closed after the 256th violation — against `framing` carrying all 2000. The one axis where a refusal IS worse is amplification, the only path here writing more than it reads, and the ablation names the cause: `maxHeaderValueBytes: 24`, same site (`grpc-status 8`), **216 B down becomes 131 and 1.61x becomes 0.98x** — the parser's diagnostic text, which is the thing that lets a client fix its own request. Recorded, not changed; 1.61x is not a reflector anyone can aim. **The round's first ablation arm was void and its own instrument caught it**: `maxHeaderValueBytes: 8` came back `status 3, 300 ops`, `validateMetadata` refusing the request's own `content-type: application/grpc` — the headers site and its backstop, never the framing site. Same trap as round 395, caught by the `grpc-status` column P-84 grew for it two rounds ago. B-58 stays open, narrowed to the half no measurement can settle: whether `closeOnProtocolError` should mean malformed framing. No gate — no library code changed
- **[398](398-the-dependency-shipped-the-fix.md)** FIXED, rpc_dart_http2 — **B-53 closed by `http2: ^3.1.0`**, the owner's own request. Open since round 384 and the longest-standing http2 lead: a client-side RST_STREAM racing responses still in flight destroys the WHOLE connection. Rounds 385 and 387 ablated it to the reset alone and placed the cause below rpc_dart; the lead's `reason:` said the way out needed something `package:http2` does not expose. 3.1.0's changelog opens with *"gracefully handle receiving headers on a stream that the client has canceled (#1799)"* — exactly the mechanism 387 described, the SERVER writing to a stream the client just reset. P-73 reused unchanged, resolved version the only variable: **`abort racing responses` 5 of 5 DEAD → 5 of 5 pong, awaited and unawaited both**, six already-clean arms untouched. Neither behaviour trade the owner was asked to decide had to be made. **The round's first measurement was invalid and the owner broke it with one sentence — *maybe the test is wrong?*** The skipped witness was run ALONE, twice per version, then under the workspace gate on both: five green runs and the stated conclusion "the upgrade is not what changed it". B-53's own text says the case is *"clean run alone, and not enough under load"* — every one of those runs was the arm that cannot see it, and the deterministic answer was in the lead's own `probe:` field. **L-17**: a `skip:` line states its CONDITIONS, and reproducing a pass under other conditions measures nothing while reading exactly like evidence; reach for the probe the lead NAMES before the test that happens to be nearby. Canary `abort_racing_responses_keeps_the_connection_test.dart`, derived from P-73's racing arm, fails on 2.3.1 (`['pong','pong','pong','DEAD','DEAD']`) with its three guards green on both — and the file says in its own header that a version which SETTLES before aborting witnesses nothing, because that is the mistake the round made first. Two mechanical edits the upgrade forced: `terminate` gained `String? message` in 3.0.0 (one test fake), and 3.1.0's new `ClientTransportConnection` member is harmless here since nothing implements it. Gate 14/14, rpc_dart_http2 **+230, no skips left**
- **[397](397-the-refusal-that-kept-the-stream.md)** FIXED, rpc_dart_http2 — RPC-22 on the SECOND refusal site in the http2 responder, the one C-46 listed as undriven. `_answerRejectedStream` (refused in the HEADERS) releases the stream in a `finally` after answering; `_answerFramingViolation` (refused in a DATA frame) fired the trailer and returned, so reclaiming the stream depended on the PEER setting END_STREAM. The hostile request is **five bytes** — valid headers, then a gRPC prefix declaring 32 MiB, which the parser refuses on the prefix alone — and the server answered all 200 with `grpc-status 8`, END_STREAM, call over as far as the protocol is concerned, then kept the stream, its inbound subscription, its parser and its outgoing pump: **incoming/subs/parsers/pumps 200/200/200/200 against 0/0/0/0 for the same frame WITH a half-close.** That pair is the measurement; one arm alone reads as "refusals are clean" or "refusals leak" depending on which gets written, and `refused (:method GET)` at 0 is what shows it is the SITE and not the peer. Bounded by `maxActiveStreams` and dies with the socket, so what it buys is a wedged connection per peer rather than unbounded growth — said plainly rather than as a leak with no ceiling. **The canary found the second half**: a call the pipeline is already running (a client-stream upload) never appears in the unary arms, because a unary responder is not dispatched until its payload arrives — and there the parse error reaches the handler's request stream while nothing CLOSES it, so the handler sat in its `await for` with **1 responder live 600 ms after the refusal**, with or without the release. Fixed with the synthesized cancellation `_fcRefuseOverrun` already sends for exactly this reason. **Both halves ablated separately** (L-01): release+emit gone → 200s and a hung handler, emit alone gone → zeros and a hung handler, so neither masks the other. Left open as **B-58**: this site still counts toward neither the 256 backstop nor `closeOnProtocolError`, and splitting the resource-limit branch from the malformed-framing one is a decision about what that knob means. Gate 14/14, rpc_dart_http2 +225 ~1
- **[396](396-the-collection-nobody-could-count.md)** CLEAN, rpc_dart_http2 — RPC-15 on C-37, whose claim that all 13 per-stream collections prune was read from each transport's `health()` — and the http2 responder's `health()` **did not expose `_outgoingPumps`**, so for that one the evidence was a reading (it shares a line with `_incomingStreams` in `releaseStreamId`), not a count. Round 395 hit that gap; this closes it. The round's one code change IS the instrument — `outgoingPumps` added to the health details, a tool rather than a cosmetic because two consecutive rounds needed it. P-84 reused with one arm and one column added: **0 pumps across served, refused-by-transport, refused-by-pipeline and abandoned-before-answer**, against **200** on `streaming, mid-answer` — 200 server-streams that answered once and parked, whose writers are live. That arm is the whole point: without it, four zeros and a counter wired to the wrong field are the same output, which is the trap 395's first bench fell into one field over. Its other columns check the instrument too — `subs=0` while `incoming=200`, the client having half-closed while the answer is still going out, exactly what the `onDone` comment says it leaves on purpose. **A note for whoever takes the last uncovered piece**: a refusal is a trailers-only HEADERS frame and HTTP/2 flow control covers DATA only, so a zero receive window will NOT park it — stalling that path needs TCP-level backpressure, a different experiment from the window games rounds 272 and 276 used elsewhere
- **[395](395-the-leak-that-was-not-there.md)** CLEAN, rpc_dart_http2 — RPC-22 on the http2 responder past construction, and **the hypothesis was built from reading and was wrong, which is the result**. The reading: `_incomingStreams[streamId]` is set on line 336 before any validation; that map and `_outgoingPumps` are pruned in one place only, `releaseStreamId`, which is the PIPELINE's call; and `_answerRejectedStream`'s own doc says a refused stream never reaches the pipeline. All true — so a peer sending invalid requests should pin one entry each for the life of the connection. **Measured, it does not**: 200 requests on one connection, read from `health()` while the connection is still OPEN, `refused (:method GET)` reads `incomingStreams=0 streamSubscriptions=0`. The refusal ENDS the stream, and the teardown that follows reaches `releaseStreamId` whether or not there was a responder to clean up. **The bench needed two rebuilds and both are worth keeping**: it first read the counters after `conn.terminate()`, which runs `close()` and clears every map, so both arms said 0 and it was measuring its own teardown; and its first "control" was a POST with no body, which the PIPELINE refuses for its own reason — both arms came back `grpc-status 3` and there was no served arm at all, which is why every arm now reports the status the peer saw. The arm that makes the zeros mean anything is `open, never ended`: 200 streams the peer never half-closes, reading **200 / 200**. Left behind rather than fixed: **`_outgoingPumps` is the one per-stream collection `health()` does not count**, so its fate here is inferred from sharing a line with `_incomingStreams` rather than measured — C-46
- **[394](394-the-guard-only-one-door-had.md)** FIXED, rpc_dart_http2 — the http2 responder, which the owner named as the next surface (*"the side grpcurl and grpc-go hit directly"*). The header-block guard's own doc names an invariant with a silent failure mode — `skipConnectionPreface` backwards "silently disables the guard in one direction and corrupts parsing in the other" — so that was checked first at both call sites and **holds** (server defaults true, caller passes false). The defect is a level up: **which doors the guard is fitted to.** RPC-08, first application since 205, and it widens — the inert neighbour is a construction PATH, not a transport. `RpcHttp2ResponderTransport` is EXPORTED and takes an already-built `ServerTransportConnection`, so a user with their own accept loop (TLS, ALPN, a shared port) writes what `RpcHttp2Server` writes minus the guard, and nothing says so. Same policy, same 64 MiB flood: **server 65 of 4096 frames, own accept loop 4096 of 4096.** Round 237's CONTINUATION flood wide open, below every rpc_dart limit because no stream exists until END_HEADERS. **And not only the guard** — the same block sets `SETTINGS_MAX_CONCURRENT_STREAMS` from the policy, without which a connection advertises package:http2's default of 1000 whatever the policy says. Two policy-derived protections, both attachable only at construction, both missing from the exported door. Fixed with `RpcHttp2ResponderTransport.overStreams`, **and the server now routes through it**, so there is one construction path rather than two (B-56's lesson applied); `_advertisedStreamLimit` and its reasoning moved with it, no tombstone left. **The number is FRAMES, not RSS**: the unguarded arm read +178 MiB and +27 MiB across runs for identical input — the GC, not the defect — while 65-against-4096 is deterministic. Canary: guard removed → `Actual: <512>`, all frames buffered, with the ordinary-call guard green and the server's own flood test still passing. Gate 15/15, rpc_dart_http2 +221 ~1
- **[393](393-two-hundred-listeners-for-one-frame.md)** FIXED, rpc_dart — B-57, the owner's U1, both halves, scope set before the fix. RPC-25. **`UnaryResponder` listens to the WHOLE connection broadcast with an `async` callback and filters inside it** — and an `async` function allocates a Future whatever it does, so every frame costs one Future and one microtask per live unary handler, for frames none of them want. **48 / 126 / 333 ms** for 3000 upstream frames at 1 / 50 / 200 parked handlers → **38 / 42 / 94** once the callback is synchronous with the filter first. 3.5x at 200, curve nearly flat, control unmoved. **The first version of the bench read a FLAT line and was wrong**: two counters planted in the responder said `3` listener entries for a 3000-frame pump, because it pumped with a SERVER-STREAM and the parked responders live on the server, whose broadcast carries inbound frames only. Pumped upstream it reads **600 400 entries at 200 handlers** — exactly N per frame. A flat line means "no defect" and "no mechanism" equally, and only the instrument separates them. **And the round's first product is a correction**: B-57's own recorded fix — subscribe to `getMessagesForStream(id)` — is WRONG, because that creates a per-stream controller and `_admitToStreamBuffer` can refuse a frame and `return` *before* the unconditional `_incoming.add`, so a large unary request would be dropped from the pipeline too by a bound that does not apply to unary today. Struck out in the lead rather than deleted. Second half: the `IRpcAdvisoryChannelError` check in `onError`, canaried (`Expected: empty / Actual: [1]`) with its guard green — and the round says plainly that on the PIPELINE path that window is ~zero, since `_stateFor` and `requestHandled = true` sit in one synchronous block; the reachable exposure is the low-level API. Still open: the fan-out COUNT. Gate 15/15, rpc_dart +1566 ~1
- **[392](392-a-hundred-characters-two-hundred-bytes.md)** FIXED, rpc_dart_websocket — the owner's audit of core and the four transports, W1. RPC-07. `closeForProtocolError` trimmed its close reason by CHARACTERS against a cap expressed in BYTES, **and the file contradicts itself three lines apart**: the comment says "caps the close reason at 123 BYTES" directly above `reason.length > 100`. The text is peer-controlled — `validateMetadata` quotes the header name it rejected, and a name is usually invalid for being non-ASCII, so `channel_transport.dart:705` hands 138 bytes of Cyrillic straight to it. **The current SDK's VM behaviour turned out to be the owner's BROWSER arm, not their VM one**: they predicted an over-long control frame and a 1002 from the peer; `package:web_socket` now validates and THROWS, which `catch (_) {}` swallowed — so `sink.close()` never ran while `_closed` was already true, making the ordinary `close()` a no-op. The socket stays open and the peer is never told, on both platforms. Fixed by cutting on UTF-8 byte boundaries plus a reason-less fallback close. **The bench's fixture is the design**: 84 characters and 138 bytes, both asserted in the test — over the byte cap or there is nothing to see, under the old 100-character trim or the old code would have cut it anyway and the arm would prove nothing. A first version used 140 characters and failed its own second assertion, catching itself before the round could conclude anything. Two canaries: the trim removed reproduces the SDK error and the 5 s timeout; **the fallback removed changes nothing**, so it is recorded as unwitnessed here and kept for the runtime this VM test cannot reach (C-26's class). U1 filed as **B-57** with everything verified, including a better fix than the one proposed — `getMessagesForStream(id)` kills the O(N) fan-out AND withholds advisory errors, fixing both halves. Gate 15/15
- **[391](391-the-order-without-the-rule.md)** FIXED, rpc_dart — **the owner audited round 386's fix and found it had traded a throw for a HANG.** `subscription.cancel()` returns the SOURCE's cancellation future, and for an `async*` that completes only when the generator body does — *"cancellation does not affect an async generator suspended at an await"* — so `await _requestSub?.cancel()` never returns for the commonest bidi shape there is, a chat pumping an idle input stream. Measured: `TimeoutException after 0:00:05`. **Three of the five implementations of this mechanic carry a comment saying not to await it** (`ServerStreamResponder`: *"Not awaited: a handler stuck in cancel must not block teardown"*; `ClientStreamCaller`: *"cancelling a stalled producer can block indefinitely"*; the bridge: *"Awaiting it deadlocked cancel()"*). Round 386 copied the fourth copy's ORDER and not the rule the other three state — **L-16**, new: copy what a sibling AVOIDS, not only what it does. Dropping the await is safe because `_recordCancel` clears the add-stream state SYNCHRONOUSLY, and that is measured rather than argued: 386's own witness, close-during-addStream-does-not-throw, still passes. Same fix on the responder's mirror. **And why 386's three witnesses were all green**: its two sources were an `async*` parked on a YIELD (cancel wakes it) and a plain `StreamController` (cancel is immediate) — neither can express a generator suspended at an `await`. L-15 one turn further out: not an arm whose subject never arrived, but one that arrived in the single state that works. Canary hangs both new witnesses with every other test green. Gate 15/15, rpc_dart +1564 ~1. Files **B-56**, the owner's own proposal: five copies of one subscription discipline, one clause added per round for four rounds running
- **[390](390-the-signal-the-caller-never-had.md)** FIXED, rpc_dart — B-54 at the owner's direction, the last of their three and the last round before the cap. Round 386 had refuted both cheap fixes and left a surface change, which is the thing not to start near a cap unless it fits in one round. It fit, because **the signal already existed and nothing exposed it**: every ending closes `_responseController` — END_STREAM, a non-OK trailer, a deadline, cancellation, the scope's disposal, all eighteen sites — so that controller's own `done` future covers all of them. `CallProcessor` now exposes `Future<void> get done` and `requestSink` cancels its subscription on it. **`produced 11 when the call ended, 32 a quarter-second on` → `2 and 2`.** Which is also why the two earlier candidates failed: both asked the wrong object — `send()` never throws (C-35) and `isActive` describes the PROCESSOR rather than the call. `ClientStreamCaller.call(Stream)` has raced its request stream against the response with `Future.any` all along, so RPC-25 for the sixth round running and the sibling as model for the third time. Canary prints `Expected: <13> / Actual: <39>`; the witness carries a second assertion — `atEnd` must be below the source's own limit — so a source that merely drained in time cannot pass it. Gate 15/15, rpc_dart +1562 ~1. **One red recorded rather than buried**: the first workspace run showed `rpc_dart_isolate +77 -1`, which did not reproduce alone, in a four-package concurrent run, or in a second full run, on a package this round does not touch and with no captured message (L-14)
- **[389](389-the-call-that-never-ended.md)** FIXED, rpc_dart — the owner asked whether all three defects from their audit were fixed; one of three was. This takes the third, B-55, because it is silent-wrong-answer severity and its only blocker was a broken RIG. Rebuilt on `bidirectional_coverage_test`'s pair (`RpcInMemoryTransport` through `NoZeroCopyTransport`, low-level responder on id 1) and both control arms answered at once. **The reading was directionally right and the consequence worse than predicted**: the client is not told OK, it is told **nothing for ever** — `2 payloads, NEVER ENDED`. `addStream` does NOT close its target controller, so a failing source reaches an `onError` that only logged and the controller stays open: `onDone` never runs, `finishReceiving()` is never called, no trailer is sent, and the caller holds an open call for the life of the process. `ServerStreamResponder` has done the right thing all along — `wireStatusFor(error)` then `sendError` — so RPC-25 for the FIFTH round running, sibling as model again. `NEVER ENDED` → `status 13`, both control arms unchanged, canary prints the defect verbatim. **The round's keeper is about the witness**: its first version asserted only `isNot(contains('ended OK'))` and **PASSED against the unfixed tree**, because "never ended" is also not "ended OK" — it would have recorded a fix for a defect that was never there. Measured with an OVERALL deadline rather than a per-event `Stream.timeout`, which is what let "never ended" be a first-class answer at all. Gate 15/15, rpc_dart +1561 ~1
- **[388](388-the-reset-the-dependency-already-sends.md)** FIXED, rpc_dart_http2 — B-53 at the owner's direction; they chose the grace-delay option, and reading `package:http2` to implement it found something strictly better, which the round shipped instead and says so. **The dependency ALREADY sends the reset**: `stream_handler.dart:332`, `streamQueueIn.onCancel` enqueues a `ResetStreamMessage` when the state is `HalfClosedLocal`, and `resetStream` cancelling our incoming subscription is precisely what runs it — through the stream's **outgoing queue**, in order behind our own frames. `terminate()` then wrote a SECOND one directly, out of band, and a double reset is what the server answers with GOAWAY `errorCode: 1` (PROTOCOL_ERROR). Which is also why round 387's ablation looked like it removed cancellation and did not: it removed only the duplicate. The guard is the one `releaseStreamId` has used all along, 130 lines up in the same file — terminate only a stream we have NOT half-closed. **RPC-25 for the fourth round running, and the fourth time the sibling held the answer.** No grace, no timer, no trade. `50 ms / let go at the last payload` **DEAD from call 2 → 5 of 5 clean**, and **HTTP/2's duplex column is clean on both links** — `fullDuplex 30/30 ORDER PRESERVED`, `concurrent 8 of 8`, `sequential 8 of 8` — where round 385 read eight MISMATCHes and a dead connection. Canary restores the unconditional terminate and the witness dies with the defect verbatim; the GUARD that matters — an abandoned download must still stop the server's handler, since that path now relies on the dependency's reset — stays green on both sides. **Not fixed, and this round is what separated the halves**: unskipping round 384's witness showed the case where the producer ERRORS and the stream was never half-closed, so the dependency sends nothing, our reset is the only signal, and mid-response it still costs the connection. Skipped again with the narrower reason
- **[387](387-the-reset-is-the-trigger.md)** DEFERRED, rpc_dart_http2 — B-53, the one thing between the owner's goal and a finished HTTP/2 column: websocket and isolate are settled, http2's endings are clean and its DUPLEX is not. RPC-18, because round 385 proved the death happens below rpc_dart's own error paths. **The trigger is now certain, by ablation**: `stream.terminate()` removed from `resetStream`, nothing else touched, and the `50 ms / let go at the last payload` arm goes **DEAD from call 2 → 5 of 5 clean** with the other three unchanged. So the RST_STREAM a cancel becomes is what costs the connection, and nothing else in the teardown does. **Both cheap guards are dead, measured**: gating on `_statusReceived` never fires, because that set is filled when *rpc_dart* parses the trailer and in this race it has not. **And the dependency's own code says the whole thing should be harmless**, which is why it took an ablation to believe — in `http2-2.3.1`, `_terminateStream` writes RST_STREAM only for a stream in an open-ish state, and an incoming `RstStreamFrame` for an unknown stream is a connection error only if it is *idle*, which this is not; the comment right there reads *"RstFrames for already dead (known as 'closed') streams should be ignored"*. Both guards read correct and the connection still dies, so what is left sits between them, inside `package:http2`, invisible from either end's logs. **Why the obvious fix is a TRADE and stays the owner's**: suppressing the reset for a stream we have already half-closed fixes this and breaks what cancellation is for — a client that half-closes at once and then abandons a long server-stream download needs that reset — and both are the same wire state, distinguishable only by whether the peer has finished, which is the fact rpc_dart learns too late. Two routes that are not trades: ask package:http2 for the stream state (its API does not expose it — a small upstream request), or report it upstream, where the reproduction is now four arms and one ablated statement
- **[386](386-the-fix-that-could-not-close-what-it-opened.md)** FIXED, rpc_dart — **the owner audited round 384's fix hours after it landed, from the source plus the SDK's `stream_controller.dart`, with no Dart to run it — and found what it missed.** RPC-25, same lens, same sibling. The class counted first (L-12): **six sites, all in the two convenience SINKS**, the third copy of plumbing `ClientStreamCaller.call(Stream)` and the endpoint bridge already have. `StreamController.close()` THROWS while an `addStream` runs, and `BidirectionalStreamCaller.close()` did `unawaited(_requestSink!.close())` — the throw is SYNCHRONOUS, so it escapes before `unawaited` can wrap it and `await _processor.close()` on the next line never runs; on round 384's own `onError` path it lands in an unawaited future and so **in the zone**, which this file's own comment calls exit 255 in a server process. Measured: `close threw: StateError`, **1 zone error** `Bad state: Cannot add event while adding a stream`. **And the arm that explains why 384 shipped green is in the bench itself**: the same path with a source that ENDS at its throw (an `async*`, 384's witness) reads **0** — an `async*` cannot express "errored and kept going", so the controller was never in the addStream state when close() ran. L-15 one level up: an arm can be void for the FIX as well as for the defect. The owner also found the standing evidence before anything was measured — `request_sink_bounds_its_producer_test.dart:111` read `await caller.close().catchError((_) {})`, closing while a deliberately stalled addStream still pulled, **with the error swallowed**; that line is deleted and the test is now a second witness, failing on the same canary. Fix is the order the responder's own `close()` has always used: hold the subscription in a field, cancel it, then close. Two canaries, two messages; the GUARD (close with no addStream) green throughout. **Not fixed, and the refutations are the other product**: B-54, the producer runs on after the call ends (11 -> 32), where stop-on-send-failure never fires (C-35) and gating on `_processor.isActive` never fires either — both written, measured, reverted rather than shipped as unreachable branches; and B-55, the responder's `responseSink` swallowing a source error into an OK, which is 384's defect mirrored and is filed on a READING because its probe hangs before reporting. Gate: 15/15 SUCCESS, rpc_dart +1558 ~1
- **[385](385-the-consumer-that-let-go-one-beat-early.md)** DEFERRED, rpc_dart_isolate + rpc_dart_http2 — finishing the owner's goal where 384 left it: websocket had the full endings-and-duplex matrix on real wire, **isolate and http2 had only the request-sink question**. RPC-10. **Isolate is clean in both modes** (P-76) — seven endings, three scales, duplex, 8-way concurrency, and the thing no endings matrix had ever run, **zero-copy**, the codec-less branch through `_ensureBidirectionalResponder`; the two modes agree in every cell, with the serialized pass as the zero-copy pass's control. Its counters are reported OVER THE WIRE by a unary `stats` method, since the responder lives in another isolate — which makes the baseline 1, not 0, and is why the unary arm exists. **HTTP/2's endings are clean on both links too** (P-77) and then the duplex section killed the connection over the latent one. Two changes turned eight identical `MISMATCH`es into a reading: a fresh connection per duplex case, which showed the death FOLLOWS a call that succeeded, and a SEQUENTIAL arm beside the concurrent one, which died too and removed concurrency as the variable. P-78 then isolated it to one thing — **when the consumer lets go**: `direct/early 5 of 5 clean`, `direct/late 5 of 5 clean`, **`50 ms/early DEAD from call 2`**, `50 ms/late 5 of 5 clean`. So a consumer that stops reading once it has the messages it wanted — `.take(n)`, a `break`, a UI closing a subscription — cancels while the trailer is still on the wire, the cancel becomes RST_STREAM, and over any real link that lands on a stream the server has already closed. **The relay's own pipe labels say the SERVER closes it, and a `LogController` on the server says rpc_dart never sees it**: the direct arm logs five handled `peer sent RST_STREAM (errorCode: 8)` and survives, the latent arm logs nothing at warning or above and dies — so the cause is inside `package:http2`, B-12's address. One candidate refuted by measurement: the outgoing pump leaves its `addStream` future unhandled unless `_finish()` runs, and handling it at construction changed nothing; reverted, because a change that fixes nothing is not a fix. **B-53 updated rather than duplicated** — same defect, an ordinary trigger instead of an exotic one, and the severity a long way up
- **[384](384-the-sibling-told-the-peer-and-this-one-did-not.md)** FIXED, rpc_dart + websocket + isolate + http2 — the owner's goal: confirm IN PRACTICE that bidi works under any conditions, priority websocket > isolate > http2. Every bidi result in 372-376 was taken on a pair and round 377 asked the real transports only ONE question, so the scope taken was **C-41's seven endings and P-64's duplex cases over a real websocket, direct and through a 50 ms round trip** (P-74, a Dart TCP relay rather than a container). All seven clean on both links, full duplex 30/30 order preserved, 8 concurrent calls no cross-talk, and **both directions past the window at once are independent** (P-75) — the one stall is the MIRROR handler's own coupling, proven by two single-direction controls, and it recovers in full. **Two findings.** First, RPC-15 on the loop's own record: C-41's `deadline: 0` is FALSIFIED (P-63 unchanged now reads 5/20/27) — not a regression but an arm that, before round 373, measured a call **the server had never heard of**, because a bidi caller holding its request stream open sent no initial metadata. *A bench arm whose subject never reaches the code under test reports the same zero as a clean one.* Second, the round's own defect, RPC-25 on a FAILURE PATH: **six request-side failure sites in the class, four tell the peer and two do not, both in `BidirectionalStreamCaller.requestSink`.** An erroring request sink logged and returned, so the handler sat in `await for (requests)` forever — **1 / 6 / 26 streams, responders, live handlers and flow-control entries held on one connection, unchanged after +3 s**, against a half-close and an explicit `abort()` both at 0 (P-72). The sibling is the control AND the model: `ClientStreamCaller.call(Stream)` has notified on this path all along — and it notifies **and then closes**, the half a first attempt at the fix missed. 26 → 0; the paced arm pins the cost boundary (only a request still in flight when the producer throws can be lost, `got` 2/12/52 unchanged). Canaried twice, one per half, with different real messages on core and all three transports. **What the round did not fix is B-53, and it is the interesting part**: on HTTP/2 the notice is RST_STREAM, and a reset racing in-flight responses **destroys the whole connection** — reachable from the public `abort()` with none of this round's code (P-73's matrix eliminates the await and the sink path; what every dead arm shares is responses in flight). The close narrows it enough to pass alone and not under load, so that one test file is skipped naming B-53, and the trade — *a permanent leak on a live connection* versus *no leak on a connection that can still die* — is the owner's, as it was in B-12. **And a test lesson billed in the gate**: the first witnesses asserted after a fixed sleep, passed alone and failed inside the workspace run, because the teardown is the PEER's and a loaded machine makes it late rather than absent
- **[383](383-slicing-the-wire-does-not-do-it.md)** FIXED, rpc_dart + rpc_dart_websocket — B-44 at the owner's direction: a consumer's client-stream upload arrives with the first message's ids null, **86 times in 3.6 days**, on dart2js through a browser WebSocket and never on the VM. The 2026-09-15 session eliminated four candidates and ranked **the network path** second — production is `wss://` through an ingress, every bench so far a bare local socket — and round 378 put the tool for it in reach. RPC-07. Bench **P-71**: the consumer's exact shape, 17 chunks with the ids only on the first, **every field a function of the index** so that lost / reordered / duplicated / mis-decoded are four distinguishable outcomes rather than one — the distinction B-44 says the consumer's logs cannot make. Through toxiproxy's `slicer`, which cuts writes at boundaries the sender never chose: **`first ok | handler got 17` in all four arms**, direct and sliced, at 32 B and at 256 KiB. Not reproduced — WebSocket framing reassembles above TCP, so the parser never sees the split. **INCONCLUSIVE rather than CLEAN, and the reason is the round's own weakness**: the ablation was meant to drop only the FIRST frame and print the consumer's exact symptom, and instead it removed the responder's dispatch too, taking every arm to `handler got 0`. That shows the probe separates *delivered* from *not delivered*, not that it would recognise B-44's shape — and `measurement.md` item 10 says a bench not shown to see the defect makes the verdict INCONCLUSIVE. Said plainly because it is the lesson: **the ablation was designed after the measurement instead of before it**, which is how it came out too blunt to certify the reading. Then the round kept going instead of stopping there, and measured the rest: **the consumer's own `55159adf`** in a worktree (40 sequential and 20 concurrent uploads, `badFirst=0 short=0`), **dart2js** — the axis B-44 says it is exclusive to and which no bench had ever varied — and **coalescing**, via a 64 KB/s bandwidth toxic that makes writes arrive joined. All clean. **Ten axes measured, nothing reproduced**, and the owner closed it on that. **What the round shipped instead is the reason it took six rounds: the failure is SILENT.** A request vanishing between the peer and the handler lets the caller be told the call succeeded over a sequence the handler never saw — so neither end's logs can answer B-44's own question. The pipeline now compares what it ACCEPTED with what it DELIVERED as the call ends: `the pipeline accepted 17 and the handler was given 2 — 15 never arrived`. Counted after the binding is found, so a frame legitimately refused does not read as lost; reported at `error`, never raised, since by then the call has answered. **Two canary mistakes worth keeping**: the first incremented the counter and then dropped, so the counters agreed and the detector stayed silent while messages really were lost — *a canary that leaves the instrument blind reads exactly like a clean run*; and the GUARD asserted delivery before the log, which failed on the same canary and hid whether the detector had spoken. Now: if this is the library's, the consumer's next occurrence names itself — and if nothing ever appears, that is evidence too
- **[382](382-both-request-paths-are-bounded.md)** CLEAN, rpc_dart — B-51, third of the three the owner named, filed from the consumer: the request direction is the one their upload travels, roughly **8 MB in against 400 bytes out**, while the direction four rounds had measured is the cheap one. RPC-05. Bench **P-70**, a handler that reads one message and stalls: **bidi-stall 66 of 2000 (1.0 MB) — bounded, at the window** — with `bidi-drain` at 2000 as the control that makes 66 mean something, since the same rig with a draining handler pulls the producer dry. **And then the ablation corrected the lead's premise, which is the finding worth keeping.** Removing `deferFlowCredit` from `_pipelineFedRequestStream` moved `client-stall` 66 → **2000** and left `bidi-stall` at **66**: that method is the CLIENT-STREAM path, because `_ensureBidirectionalResponder` binds through `_stateBoundStream` and a bidi handler is fed by the transport's own per-stream metering instead. Two request paths, both bounded, by two different mechanisms — and the ablation moved the arm the lead did NOT name while leaving the one it did. Keeper: **an ablation that confirms sensitivity can also correct ATTRIBUTION** — a round that saw `66` and stopped would have reported the right number about the wrong code, which is the same failure mode as round 380's, one level down. For the consumer the answer is unchanged: their upload is a bidirectional handler and that path is bounded
- **[381](381-what-endpoints-promises.md)** FIXED, rpc_dart + rpc_dart_websocket — B-37, carrying out the owner's decision: **leave the getter, document what it means.** Second of the three named, in order. RPC-23, and here there was no prose at all — `/// Active RPC endpoints.` over a getter that is **empty in peer mode**, because every connection there is an `RpcPeerEndpoint`: a type that serves calls, is not an `RpcResponderEndpoint`, and therefore cannot appear in a `List<RpcResponderEndpoint>`. So an empty list means *none of this kind*, never *no connections* — and round 354 measured what that reading cost, a graceful drain shutting the server down on top of live calls. That defect is fixed and does not use this getter; what remained was the promise nobody wrote down. Now written, including WHY the narrow type stays, so the next reader does not re-litigate it: widening breaks every external implementor and hands callers something that may not serve calls, a second getter leaves this one quietly lying in one mode. No signature moved. **No canary, because prose has no runtime — and none is needed here**: the claim is checkable from the TYPES alone, since the getter is `whereType<RpcResponderEndpoint>()` over a `List<RpcEndpointBase>`, so "empty in peer mode" is what the signature says rather than an observation that could age. Left standing and said so: a peer-mode caller still has no supported way to enumerate what is serving — that is the decision, not an omission
- **[380](380-the-window-that-bought-thirty-eight-times.md)** RETRACTED, rpc_dart_websocket — the owner had just decided B-47 (derive `initialSendWindowBytes` from `maxMessageSize`), **and the decision was taken on a claim I wrote in round 366**: *"the park buys nothing that can be named."* RPC-15, because before changing a shipped default you measure the claim it rests on. The field's own doc comment contradicted it with numbers, and `measurement.md`'s rule is that a measured table inside a comment is somebody else's run rather than evidence — so it was measured again, over a real socket at 50 ms RTT with a handler that never reads, 4 KiB frames, 40000 offered: **no window 40000 frames / 156.25 MiB, shipped 64 KiB 1039 / 4.06, `= maxMessageSize` (16 MiB) 5108 / 19.95.** The doc was right to within 0.01 MiB. **The window buys a factor of 38, and the decided fix would have weakened it fivefold.** Both statements are true of different things and 366 generalised the narrow one: for ONE message larger than the window the park really does buy nothing, because the gate admits on `credit > 0` rather than on fit — but from the second frame of a BURST the window is the only bound a sender has before its first grant, and a burst is what the field exists for. 366 measured 2, 3 and 8 frames and never ran a flood. The defect 366 actually found (`finishSending` overtaking a parked send) is fixed; a park of one round trip is not a defect, it is flow control working. Nothing changed in the library — the numbers are pinned instead by `initial_window_bounds_a_flood_test.dart`, so the claim cannot be made again unmeasured, with the `null` arm as the control that proves the rig reaches the regime. Keeper, and it is about this loop rather than the code: **a decision the owner takes on a round's sentence is only as good as that sentence, so the round that carries it out re-measures the sentence first.** Reported before acting, not after. Where the need goes instead: raise the window in the CONSUMER's policy, where the chunk size is known
- **[379](379-the-gate-that-failed-on-a-missing-file.md)** FIXED — the owner asked to bring the backlog up to date, and B-48 was the entry to check first because I filed it myself in 368 with *"not established: whether it pre-dates round 368"* — an admission the round had not finished. RPC-15. Both standing theories were wrong: not Chrome (websocket's arm passes 4/4 on the same machine), not round 378's IPv4 trap. **The file does not exist.** `rpc_dart_isolate/test/` holds no `web_smoke_test.dart`, and the `test:web` script named it anyway. `dart test` answers a missing path by building a load suite for it and starting a browser that then has nothing to connect to, so the failure surfaces as `BrowserManager._start` timing out — **which is exactly the cold-start flake the comment three lines above it describes.** Every retry and every raised timeout read as confirmation of the wrong theory, which is why it survived its own filing, round 377's work in the same package, and two deliberate re-runs at 3x and 6x. The "does it pre-date 368" question is answered by the cause: the path was wrong for as long as it has been in the script. Path removed with a comment saying why its absence looks like a flake; **`melos run test:web` is green end to end for the first time this session**, and the two `web_worker` files that were always in the list still run — the arm was corrected, not disabled. Keeper: **a red that reads like a known flake is the hardest kind to see, and the tell is that it survives the remedy the flake would respond to** — three retries and a doubled timeout changed nothing, which should have been the signal much earlier. Left open deliberately: no smoke test was WRITTEN for the package, because adding coverage is a different job from repairing a gate and inventing it would hide the question
- **[378](378-the-link-that-did-not-change-the-answer.md)** CLEAN, rpc_dart + rpc_dart_websocket — the gap 377 left at the top, and the one the loop's own data made uncomfortable: **rounds 370, 371 and 374 each fixed a missing pause and measured it on `RpcChannelTransport.pair()`** — the harness P-58 exists precisely to warn about for round-trip-shaped questions, which back-pressure is. Three FIXED rounds resting on a bench the journal distrusts for their own subject. RPC-15. The owner suggested **toxiproxy** and it turned out to be installed already. Bench **P-68**: a real websocket server, the client through toxiproxy with a 50 ms latency toxic on each stream. `bidiPump` **70 direct / 70 through 100 ms RTT**, `serverStream` 74 / 70 — the window either way. **The ablation had to run THROUGH THE PROXY or it would only prove the direct rig sensitive**: with 374's pause forwarding off, `bidiPump` goes to **2000 in both links** while the sibling never moves in any cell. **Latency does not change this answer** — the opposite of what it did for P-58, and the reason is worth keeping: P-58 asked about a state that only EXISTS while a grant is in flight, so removing flight time removed the state; this asks about a bound applied by a `pause()` on the consuming side, which happens whether or not the wire is slow. So 370/371/374 stand, now for a stated reason rather than by default. Environment left as found — the container this round created was removed and the other project's, already running with three toxics, was not touched, verified by listing before and after. Trap carried: **reach a docker-published port over IPv4 explicitly** — `curl http://localhost:18474/...` returns `000` and `curl -4 http://127.0.0.1:...` returns 200, the same `::1`-first trap the repo's own `test:web` works around with `NODE_OPTIONS=--dns-result-order=ipv4first`, met in a second place. The rig now exists for what was not asked: bandwidth, jitter, and **slicing** — the interesting one for a framed protocol, since it splits writes at byte boundaries the sender never chose
- **[377](377-the-same-question-on-real-wire.md)** CLEAN, rpc_dart_websocket + rpc_dart_http2 + rpc_dart_isolate — the gap 376 named as the largest: **every bidi measurement in 372-376 was on an in-process pair.** 373's fix lives in core, but each real transport builds its own metadata frame and performs its own open, so *core announces the call* is a claim about core, not about the wire. RPC-10, asked of all three real transports with real servers on loopback and a real spawned isolate. All clean — `silent` **3 DONE** on websocket, http2 and isolate, against a `control` (request stream that CLOSES at once) of 3 DONE. **Every value is a good one, so the round rests on the ablation** — and removing 373's dispatch from CORE, which all three resolve from local source through the pub workspace, collapses every `silent` to **0 HANG** while every `control` survives. **So the defect was real on all three real transports**, not only on the pair 373 measured, and one core fix carries them all. The reason it is transport-independent is worth keeping: the open is announced by the CALLER and dispatched by the shared PIPELINE, so a transport only has to carry a metadata frame it already carries — http2 being the one worth checking hardest, since the shape maps onto HEADERS then DATA and a caller with no message must still get its HEADERS out. Six new tests, one witness plus control per package. L-05's other half: a gate ablation proves SENSITIVITY, not PORTABILITY — this round buys the portability by running the real transports instead of arguing from the shared layer. Remaining, narrower than before: **latency** (all three on loopback — adequate for THIS question, which is not flow-control shaped, and NOT adequate for the flow-control results of 370/371/374), the request direction of `_pipelineFedRequestStream`, `abort()`, a deadline mid-duplex, and dart2js
- **[376](376-the-fix-proven-on-one-wiring.md)** CLEAN, rpc_dart — the owner asked what might have been missed, and the honest answer began with the round just shipped: **373's fix was proven on `RpcChannelTransport.pair()` with a plain caller/responder pair, and that is one wiring out of several.** A fix that covers the case you measured and not the ones you did not is L-12's under-delivery from the other end. RPC-15, re-measuring the loop's own record. Three routes into the same dispatch: the **peer endpoint** (filters inbound by stream-id parity), the **zero-copy branch** of `_ensureBidirectionalResponder`, and **eight concurrent calls** on one connection. All clean — `silent` 3 DONE / 3 DONE / 8 of 8 against a `control` (request stream that CLOSES at once) of 3 DONE. **Every value is a good one, so the round rests on the ablation**, and the ablation says two things at once: removing 373's metadata dispatch collapses every `silent` arm to **0 HANG** while every `control` survives — so the probe sees the defect on all three wirings **and the defect was genuinely present on all three**. The fix is load-bearing there, not merely untested. Trap worth keeping: the zero-copy arm first reported `ArgumentError` in BOTH columns, which is measurement item 4 — *when the control shows the same symptom as the case under test, the bench is wrong, not the library* — `RpcChannelTransport.pair()` does not support direct objects and `RpcInMemoryTransport.pair()` does. Pinned by four tests so the coverage cannot quietly regress. **The largest remaining gap is named rather than left implied**: every bidi measurement in 372-376 is on an in-process pair, and http2, websocket and isolate each build their own metadata frame — whether a HEADERS-only open reaches the peer there is unmeasured and needs a transport-level bench
- **[375](375-the-count-that-answers-the-wrong-question.md)** FIXED, rpc_dart — the owner asked to settle B-50 and attached consumer evidence: rhyolite moved the blob upload this record was written about **off client-stream and onto bidirectional**, acknowledging each blob on the response stream as it is durably written. That evidence lands on round 369's ANALYSIS and **refutes round 369's proposed FIX**, which is the more useful outcome — and the refutation is sharper than my own argument was. `x-rpc-requests-consumed` **answers the wrong question**: a count at the end reports what the library HANDED the handler, while resumption needs to know WHICH pieces are durable, and a handler can be given 17, commit 3 and fail. 369 optimised *tell a full read from a partial one*; the real question is *where do I resume*, and the first does not yield the second. Rejected on that, not on cost. RPC-23, two prose/code divergences. **`droppedRequests` documents a reader that does not exist** — one declaration, two increments, zero readers — and **four paths were driven at it across two rounds and none arrived** (`shortRead`, `shortHold`, `short`, `neverReads`, 0 `DROPPED` records each); reading the code for why, one increment needs a peer sending past its own half-close (a protocol violation) and the other needs `detachRequestSink`, which fires from an `onCancel` that did not. And `addClientStreamMethod` said nothing about the behaviour B-50 measured (17 sent, 3 read, caller told OK, byte-identical to a full read). Both comments now say what the code does, and the API routes a resumable transfer to `addBidirectionalMethod` with a per-message ack, naming the reason the library cannot do better: **it knows what it handed over, never what was committed.** No canary — prose has no runtime — so the evidence is P-60's two controls, reused unchanged. **The honesty problem is now a DECISION rather than a gap**: no cheaper mechanism exists, because the caller knows what it sent and only the peer knows what was read. What the round delivers is that nobody arrives there by accident
- **[374](374-the-copy-an-ordinary-application-reaches.md)** FIXED, rpc_dart — the last open thread in the bidi goal, and the one round 371 named and deliberately did not sweep into: `_pumpBidirectionalResponses` is a **THIRD** implementation of the job 370 and 371 fixed on the two sinks. **It matters more than either of them** — the sinks need `BidirectionalStreamCaller`/`Responder` built directly, while this pump is what an ORDINARY application reaches, an `async*` bidi handler on a contract. Bench **P-65**: **2000 of 2000 — 31.3 MB — against a control of 68 (1.1 MB), the window.** The control is a server-stream handler on the same rig whose relay already forwards pause, differing by exactly that, unchanged before and after. The `await for` pauses the relay while a send is in flight and nothing passed the pause on, so the relay was an unbounded buffer between the handler and the wire — while `ServerStreamResponder` has the same relay for the same reason and DOES forward it, with a comment saying why. Two files, no import between them, the analyzer sees two correct methods: RPC-25's shape in its clearest form yet. **2000 -> 68, exactly on the control — the third time in three rounds** (P-61 68 against 66, P-62 68 against 68). Canary gives a number: `Actual: <2000>`; GUARD (an ordinary bidi call still delivers 40 in order) green on both sides. Named rather than implied: the REQUEST direction of the same pump was not measured — it reaches the handler through `_pipelineFedRequestStream`, a different mechanism with its own `deferFlowCredit` accounting and its own bench
- **[373](373-the-subscription-that-never-reached-the-server.md)** FIXED, rpc_dart — the second half of the owner's bidi goal: 372 took the leaks and came back clean, this takes **correct handling** — whether the two directions are genuinely independent, which is the one property bidi exists for. RPC-20, *the window before the first listener*, where the party that has not started is the CALL ITSELF. Bench **P-64**. **The natural shape of a bidirectional call is a SUBSCRIPTION — open the channel, listen, maybe send later — and that call did not reach the server at all**: `0 HANG` against a control of `5 DONE`. **The control is one line of the caller's own code** — the same handler driven with `Stream.empty()` instead of a request stream that never closes — so the trigger is named exactly: the HALF-CLOSE, not the payload. Same for a server that finishes first while the caller holds its request open: `0 HANG`. **Two hops, and instrumenting both at once is what separated them (L-07)** — the probe samples the server mid-call, because "the caller got nothing" cannot distinguish *the responder never heard of this call* from *it answered and the answer was lost*: `openStreams/responders/metadataStreams` went **0/0/0 -> 1/0/1 -> completes**. Hop 1, the caller: initial metadata is sent by the first request or by the half-close, and a subscription does neither — `CallProcessor.finishSending` already carries a comment about this exact failure, fixed for the zero-message call that DOES half-close and left behind for the one that does not. Hop 2, the pipeline: the responder is dispatched only from `_handleDataMessage` and `_handleEndOfStream`. **Had only the caller's counters been read, hop 1 would have looked like the whole defect and the round would have shipped a fix that still hangs.** After: `0 -> 5` and `0 -> 1`, with `outlivesHalfClose` (10) and full duplex (30 each way, order PRESERVED) unchanged, so it is not a blanket "more messages get through". Scoped to bidi deliberately — the other three shapes are guaranteed to send a request frame or a half-close. **P-63 re-run after the fix: all seven endings still zero on all eleven counters**, so dispatching earlier does not leak. Two canaries for two halves, and both produce the SAME three red witnesses, which is right for two hops in series; the GUARD (zero messages WITH a half-close, the neighbouring case that already worked) stays green under both. Said rather than hidden: **the failures are timeout-shaped**, the weak form `canary.md` names — unavoidable when the defect is a call that never happens, so each carries a real diagnostic through `fail(...)` and the third asserts an EVENT at the peer rather than polling a gauge (L-11)
- **[372](372-seven-ways-to-end-a-bidi-call.md)** CLEAN, rpc_dart — the owner set a new goal: take the bidirectional shape seriously — bugs, leaks, correct handling. Three questions; this round takes the LEAK one, because it is where bidi differs structurally: two independent directions means the most ways to END, and a leak shows up per ending rather than per call. Bench **P-63**: eleven counters, seven endings, three scales, ONE connection throughout (L-08) — `openStreams`, `activeResponders`, live handlers, transport `activeStreams`/`streamControllers`/`statusSeen`, and flow control's `sendCredit`/`deferred`/`owedConn` **on both sides**. Ordinary call, consumer cancel, token cancel, handler throws, deadline, a caller that never half-closes, and unary as the control shape. **Every cell zero at 5, 20 and 60 calls** — three scales because one reading cannot separate retention from churn (measurement item 7); the curve is flat at zero, not falling. **The round rests entirely on the ablation, because a zero is suspicious** (item 8): removing the bidi responder's cleanup makes the SAME counters climb linearly — **5 / 25 / 85 on `openStreams`, `activeResponders` and `sendCredit`** — so the instrument sees this class at these scales. Restored, `git diff --stat` empty, re-measured back to zero. `neverFinish` is the cell worth naming: a caller that never half-closes and walks away leaves nothing, which is the shape that on other paths has historically parked a responder for good. Three exclusions stated so the negative is not read wider than it is: RSS and anything no counter names; endings made of LATENCY, which the in-process pair flattens (P-58); and duplex SEMANTICS — whether the two directions are genuinely independent — which is a different bench and is the next round rather than folded in here (L-12)
- **[371](371-the-mirror-of-the-same-missing-pause.md)** FIXED, rpc_dart — the half round 370 named under `## Not fixed` and this round took immediately rather than letting it age: `BidirectionalStreamResponder.responseSink` has the identical listener shape, and the damage is on the SERVER — one consumer that stops reading makes the handler materialise its whole output in memory. Bench **P-62**, the mirror of P-61: a caller that opens the stream and pauses, a 1 MB window, 16 KiB messages, counted inside the HANDLER's producer. **2000 of 2000 — 31.3 MB — against the control's 68 (1.1 MB), which is the window.** The control is `ServerStreamResponder` through the ordinary endpoint API, which already forwards pause via `relay.onPause` and differs by exactly that; it reports the same 68 before and after, so the reading is of the mechanism and not the timing. **2000 -> 68 after, landing EXACTLY on the control** — as 370's fix landed on its own sibling. Same fix shape (pause before the send, resume on completion, behind a `finished` flag), and round 368's guard preserved: the failure is caught in `.catchError` rather than raised out of a listen callback. Canary gives a number: `Actual: <2000>`, 31.3 MB, GUARD green on both sides. **All four gates green including `test:unit`** — the one 370 could not claim — with the runs paced 20 s apart, which is the only difference. Two things named rather than discovered later: a direct `responseSink.add()` is still unbounded (the `StreamSink` contract), and **the endpoint's own bidi pump was not measured** because it does not use the sink at all — `_pumpBidirectionalResponses` drives its own loop, making it a THIRD implementation of this job rather than a fourth copy of this one. Keeper: **when a round declines to widen its class, take the widening as the next round rather than filing it** — the cap had just been raised, and the bench was already written
- **[370](370-thirty-one-megabytes-of-demand-nobody-asked-for.md)** FIXED, rpc_dart — B-49, filed by round 368 and left for a bench; of everything open it is the only item clearing the severity bar on its own (unbounded memory) and finishable in one round, because the fix already exists in the sibling. The owner was asked where to point the round and said *decide from the measurements* — which pointed here, 369 having found client-stream's defect to be a wire decision that is his, and server-stream clean on five edge cases across two rounds. Bench **P-61**: a handler taking one message and stalling, a 1 MB window, 16 KiB messages, counted INSIDE the application's own producer so it measures demand the library created. **`requestSink` pulled 2000 of 2000 — 31.3 MB — where the sibling `ClientStreamCaller.call(Stream)` pulled 66, which is 1.03 MB, i.e. exactly the window.** That the control lands ON the window rather than merely lower is what makes the bench a measurement and not a timing artefact; and **2000 is the probe's ceiling, not a plateau** — the producer was exhausted, and the file upload the owner described has no such ceiling. Mechanism: a `StreamController` under `addStream` stops pulling only while its subscription is paused, and this listener never paused, so `_sendSequence` was an unbounded queue in front of the transport — the exact defect the sibling documents having fixed on its own side. **2000 -> 68** after, against the control's unchanged 66: one message in flight and one in the controller, the shape of the pause. Round 368's guard survives the change of form (callback now synchronous with `unawaited(...catchError(...))`), verified against its own witness. Canary — `pause()` off in place — gives a NUMBER, not a timeout: `Actual: <2000>`, 31.3 MB, while the GUARD (all 40 delivered in order) passes on both sides. Witness polls to the threshold rather than counting after a sleep, because the producer runs in this process. **Gate not claimed clean**: `test:unit` failed once on the third heavy suite in a row and **the test could not be named**, which `config.md` requires before calling anything a flake — two further runs green, load 4-6, likeliest candidate this round's own 10 s GUARD wait, recorded as a guess and **the threshold was not loosened to make it go away**. Two things said rather than implied: a direct `sink.add()` is still unbounded, and that is the `StreamSink` contract (`add` returns void, there is nowhere to push back) — the bound reaches `addStream` producers, which is what an upload uses; and the mirror `responseSink` has the same listener shape and was NOT measured, because widening a round's class at the end is how a subset gets reported as the job (L-12)
- **[369](369-ok-over-three-of-seventeen.md)** DEFERRED, rpc_dart — the owner named client-stream and server-stream as the priority and then gave the use that decides what "correct" means: **resumable upload of large CDC-chunked files, where re-sending the whole file is the cost being avoided.** RPC-25's detector again — the same question of both shapes — and the question that use makes decisive: *can the caller tell that the other side read everything it sent?* Bench **P-60**, 17 messages, three controls in one run: `shortRead` **17 sent / 3 given to the handler / caller told OK**, against `fullRead` **17/17/OK** — differing by exactly one line of handler code and **byte-identical from the caller** — while `throws` returns `RpcStatusException`, so the bench can read a non-OK ending and those OKs are the call genuinely being reported successful. `shortHold` holds the answer 400 ms to keep the stream state alive and changes nothing. **Server-stream is clean** (its consumer cancelling stops the handler), which is what makes this a divergence rather than a blunt bench. The later frames die as trailing frames of a torn-down stream at `internal` level, so **not one log line** either: for an upload the caller records the file as stored. DEFERRED because the fix is a wire change — **B-50**, written up with the design rather than as a bare question: *resumption is the application's and its place already exists* (the library knows what it HANDED OVER, never what the handler COMMITTED — 17 given, 3 written, then a crash — so a library-side "accepted" count would look authoritative and not be; `nextOffset` belongs in the response message), while *honesty of the status is the library's and is what is missing* — and an ERROR is the wrong shape, because an early answer is USEFUL mid-upload ("I already have this chunk"), so the ask is a SIGNAL (`x-rpc-requests-consumed` in the trailer) and not a refusal. A mid-upload disconnect is already honest (368's EC2), and "how much do you have" afterwards is an application round trip — ListParts, tus HEAD — not a transport concern. Second finding, separable and needing no decision: **`droppedRequests` documents itself as read by the pipeline at the end of a call, and `grep` returns the declaration, two increments and no reader** — neither measured path reached it (0 `DROPPED` records on both arms), so the promise is false and the counter may be dead; establishing that needs a third bench and the probe budget (3) was spent, so it is open rather than answered. Keeper: **ask what the USE makes decisive before deciding what "correct" means** — the same OK that is unremarkable for an RPC is data loss for an upload
- **[368](368-the-callback-that-could-kill-the-process.md)** FIXED, rpc_dart — the owner asked mid-round for **full correctness of all four call shapes in ordinary AND edge cases**, which is a parity question, so the instrument was RPC-25's detector (ask the same thing of all four siblings, the divergence is the defect) and the shape it produced was RPC-13's, whose `breaks:` is *a process crash*. Class counted first (L-12): **11 `async` callbacks handed to `Stream.listen` in `lib/src/rpc/streams/`, 5 already carrying a try/catch and 6 not.** Bench **P-59**, five edge cases asked identically of unary / server / client / bidi. **Four of the five came back identical on all four shapes** and are filed as the negative **C-40** — ordinary calls, a dead transport mid-call, a consumer walking away, and the one that matters most, *a handler that throws after emitting two*: every streaming shape delivers its partial output **and then an error**, so there is no silent truncation anywhere. The fifth diverged, and its two rows are each other's control because they are the same job written twice — the library driving a producer the application handed it: `ClientStreamCaller.call(Stream)` wraps its send in `.catchError` and reports **uncaught +0**, `BidirectionalStreamCaller.requestSink` did not and reported **+1**. An `async` listen callback has nothing awaiting it, so `CallProcessor.send`'s deliberate `RpcStatusException(14)` for an inactive call went to the zone — exit 255 for a server, the same death `_detached` was written for after two production replicas. Reached by nothing worse than a cancelled call whose producer had not noticed yet; the sink is still open, so the push is legal. **Six guards added, matching the shape `responseSink` already used**, and `UnaryResponder`'s 90-line listener extracted to `_onIncomingMessage` so its guard has one place to sit rather than three. `+1 -> +0`, **CONTROL row unchanged at +1 so the instrument did not go blind**. Two canaries, one per witnessed site, each a `rethrow;` in the new catch: `Actual: [RpcStatusException(14): Request not sent: the call is no longer active]` and `Actual: [RpcStatusException(14): Transport is closed]` — real messages, no timeouts, and each killed ONLY its own witness while both GUARDs stayed green. Said plainly rather than implied: **4 of the 6 guarded sites have no witness** (L-04 — a guard against a crash is witnessed by an absence), each named. Two leads: **B-49**, the same sibling pair one axis over — `requestSink` has no back-pressure where `call(Stream)` pauses, and the sibling's own 32.8 MB figure belongs to the other API and was not borrowed; and **B-48**, `test:web` red on `rpc_dart_isolate`'s Chrome arm, which fails at `BrowserManager._start` before a line of Dart runs, identically at 3x and 6x, against a control where the same Chrome and the same local core pass 4/4 elsewhere. Keeper: **an `async` callback handed to `listen` is an unguarded `unawaited`** — the repo already knew that for teardown work and had not asked it of the call shapes
- **[367](367-the-flow-control-copies-are-all-watched.md)** CLEAN, rpc_dart + rpc_dart_http2 — the owner asked to refactor flow control; the round took RPC-25's question instead of the change the request named, because **the lens is the only thing that can justify a refactor and its bar is drift, not line count.** Counted the class first (L-12): **three** copies of flow-control accounting, not two — core's credit-and-grants scheme plus the un-consumed budget written twice in http2. Detector step 3 found every http2 difference to be a specialisation reachable only on its own side (the `_fcDeferred` gate, the refusal action, the `_fcForget` sites) plus **one genuine drift with three answers — what `close()` clears** — which nothing reaches, because after close every write is refused by `isClosed`, the maps die with the object, and a reconnect builds a new transport (360's rule: drift is a defect where something reaches it). Then round 331's unequal-coverage arm, the same rule ablated in each copy: **`+218` → `+212 -6`** (responder), **`+217 -1`** (caller), **`+1485 ~1` → `+1484 ~1 -1`** (core). 331's bar is one watched and one NOT; 6/1/1 is unequal in degree only, so the extraction would inherit coverage the copies already have — *what a no-drift candidate earns: nothing.* And `RpcFlowController` is not an RPC-25 candidate at all: **one** copy of the credit scheme, no sibling to compare. Declined as a loop target and said so plainly to the owner, whose question was wider than the lens — the readability case for splitting `_fcMetered` and `_fcForget`, each serving two mechanisms, is untouched and is ordinary work rather than a round. Two observations kept: the core's sole witness for the charge-both rule fails by **30-second timeout**, the weak form `canary.md` item 2 names; and at 367 of a 370 cap, a ~400-line extraction across a file eight test files read diagnostics from is the multi-round tree the skill says not to start. Keeper: **when the owner asks for a refactor, measure whether the duplication is watched before agreeing it is duplication worth removing** — the answer here was three ablations and no change to the flow-control code. Two gates were RED before the round touched anything and both were repaired rather than reported: `analyze`, on the `curly_braces` lint `config.md` names by name (left by `2ec53853`, one line), and **`loop.py lint` at 33 errors, every one of them round 366's** — four records with no line in any index, off-schema frontmatter on seven files, `RPC-01` not listing 366. Repairing C-38 also caught a rule-one divergence: it says the fix was *"Reverted; the transport is untouched"* and `_refuseIfClosed` is in the transport today, by that record's own recipe
- **[366](366-the-end-that-sailed-past-a-waiting-message.md)** FIXED, rpc_dart — **brought in from outside the journal**: a consumer's blob uploads began failing on the release that bumped 5.0.1 → 6.0.0 and on nothing else, the server refusing the same blob fifteen times with `Declared length 2442197 does not match received 524288 bytes` and not one reconnect in the logs. RPC-01, whose shape is credit accounting whose release is decided elsewhere. An end-of-stream carries no payload, so nothing meters it — and `finishSending` wrote it immediately, **overtaking a `sendMessage` still parked in `_fcAwaitCredit`**. The peer then sees a stream that ended with N-1 messages and a clean end, indistinguishable from a sender with nothing more to say, while the sender is told nothing because the parked send is never woken. Witness on pristine HEAD: `Expected: RpcStatusException with statusCode 14 / Actual: 'never completed'`. Fixed by marking the stream finished and WAITING for a parked send before the end goes out; only parked sends are tracked, because an unconditional await adds a microtask hop to every send and that hop has reordered frames on this transport before. **Two canaries and only one carried weight** — removing the wait reproduced the truncation (`Expected: <4> Actual: <3>`), while a second refusal in `_fcAwaitCredit` left both tests green and was dropped rather than committed, *a change no canary can kill is a change with no evidence behind it*. Bench **P-58**, which exists because `RpcChannelTransport.pair()` answers "never parks" in every row: the parked state needs a real socket and a real RTT, and park duration tracks RTT exactly (20/40/200 ms). Trap kept: **the credit gate admits on `credit > 0`, not on whether the message FITS**, so a 256 KiB frame passes a 64 KiB window and the SECOND frame is the one that parks — reading the constants predicts the opposite, and did. Left open as **B-47**: the initial window is smaller than a message this library's own users send, which is a shipped-default trade the owner must make. **This round did not run `loop.py lint`** — see round 367, which found and repaired its four unindexed records and their off-schema frontmatter
- **[365](365-the-clock-one-sandbox-does-not-have.md)** FIXED, rpc_dart_wasm — the owner's item 8 and the iOS half of item 15, both unblocked when a simulator came up mid-session, plus B-42 closed. **The first two-platform device gate of the session** — Android `+24 ~2`, iOS `+26` — and running the SAME guest on both is what turned three open questions into a defect. Sent to ask whether an offscreen WKWebView throttles guest timers: it does not, iOS median lag 0-1 ms up to a 100 ms delay and ~99 ms on a 1 s timer, Android a flat 3-7 ms. **The finding was the other arm**: the identical guest, identical call, threw `RpcStatusException(13): Internal server error` on Android. dart2wasm's glue calls `performance.now()` (`guest.mjs:123`); a WKWebView is a full browser environment and supplies it, `JavaScriptSandbox` is a bare V8 isolate and does not — so **any guest using `Stopwatch` crashed on Android and worked on iOS**, opaquely. Fixed with a `Date.now()` shim installed only when `performance` is absent; millisecond resolution, so a guest timing sub-millisecond intervals reads 0 rather than a wrong number. The canary is also the field symptom — disabling the shim reproduces exactly what a user sees. **Item 8's defensive fix deliberately NOT applied**: the ordering it defends was measured to hold over 10000 frames on both platforms, and chaining the fetches would serialise every send, removing the pipelining those figures rest on — a speed trade, and the config says ASK before making one. Filed as **B-43** with three options and no recommendation. Keepers: **ablating each language separately is not the same as running the same code on both** — 348's rule catches an unported fix, this is a gap in what the two HOST ENVIRONMENTS provide for free, so list the JS globals each boot script assumes; and **a bench built for one question found the defect**, because the timer probe was the first guest code here to use a `Stopwatch`
- **[364](364-the-readme-named-an-engine-that-cannot-run-it.md)** FIXED, rpc_dart_wasm + rpc_dart_websocket — the owner's list, P2 item 15, four asks split by what could be measured and the split stated up front: engine name FIXED, frame cost FIXED (Android), proxy note FIXED and verified, offscreen `Timer` latency NOT DONE (iOS only). RPC-23's **strongest and cheapest-to-detect form: prose naming a component that CANNOT do the job.** The README said iOS used `JavaScriptCore`, twice — and JSC has no WebAssembly, so if it were true the package could not run a dart2wasm guest on iOS at all, which it does through an offscreen `WKWebView` with a custom scheme handler. Refutable from the component's own capabilities, no diff archaeology; and a search for the NAME caught both mentions where a read-through would not. Corrected WITH the reason, so it cannot come back. The cost question had no answer in the README at all, so it was measured: **`empty unary p50 12.9 ms`, `1 KiB 12.3 ms`, `64 KiB 15.5 ms`, `1 MiB 114.9 ms`** on an Android 11 emulator — published with the hardware named and the SHAPE explained (flat to 1 KiB because the price is the boundary not the payload; p99 ~10x p50 because a call can miss a driver tick). **The claim written into the websocket README was verified rather than asserted**, and its source was a repository comment — exactly what this lens says not to trust: running the existing probe turned it into `96 MiB sent → 1 chunk → 96 MiB`, and `RpcWebSocketServer` was grepped for a connection limit before the README stated there is none. Documentation has no switch to flip, so that verification IS the canary. The unmeasured iOS timer latency is named in the README where a reader would otherwise assume parity. Keeper: **some stale prose is not out of date but IMPOSSIBLE** — ask what the named thing can do and whether the package works
- **[363](363-the-check-that-would-have-refused-everything.md)** FIXED, rpc_dart_wasm — the owner's list, P2 item 12. `stripModuleSyntax` is four literal `replace` calls per plugin with no check that they accomplished anything, pinned to the module forms dart2wasm emits today; `export let`, `export default`, a trailing `export {a, b}` and every `import` survive it. **The item's premise held on one platform and not the other**: it predicted `compile is not defined`, and Android's message already named the token (`SyntaxError: Unexpected token 'export'`) because `evaluateJavaScriptAsync` rejects on it — the confusing symptom is iOS's, where a SyntaxError kills the whole `<script>` tag, which the Swift source says two comments away and which nobody has run. **The control was the finding**: swapping the line-anchored rule for `plainJs.contains("export")` took the device suite from `+21 ~2` to **`+3 ~2 -13`**, every guest boot in the repository, because `export`/`import` appear 19 times in the real glue and only 4 at statement position — `dartInstance.exports`, `importObjectPromise` and a comment naming the `'import'` API are the rest. Written without a regex in either language, so a trimmed `hasPrefix` means the same thing in Kotlin and Swift where no two regex engines would. **Shipped on BOTH platforms with only one witnessed, departing from round 357's revert on stated grounds** — 357 changed a live path's behaviour, this adds a check to a path already failing, so a false negative leaves today's behaviour and the dangerous direction is the one Android measured. Filed as **B-42** with the argument written out so the owner can overrule it. Keeper: **when a fix is a new REFUSAL, the arm that must keep working is worth more than the arms that must now fail**
- **[362](362-the-thread-was-not-the-cost.md)** INCONCLUSIVE, rpc_dart_wasm — the owner's list, P1 item 11, taken because an Android emulator came online mid-session. RPC-06's Ask is *was this code RUN, or only read and compiled?* — round 357 answered "compiled"; this round RAN it five times and **the code disagreed with the hypothesis**. The code fact is certain: `CoroutineScope(Dispatchers.Main)` with `Base64.decode` over batches of up to 4 MiB per drain, on the thread that dispatches every platform channel in the app. The measurement says it is not the cost: `before 22/59`, `after 12/70`, **`ABLATED 39/38`** (idle worst / busy worst, ms) — with Base64 put back on Main the main thread is no less available than with the fix. Idle worst ranges 12-39 ms, the size of the effect being looked for. **Run 5 is the canary and it is why nothing shipped**: the fix switched off produced no worse a result than the fix in place, so per *no failing witness, no fix* the change was REVERTED. The negative is the product: the occupancy IS real (busy clearly worse than its own control in 3 of 5 runs) but comes from work that cannot be moved — `evaluateJavaScriptAsync` marshalling 4 MiB across Binder, and `messenger.send`, which must be on Main by contract. **The item's criterion is also unmeetable as written**: "no frames >16 ms" on an emulator whose IDLE control alone reaches 39 ms. Measured through a platform-channel round trip rather than frame timings — Flutter's UI thread is not the Android main thread, and an idle test app renders nothing to be janked. Filed as **B-41** with all five runs and the fix written out. Keeper: **running is not measuring** — a green native gate and a green device suite passed in every arm above and say nothing about a performance claim
- **[361](361-the-only-place-to-authenticate.md)** FIXED, rpc_dart_websocket — the owner's list, P2 item 13, **and the headers half is not hygiene**: a websocket client can authenticate in exactly ONE place, the upgrade request, because there is no second round trip to attach a token to — so `connect()` could not be used against an authenticating server at all. RPC-22 from the CLIENT's side of the pre-protocol stage rounds 274-275 established for the server. Measured at the PEER, off a recording `HttpServer`: `connect(headers: ...)` → `ABSENT`, against `[Bearer t0ken]` for the same header attached by hand — and that workaround costs the reconnect factory, so it trades two security defaults (keepalive, compression-off) to buy one. Bound: **`10016ms` STILL HANGING** against a peer that accepts TCP and answers nothing, → `806ms TimeoutException`. Both parameters had to reach FOUR places or they are decoration — the signature, both `openWebSocket` implementations, and the RECONNECT factory (U-10), because a token that goes only on the first upgrade authenticates exactly once. `Future.timeout` abandons the AWAIT not the WORK, which core learned on `RpcClientConnection`, so the dart:io path closes a late arrival rather than dropping it. **Three canaries, and two of them fail IDENTICALLY — which is the finding, not a flaw**: `connect()` builds one `openChannel` closure for both the first open and the reconnect, so there is a single path and no second source; reporting them as isolating two halves would be a claim the measurement does not support. The timeout witness was REWRITTEN after its first version hung into the runner's 30 s timeout, which reports nothing; raced against a 3 s bound it now fails in 3 s with `STILL HANGING: connectTimeout was not applied`. `test:web` run deliberately: `ws_open_stub.dart` IS the web implementation, so this round changes code the VM gate never executes. Keeper: **when this lens finds a stage, ask what each side must ACCOMPLISH in it, not only what it must be protected from**
- **[360](360-two-routes-into-one-concept.md)** FIXED, rpc_dart — the owner's list, P2 item 14, three core defects filed as one line and taken as one round because they share a shape: each produces a WRONG ANSWER rather than failing, and each has a SIBLING beside it that gets the same question right (RPC-25). **(a)** `RpcStreamIdManager` takes a resume cursor two ways — the `resumeAfter()` METHOD aligns parity and documents it, the `resumeAfter:` CONSTRUCTOR parameter took it raw — so a CLIENT resumed at 4 issued **6, 8, 10**, the SERVER's half of one shared id space, where everything downstream (half-close, release, flow-control credit, `_statusSeen`) is keyed on the id alone. `IRpcStreamIdSequence` states the contract it broke: *"must leave parity intact"*. **(c)** the parser's decompress `catch` rewrapped EVERY throw as *"Decompressed gRPC payload exceeds the configured limit"* — but a decompressor throws for the bomb it was asked to stop AND for malformed or non-compressed input, and the catch cannot tell them apart, so a peer with a CORRUPT frame was told to send less, which cannot help. Both fixed; `_alignedStart` gives the parity rule one home. **(b) DECLINED and filed as B-40**: `_methodPathFromKey` cannot round-trip a dotted service name its sibling `_parseMethodPath` explicitly admits — real drift, and the ordinary path measured CLEAN, so nothing reaches it and the damage is a diagnostic string. Two canaries, each failing only its own witnesses; the GUARDs are what stop each fix overshooting — a correctly-parity cursor must NOT be nudged, and the limit must still be named. **Two gate runs re-run rather than accepted**: `test:unit` failed once at load 15.01 in a package this round does not touch, and `test:web` failed once inside pub's `Entrypoint.ensureUpToDate` rather than in any test, while the device runs' `flutter pub get` was settling; both green on re-run. Keepers: **two ways to set the same field are two implementations** — point the detector at FIELDS, not only classes — and **drift is not automatically a defect; it is a defect where something reaches it**
- **[359](359-the-state-nobody-named.md)** FIXED, rpc_dart_websocket — the owner's list, P1 item 10, both halves. RPC-19 back on a FLAG after 353 widened it to signals, and the defect is a state the flag has **no value for**: `_disconnected` is written only from the OUTCOME of a reconnect — `false` on success, `true` in the catch — while `_reconnectOnce` closes `_inner` and THEN awaits the factory for a whole handshake. Nothing describes the duration, so `_ensureUsable` passed over a closed inner for tens to hundreds of ms. Six methods against three states: **the in-window column should equal one of the two controls and equalled neither** — `createStream id=3`, `sendMessage returned` (accepted and dropped by the corpse, so the caller waits out a deadline for a frame never sent), and `getMessagesForStream` answering **`RpcStatusException(14)`, which is RETRYABLE** and invites the caller to repeat what cannot work. One line fixes it, `_disconnected = true` right after `_inner.close()`, and the in-window column becomes the disconnected column. Second half: `sendDirectObject` was the one send method of three without `_ensureUsable` — and it has **no reachable failure**, because `supportsZeroCopy` is false so the caller pipeline refuses a codec-free call first; fixed for consistency and witnessed at the transport level, where the guard changes the answer from the inner's `UnsupportedError` to the explicit `StateError`. Two canaries, each failing only its own witnesses. **The GUARD that earns its place is "the window ENDS"** — a transport that refused forever would pass both witnesses and be far worse than the defect. Keeper: **a flag written from a `catch` describes an outcome, and an outcome is not a state**
- **[358](358-the-catch-that-catches-nothing.md)** DEFERRED, rpc_dart_websocket — the owner's list, P1 item 9, which round 353's fixture had already tripped over. The item proposes the fix (*"send → no-op"*) and **the proposed fix does not work, measured**: `try { _ws.sink.add(data); } catch (_) {}` was applied and re-run and the crash is identical, because `WebSocketSink.add` queues into a StreamController and the real `sendBytes` runs a microtask later in the zone that CONTROLLER was built in — so a `catch` at the call site sees nothing, and neither does a `runZonedGuarded` there. The give-away is a `runUnaryGuarded` frame BETWEEN the call and the throw. Three things the table settled that the wording did not: **a PEER close is not this defect at all** (the sink keeps accepting until it learns — every peer arm returns), the failure reaches the **ROOT zone** and ends the isolate rather than merely throwing up, and **neither candidate guard can see it** — the crashing row reads `isClosed false` AND `closeCode null`, because within one turn nothing observable has changed. What DOES work, also measured: building the `WebSocketChannel` inside `runZonedGuarded`, whose arm returns normally with the construction zone receiving the error — which makes it fixable at the library's own entry points and unfixable for a user-supplied socket. DEFERRED to **B-39** with both refuted fixes named so they are not retried; it is **the identical decision B-35 is waiting on**, one dependency over. Bench **P-49**, rebuilt once: the first version drove a full endpoint pair and could not tell the arms apart, because `onDone` always won the race — the window IS the subject, so the bench has to address it directly. Keeper: **a sink's `add` is a queue, not a call**
- **[357](357-a-fix-nobody-could-run.md)** INCONCLUSIVE, rpc_dart_wasm — the owner's list, P1 item 7. **The finding is conclusive and the measurement is missing, which is the verdict.** iOS drives host-to-guest with one long-poll `fetch('rpc-wasm:///recv')` whose `.catch` is `_recvRunning = false` and nothing else: the direction dies silently, `recvQueue` grows, and every in-flight call waits out a deadline that is OPTIONAL on this transport. **Android's driver loop already fixed this**, and its comment describes the iOS behaviour word for word — *"the isolate stayed in runtimes, nothing further was evaluated, and Dart was never told"* — while iOS does not even log. The fix was written and `analyze:native` said `PASS swift / PASS kotlin`; **it was REVERTED rather than committed**, because RPC-06's own Ask is *was this code RUN, or only read and compiled?* and round 348 already priced what two green lines from a gate never shown to fail are worth. Five `flutter emulators --launch` attempts and waits up to 240 s never registered a device — though one booted earlier in this same session, for round 356's two canaries — and `xcrun simctl`/`open -a Simulator` are outside the allowlist. Everything is preserved in **B-38**: the sibling comparison, the four-step patch, the reachable witness (a stray `/recv` from the guest supersedes the loop's pending task, a rejection with the runtime alive), and the two expensive traps — back off through `_nativeSetTimeout`, because the next tag declares a hoisted `setTimeout` POLYFILL that queues into the guest's own timer table; and report death by `postMessage`, not by a fetch of the scheme that just stopped answering. Keeper: **a fix that type-checks is not a fix**, and shipping one behind a green verdict line is worse than filing it
- **[356](356-the-zone-ate-the-reason.md)** FIXED, rpc_dart_wasm — **the owner's 6.0.0 list, P0 item 6, and the last of them.** RPC-13 from the CATCHING side: every earlier instance is an error with nowhere to go, this one had somewhere to go and a zone took it. `runZonedGuarded` routes a SYNCHRONOUS throw from its body to its handler and returns normally, so `RpcWasm.run`'s `late final result` was never assigned and `return result` failed on its own variable — a guest author whose `configure` threw was told `LateError: LateInitializationError: Local 'result' has not been initialized.` instead of their bug. **The measurement checklist already had the rule** (D2, *a guarded zone catches only its own side's errors*); what was new is reading it as applying to the zone's OWN body. `runZonedGuarded` appears exactly once in the whole workspace's `lib/`, so the sweep was a grep and the value was in knowing there was nothing else. **Measured on a device because there is nowhere else**: `rpc_wasm.dart` is `dart:js_interop`, so no VM runs it — `test:wasm` is `flutter test`, `test:web` excludes the package, and before this round nothing had ever RUN a line of it but the happy path. The guest's `main()` now fails one boot on purpose and the integration test reads back what the caller got: `LateError...` → `StateError: Bad state: configure exploded on purpose`, `+17` → **`+18`**. **The second half is a finding, not just a fix**: writing the witness needed two boots, which `_boot`'s `_initialized = false` explicitly permits — and the retry was broken, because `endpoint.close()` reaches the bridge only after several awaits while the retry installs its own `rpcWasmReceiveBytes` synchronously, so the late close deleted the LIVE handler (`+9 -9`, every guest test dead). Two canaries failing differently: one corrupts the error, the other kills the pipe. Keeper: **`_initialized = false` is a promise, and the only way to find out it was empty was to take it**
- **[355](355-the-encoding-both-sides-agreed-on-and-neither-said.md)** FIXED, rpc_dart_wasm — **the owner's 6.0.0 list, P0 item 5**, and RPC-06 widened to the place that is in NEITHER language. Round 348's rule is that two scripts in two languages say nothing about each other; the corollary is that a disagreement about what the bytes BETWEEN them mean passes `analyze:native`, `dart analyze` and the whole suite. Both plugins encode text as UTF-8 in one line each (`data(using: .utf8)`, `toByteArray(Charsets.UTF_8)`) and nothing on the Dart side named an encoding at all. `sent -> arrived`: `ascii 22 ch/22 B -> 22 ch` (control, unchanged by the fix), **`cyrillic 17 ch/30 B -> 30 ch`**, `emoji 9 ch/11 B -> 11 ch`, `accents 19 ch/22 B -> 22 ch`, and a death reason reading `Ð³Ð¾ÑÑÑ ÑÐ¿Ð°Ð»`. **The arrived count equals the BYTE count**, which is the signature of one character per byte — print both and the pair names the failure mode. **ASCII is the control and ASCII is why it shipped**: below 128 the two readings agree exactly, `String.fromCharCodes` cannot throw, and the suite's shared helper builds its payload with `text.codeUnits`, so every test passed on input that could not tell them apart. Swept all 14 `fromCharCodes` sites in every `lib/` first: two wrong, both here; the other twelve are ASCII by protocol (http2 keeps header values verbatim on purpose — a `-bin` value is base64 and decoding would double-process true binary) or read an alphabet the code defines itself. **Two canaries, because the fix has a decoder AND a flag**: reverting to `fromCharCodes` gives mojibake, dropping `allowMalformed` gives `FormatException: Unfinished UTF-8 octet sequence` — a throw inside a platform message handler and inside the death report, which would turn a truncated log line into a crash. Keeper: **for any cross-boundary payload, ask what input would DISTINGUISH the two readings, and check that some test sends it**
- **[354](354-the-drain-that-polled-a-key-nobody-published.md)** FIXED, rpc_dart + rpc_dart_websocket — **the owner's 6.0.0 list, P0 item 4**, and RPC-25 on a MAP KEY rather than a method. `RpcResponderEndpoint` and `RpcPeerEndpoint` are sibling subclasses over one `RpcResponderPipelineMixin`; the responder's `collectEndpointMetrics` computed five metrics about responder streams INLINE — `metadataStreams`, `bufferedMessages`, `clientStreamBuffers`, `activeResponders`, `contractKeys`, all from `_respStreams`, which the mixin owns and both siblings have — and the peer's called the shared `collectResponderMetrics()` and stopped. `RpcWebSocketServer._inFlightCalls()` polls `activeResponders` to decide whether a graceful drain is done: `responder 1 / 1746ms / returned "finished"` against **`peer null / 1ms / status 14`**. **`null`, not `0`, and `?? 0` erased the difference** — a missing key and an idle server are not the same fact, and the fallback made them indistinguishable at the one call site that had to tell them apart. `stop()`'s own comment says peer endpoints are deliberately included, *"counting only RpcResponderEndpoint would drain a peer server instantly"*, which described an intent the count could not deliver. Consumers counted first: two servers poll this key, and only websocket can hold a peer endpoint (http2's `_endpoints` is typed `List<RpcResponderEndpoint>`), so one fix covers both. Moved all five into the mixin; the canary fails the new peer witness AND the responder test that predates it, which is the evidence there is one home now rather than two copies. GUARD kept: an idle server still returns promptly, because a count that never falls to zero would pass the witness and hang every shutdown. Filed **B-37** for the `endpoints` getter, which is empty in peer mode and cannot be widened without breaking `IRpcServer`. Keeper: **when a consumer reads a map with `as int?` and a default, ask which producers publish that key — the type system will not**
- **[353](353-reported-not-fatal-was-half-true.md)** FIXED, rpc_dart + rpc_dart_websocket — **the owner's 6.0.0 list, P0 item 3**, and RPC-19 widened past booleans: the object carrying two meanings is an ERROR STREAM. `RpcWebSocketChannel` reports a TEXT frame with a plain `RpcException` under a comment saying it is *"reported rather than fatal, so the connection stays usable"* — and `RpcChannelTransport` answers any channel error into EVERY per-stream controller, deliberately, because *"a connection-level failure is the answer to every call in flight"*. Two unary calls parked in a handler, one text frame from the server: `ok, ok` → **`RpcException, RpcException`**, `connection alive` in both arms. **The comment was half true and the suite checked that half**: `non_binary_frame_test.dart` asserts on the CHANNEL, so every assertion in it passed while one app-level keepalive from a proxy killed every call on the connection — with an `RpcException`, which is not a status and carries no retry semantics a caller could even classify. Every `addError` reaching that subscription was enumerated first, seven sites across core and five transports, and exactly one meant something other than "the connection is gone"; the rest close the connection in the same breath or were already per-stream scoped. Fixed with `IRpcAdvisoryChannelError`, a marker in core checked at the one place that amplifies — the report still reaches `incomingMessages`, where both pipelines log it, so nothing is dropped and no logger plumbing was needed. Two canaries; the third GUARD (a real 1011 close still answering both calls) stayed green under both, which is the property an over-broad fix would have removed. **The first `test:unit` run failed and was not called a flake**: `uptime` read a load average of 20.02, the condition `config.md` names, both packages passed alone, and the whole gate passed at 5.32. Keeper: **when a comment says "not fatal", ask fatal to WHAT** — the thing it names is rarely the only thing at stake
- **[352](352-the-wrapper-that-declared-nothing.md)** FIXED, rpc_dart — **the owner's 6.0.0 list, P0 item 2.** RPC-04, and the sweep is the finding: round 209 looked for wrapper HOOKS — a `transportWrapper` a user passes something to — concluded http2 was the entire surface, and core's own `_ReconnectingTransportProxy` sits outside that definition. It is a wrapper the LIBRARY builds, behind `RpcClientConnection`, which the docs call *"the recommended, transport-agnostic way to get auto-reconnect on a client"*, so it is what most applications hand to an endpoint — and it declared `IRpcTransport, IRpcStreamReset` and nothing else. The same endpoint built twice over the SAME transport: `20 MiB received` direct against `gRPC frame buffer overflow (max: 16777221)` through the proxy, a zero-copy call `accepted` against `Zero-copy requires a transport that supports zero-copy`, and `deferred=1` against `deferred=0`. **16777221 is 16 MiB plus the 5-byte prefix — `const RpcSecurityPolicy()`'s own effectiveMaxBufferedBytes** — which is the argument for measuring the EFFECT: the number names which policy the parser was built from, where an `is` check only says the probe failed. Three of the four losses were missing interfaces and **the fourth was a LITERAL**, `bool get supportsZeroCopy => false`, over a proxy whose `sendDirectObject` has always delegated; no interface is absent, so an `implements` grep cannot see it. Also remembered from the last attach, because the responder pipeline's limit caches are `??=` and a read landing in a reconnect gap would pin the fallback for the endpoint's life. Four canaries, one per capability, each failing exactly one witness. `RpcWebSocketCallerTransport`'s class comment already stated the defect in the abstract, over the class that had it (U-14). Bench **P-44**, rebuilt once: the frame channel carries its own policy and refused the body in BOTH arms. Keeper: **a sweep's conclusion is only as wide as its definition of the thing being swept**
- **[351](351-the-ending-nobody-wired.md)** FIXED, rpc_dart — **the owner's 6.0.0 review list, P0 item 1**, taken in the order the list gives. RPC-05 on the smallest limit in the library: the circuit breaker's one-slot half-open gate. `_checkState()` charges `_probeInFlight`; `_wrapStream` gives it back from the source's `onError`, the source's `onDone` and an abandon timer — and a consumer that LISTENS then CANCELS reaches none of the three, because `onListen` cancels the timer and a cancelled subscription never delivers `onDone`. So `await stream.first` on a half-open probe pinned the gate and the breaker refused **every** later call forever, recoverable only by `reset()`. `cancel-serverStream 0/5 → 5/5` admitted and the same for bidi, against a control (the same consumer without the cancel) that reads `5/5 closed` unchanged on both sides of the fix. **The give-away was a comment** asserting a release from two mechanisms that, by the time `onCancel` runs, cannot fire — U-01 and rule one. The sibling had it right: `rpc_dart_opentelemetry`'s `_wrapWithSpan` ends its span in `onCancel`, and one grep across the interceptors is the whole sweep. Canary: `CircuitBreakerOpenException: circuit is open` from `_checkState`, with both guards green (`+2 -2`) — a drained probe still closes and a failing probe still reopens. Bench **P-43**, rebuilt once: the first control varied the cancel AND whether the source terminated, which cannot tell "cancel skips the release" from "a live source has not released yet". Filed **B-36** for the neighbouring ending that DOES release and answers wrongly — the abandon timer records a fabricated success and closes the breaker on no evidence. Keeper: **enumerate the endings** — four of five had a release and the missing one was the ordinary consumer idiom
- **[350](350-the-buffer-flow-control-never-covered.md)** FIXED, rpc_dart — **B-28, the owner's target at the cap**, open since round 282 because the lead framed it as a behaviour trade for the owner: charge metadata against the send window, or bound the per-stream controller. **It was never a preference.** HTTP/2 applies flow control to DATA only and exempts HEADERS by design, because a control frame that cannot be sent deadlocks the stream it is trying to end — a trailer waiting on a window the peer will only open once it sees that trailer. Candidate 1 was never available, and the protocol also says how headers ARE bounded: by SIZE (`SETTINGS_MAX_HEADER_LIST_SIZE`), not by the window. So the remaining work was to find the weigher, and another round had already written it — `RpcTransportMessage.bufferedBytes` (round 279), whose own doc says *"one home for the rule, because every transport buffers these"*. The per-stream view from `getMessagesForStream` was the one buffer that never asked. Now every frame queued into it is charged against `effectiveMaxBufferedBytes` and released as the consumer takes it; over the bound the **STREAM** fails with `RESOURCE_EXHAUSTED` and the connection survives, the same call the transport already makes at `closeOnOversizedFrame: !isClient`. 200 metadata frames of ~8 KiB against a 256 KiB bound: `none → RESOURCE_EXHAUSTED`, transport still open both ways, ablation `Expected: non-empty Actual: []`. **The GUARD that matters is the third**: a consumer that KEEPS UP takes all 200 frames, without which this is a throughput cap wearing a buffer's clothes. **P-31 cannot see this fix and that is recorded, not re-scoped** — it measures the SENDER and its consumer is paused, so it reports `4000 sends, parked false` before and after; the new test resumes before asserting. Keeper: **a lead that reads as a behaviour trade may already have been answered by the protocol** — 68 rounds of waiting for a decision that the spec had made
- **[349](349-the-refusal-that-could-not-fit.md)** FIXED, rpc_dart_http2 — **the curate produced this round**: it aged RPC-02 back in on a `git log` classification, and the lens then found a defect in the very change that aged it. Round 340 made the responder validate OUTBOUND metadata against the policy — and the refusal it sends when it rejects a request IS outbound metadata. `_answerRejectedStream` already knew half of this (*"a rejection long enough to explain itself could fail the same check that produced it"*) and trims `grpc-message` to `maxHeaderValueBytes`; nothing bounds the COUNT, and a refusal needs two headers to say both what and why. Measured at the peer: `maxHeaders 32 → ok(x)`, `maxHeaders 4 → status 3 with the message`, **`maxHeaders 1 → status 14 "Response ended without a gRPC status"`** — UNAVAILABLE, which reads as retryable, for a deterministic policy rejection the peer must never retry. **Round 340 introduced it**; before that the responder did not validate outbound at all. Fixed by falling back to a status-only trailer on `ArgumentError` — the status survives and the text gives way, the same trade the trimming already makes. `status 14 → status 3`, ablation `Expected: <3> Actual: <14>`, both GUARDs green (a refusal that FITS must keep its text). Keeper: **a round that fixes a rule can leave the rule's own answer outside it** — RPC-02 exists for exactly that question and had been swept clean three rounds before 340 shipped
- **[348](348-the-last-lens-that-had-never-been-applied.md)** CLEAN, rpc_dart_wasm — **RPC-06's first application**, `applied: never` since round 180 and the only lens left with an empty list. Its Ask is *"was this code RUN, or only read and compiled?"*, and both its gates are on the config's "targets nobody runs" list. `analyze:native` PASSes both halves — worth nothing until shown it can fail, so each language was ablated **separately**, because two scripts in two languages prove nothing about each other: `val probe: Int = evalResult` → `FAIL kotlin / PASS swift`; `let probe: Int = body` → `FAIL swift / PASS kotlin`. Then the level the lens cares about: a simulator was booted, so `test:wasm:device` ran for real — **`+17 All tests passed!`** across plugin and RPC suites, against a dart2wasm guest BUILT in the same run (boot path, guest timer, error in a guest timer, unhandled rejection after boot, glue that throws, all four call shapes, cancellation inside the guest, closing mid-stream). No defect. The detector's first shape is handled, and the two platforms handle it DIFFERENTLY rather than one having missed a port — Swift needs `finishBoot` because WKWebView boot is callback-driven with a timeout, Kotlin's is a suspend call in `withTimeoutOrNull` that returns or throws. **Android was NOT run** (no emulator; it compiles and the compile is sensitive). Keeper: **a lens with an empty `applied:` list is not necessarily one nobody needed** — this one costs a booted simulator and a minute, and its emptiness was a statement about habit
- **[347](347-the-line-above-the-one-i-blamed.md)** DEFERRED, rpc_dart_http2 — 346's named next step, and it worked: forcing `_gracefulCloseTimeout` to 1 ms then zero still did not reach the branch, because `close()` RSTs every stream before calling `finish()` so there is nothing to drain — design, stated at `:187`, read past twice. So the question was asked directly, and four connection states gave `finish() then terminate() → StateError: Cannot add event after closing` while three neighbours were silent. **The fix was written, witnessed, and the witness went RED**: `.catchError` on terminate does not see it, so it is not a rejection of that future. Narrowing once more — **`finish()` and NOTHING after it also throws** — identifies the source: `finish()` throws AFTER its own future completes, so no call-site handler can see it and `terminate()` was never involved. The transport's own comment says this two lines above the one I blamed (*"finish() on one throws from package:http2 into the root zone"*), quoted in 346 as evidence for suspecting the line BELOW it. **Reverted** — shipping the `.catchError` would have been a fix measured not to work. Filed **B-35** with both failed call-site attempts named so they are not retried, plus a kept characterisation test that closes the lead if the dependency changes, and a GUARD that the transport's own close stays silent (it does — measured). Bench **P-42**, whose reachability column is the control: three arms of "zero unhandled" while the line never ran. Keeper: **a comment that names the mechanism is not the same as having read it**
- **[346](346-three-benches-that-never-reached-the-branch.md)** INCONCLUSIVE, rpc_dart_http2 — re-reading round 342's `unawaited(close())` led to the last line of `close()`, which is RPC-13's shape exactly: `try { unawaited(_connection.terminate()); } catch (e2) {...}` — `unawaited` detaches the future, so the catch sees only a SYNCHRONOUS throw and a rejection reaches the zone unhandled. The line runs only when the graceful `finish()` has already failed, which is the case it exists for, and the file names the failure class two comments up (*"finish() on one throws from package:http2 into the root zone"*). **Three benches, none reached the line**: destroying the peer makes `finish()` complete with an error the `try` takes, so the 2 s timeout never fires; pausing the pipes to fake a half-open path did not fire it either and this round did not establish why. **Attempt 1's zero was unreadable and attempt 2 is what showed it** — a reachability read off the transport's own `'Graceful HTTP/2 shutdown did not complete'` warning turned two meaningless zeros into two honest "not reached". At the 3-rebuild budget, and the rule is that a bench which could not see the defect makes the verdict INCONCLUSIVE, not CLEAN. Next approach named and it is not a fourth peer variation: `_gracefulCloseTimeout` is a `static const` at `:191` — set it to a millisecond and the branch is taken on every close, narrowing the question to whether `terminate()` rejects. Keeper: **an arm that reports zero has to prove it could have reported one**
- **[345](345-the-only-gate-that-sees-it.md)** FIXED, rpc_dart_wasm — L-12 applied to 344, which fixed ONE tear-off without counting the class. First the decisive measurement: with the tear-off restored, **`dart analyze --fatal-infos --fatal-warnings` says "No issues found!" while `dart compile js` refuses to build** — the interop rules live in the CFE's JS-target checks, not the analyzer's rule set, so analysing harder cannot help and round 326's `unnecessary_lambdas` makes it worse by asking for the broken form. Coverage is therefore exactly the set of files some gate COMPILES for a JS target. Sweep: **two** library files import `dart:js_interop` (the websocket match was the word "external" in a doc comment) — the isolate's, covered since 344, and `wasm/rpc_wasm.dart`, **compiled by nothing**: `test:wasm` is `flutter test` on the VM, `test:wasm:device` builds for iOS/Android, `test:web` excludes the package. The package whose entire purpose is a JS runtime had no JS-target compile anywhere. Fixed with a committed `tool/js_target_entry.dart` and a compile at the FRONT of `test:wasm`, where `flutter pub get` has just resolved it. Canary both ways: `Error: Tear-offs of external top-level member '_sendBytes' are disallowed` from the new line, `No issues found!` from the old one. A first canary attempt tore off `globalContext.delete` and **compiled fine** — a Dart-implemented extension, not an external member — which also widened what 344 knew: the rule covers external TOP-LEVEL members too. Keeper: **"is it checked?" is not "does some gate run over it?"** — `analyze` runs over that package at `--fatal-infos` and is structurally incapable of seeing its one failure mode
- **[344](344-the-gate-line-nobody-reached.md)** FIXED, rpc_dart_isolate — opened on RPC-15 pointed at my own process: `config.md`'s gate is FOUR lines and I had run three for seven rounds while reporting green each time, which is the exact failure `license:check`'s own note was written about (*"a gate that is a SUBSET of CI's will report success for work CI rejects"*). It came back clean, 1388/1388 — **the process gap was real and the outcome was not.** `test:web` is where the defect was, and the owner pasted it from CI mid-round: `Error: Tear-offs of external extension type interop member 'close' are disallowed`, on `onDispose: scope.close` — with the compiling form already written three lines below in the same file. Two mechanisms, and the second is why it reached CI: **`unnecessary_lambdas`, which round 326 put in the lint floor, DEMANDS the broken form** (*"Closure should be a tearoff"*), so `analyze` and `compile js` contradict each other on that line and exactly one can be satisfied — the analyzer being the one whose advice does not compile. And the compile ran LAST in `test:web`, welded to the chrome run behind twelve `set -e` suites, so an unrelated ENETDOWN burst at load 8.55 aborted the script before it was ever reached. Fixed with the closure plus a local `// ignore:`, and the compile **hoisted to the front**: ablated, it now fails in 2 seconds with the byte-identical CI message instead of never running. Gate run in full for the first time this session, plus `test:web` 12 suites green. Flagged for the owner: `unnecessary_lambdas` is now known to demand uncompilable code on JS interop members and is on for every package — the second time round 326's floor has been wrong about this one rule. Keeper: **a step that runs last is only as reliable as everything before it** — order a gate by determinism, not by narrative
- **[343](343-the-ablation-that-was-masked.md)** CLEAN, http2 + http — 342's keeper applied: `RpcChannelTransport` bounds `_finishedStreams` and GATES `_statusSeen` on a controller existing, both because a stream id can be the PEER's choice, and neither is named in `RpcSecurityPolicy`. After three rounds each finding a missing port, the hypothesis was a fourth. **Wrong, and the reading says why**: the http2 caller and the http responder mint their own ids so the peer cannot name a key, and where the id IS the peer's the bound is `maxActiveStreams` or http2's own `SETTINGS_MAX_CONCURRENT_STREAMS`. All 13 per-stream collections prune; P-41 over 2000 completed calls on ONE connection reads **0 / 0 / 0** from the transports' own `health()`. **The control is the round**: deleting ONE of `_streamParsers`' two prune sites changed nothing — `onDone` and `releaseStreamId` both remove it and either suffices — so the single-site ablation produced output identical to a bench with no sensitivity at all, and stopping there would have recorded CLEAN on exactly what Q2 exists to prevent. Both removed: `1 / 2001`. Filed **C-37**. Keeper: **a three-for-three streak is what made the wrong hypothesis comfortable** — it is not evidence about the next case
- **[342](342-the-second-mechanism-that-was-not-a-field.md)** FIXED, rpc_dart_http2 — the one cell 341's enumeration left open, and the answer was not the one expected. `_validateInbound` does TWO things and only one is a field: `if (_policy.closeOnProtocolError || ++_policyViolations > _maxPolicyViolations)`. The second is a **256-violation backstop** whose own comment prices it — *"200k violating frames cost 100 MiB of RSS with the connection still open to repeat it"* — and `grep` finds no violation counter anywhere outside `RpcChannelTransport`. So the http2 responder ported the half that had a NAME and the caller neither. P-40 at the **default** policy: **2000 violating header blocks all accepted, connection STILL OPEN, RSS up 27 MiB**, against a shared-layer control that closed after 256 — unauthenticated, on the transport the responder's own comment calls "the transport a gRPC deployment actually exposes". Fixed with the same constant in both halves, **deliberately different**: the responder gets `closeOnProtocolError || backstop`, the caller the backstop ONLY, because the library's position for a client is that killing the connection over one peer fault is wrong when the other in-flight calls die with it (`closeOnOversizedFrame: !isClient`) — but 256 times is no longer one bad frame. `opened=2000 STILL OPEN` → `opened=601 closed`, RSS +27 → −11 MiB, `+210 → +213`, one canary per half. NOT swept into `rpc_dart_http`: HTTP/1.1 is request-scoped, so there is no connection to grind. Keeper: **a sweep terminates over the thing it enumerates and is blind to everything else** — 341's field pass was genuinely exhaustive and genuinely missed this, because a `static const int` plus an `int` has no field to enumerate. After the names, read the bodies for what has none
- **[341](341-the-knob-that-bought-a-belief.md)** FIXED, rpc_dart_http + rpc_dart — RPC-10 again, but asked as an ENUMERATION instead of a hunt: which fields of `RpcSecurityPolicy` does each transport actually honour? `maxMetadataBytes` is consumed by the frame channel (websocket, wasm) and http2's header-block guard, and by **nothing in `rpc_dart_http`** — whose responder validates `maxHeaders` and the per-header caps, which **do not imply a total**: the defaults allow 128 x 8 KiB = 1 MiB against a 64 KiB bound. P-39, raw HTTP/1.1 POSTs: **960 000 bytes of headers answered 200 OK, 14.6x the bound**, every header individually legal, pre-auth, and dart:io imposes no limit of its own. The gap read as intentional because the field's doc scoped it to "transports that serialize metadata" — but http2 does not serialize metadata either and applies the same number to its HPACK block, so two native-header transports made opposite choices and the prose described neither. Enforced in the transport rather than in `validateMetadata`, because each knows its own byte count and a string-length approximation in the shared method would give three transports a second, differently-accounted check. `+123 → +126`; ablation `Actual: 'HTTP/1.1 200 OK'`, with two GUARDs green — an ordinary block still passes, and a small-but-illegal block is still refused for its OWN reason, since the aggregate check runs first and could have shadowed it. Second candidate found and NOT taken: `closeOnProtocolError` is honoured by the channel transports and the http2 responder but not its caller. Keeper: **an unenforced limit is worse than a missing one, and the file says so three lines above the field it happened to** — that comment was written after three such fields were deleted, and `maxMetadataBytes` survived the cull because it IS enforced, on four transports out of five
- **[340](340-the-policy-that-only-faced-inward.md)** FIXED, rpc_dart_http2 — B-34, and the **first ever application of RPC-10** (`applied: never` since round 150). The lens asks *which packages actually go through the class that changed?* `RpcChannelTransport.sendMetadata` validates outbound metadata against the policy; websocket/wasm/isolate inherit it and `rpc_dart_http` ported it to both halves, but **http2's only `validateMetadata` was inbound** (`:1241`, `:466`). Not unchecked — `_headerValue` rejects non-printable-ASCII, and both transports refuse a Cyrillic value — but **a hardcoded check is not a policy**: under `maxHeaders: 32, maxHeaderValueBytes: 64`, **64 headers, a 200-char value and a header name with a space were all ACCEPTED** where the shared layer threw. Blast radius is ONE stream (clean calls before and after return `ok(x)`), which is what kept it a lead. Fixed with the two lines `rpc_dart_http` already has; all three cases now match the shared arm character for character. `+206 → +210`, two canaries (one per half), and the responder's ablation confirms validation precedes the stream lookup — without it a `StateError` from `_requireIncomingStream` arrives first. Named rather than hidden: outbound validation refuses against the SENDER's policy where the PEER's used to decide, which differs for an asymmetric deployment. Bench **P-38**, whose control arm carries the trap that cost a measurement — **a limit chosen to be violated must still admit the control** (`maxHeaders: 4` is below what an ordinary request carries, so the peer refused every call and it read as connection poisoning). Keeper: two comments in http2 already name `RpcChannelTransport` behaviours re-implemented by hand; the lens is the generalisation of those comments
- **[339](339-the-margin-nobody-measured.md)** FIXED, rpc_dart_http2 — a CI failure the owner pasted mid-round: `GUARD: a later reconnect still opens a new connection`, `Expected: <0> Actual: <1>`, from a gauge that polled `opened - closed` for 8 s. **Five local runs green** (file, file serial, package suite, two full `test:unit`), so the number is all there is — and it has three causes needing different fixes: a connection nothing can close, a close that has not landed, a leaky harness. Instrumenting the poll gave the one measurable fact: teardown settles in **1733/1735/1739 ms** for 2/2/3 connections — **flat**, so one fixed wait — against an **8000 ms budget, a margin of 4.6x** on an idle machine, with CI running 14 packages at concurrency 4. Replaced with an event-driven wait that returns when the last close lands (wall clock unchanged at 9 s) and reports `opened=N closed=M, still live after Xms` so the next failure names its own cause. Ablating the single-flight marker: `Actual: <2>`, `opened=4 closed=2, still live after 30007ms` — the full budget against 1735 ms healthy, which settles it as an orphan rather than slowness; the sequential GUARD stayed green, the right specificity. **Does NOT claim to have fixed CI** — what it fixes is that the next occurrence will say which cause. One hypothesis measured and DISPROVEN: the harness proxy does not orphan a socket when the client dies inside the stall, because `sub.pause()` defers `onDone` past the point `upstream` is assigned. B-34 parked. Keeper: **a budget with no measurement beside it is a guess that looks like a decision**
- **[338](338-the-guard-that-asked-a-different-question.md)** FIXED, rpc_dart — RPC-15 on a record one round old: 337 put `_logger.isInternal` in front of 158 new sites, taking the guard count to **222**, on the assumption that the guard predicts what `_log` does. It does not. `isInternal` calls `accepts(level, name)` and `_log` calls `accepts(level, name, tag)` — an **optional positional parameter that changes the answer**, because `_resolveLevel` consults the tag override FIRST, ahead of the scope overrides and ahead of `minLevel`. P-37 asks the controller what arrived rather than asking the guard: of four configurations **two disagreed, and one is a MUTE** — `setTagLevel('verbose', internal)` on a tagged scope delivers the record, the guard says false, so with the guard in place it is never offered. The call still succeeds; nothing reports anything. Reachable through ordinary use: the endpoints build untagged scopes, but **every transport takes a `LogScope?` directly** and turning one tag up is how you get verbose output from one subsystem — that configuration silenced all 41 guarded lines in http2. `child()` carries the tag, so it holds at any depth. Fixed by passing `tag` in the three guards: **4 of 4 agree**, 222 sites at once, `+1445 → +1449` and `+205 → +206`. Ablation 3-of-4 red in core and red in http2 while the untagged cases stayed green. Keeper: **a guard is a PREDICTION about other code, only as good as the arguments it makes the prediction with** — and 337's witness could not catch this because it configured the logger the way the guard happened to assume
- **[337](337-the-class-not-the-sample.md)** FIXED, rpc_dart + framework + http + http2 — the owner's correction to rounds 333/334: the question was about **every** logger call, not the unary ones. First application of L-12, so the count came before the fix: **158 interpolating unguarded `internal`/`trace`/`debug` sites in 21 files across 4 packages**, plus 26 constant-message ones that are NOT in the class (a const string costs nothing to build) and three transports with no such call at all. The count is what taught the round two things a sample could not. **Unary is the CHEAPEST of the four shapes by a factor of seven** — 333/334 drove it to 6 discarded messages per round trip and read the rest as a tail, while serverStream sat at **42.0 calls / 2297 characters**. And the two logger configurations are IDENTICAL: **attaching a real `LogController` at `error` level does not stop one character being built**, so this was never about running without a logger. All 158 guarded → **6→1, 42→5, 36→4, 28→1 calls; 94% of the discarded characters gone on the streaming shapes**, ~2.7 us per serverStream round trip, and the residual is provably free because it is exactly the constants. Two idioms, because nullability differs: core `if (_logger.isInternal)`, transports `if (_logger?.isInternal ?? false)` — two canaries, and the one that matters is the ablation showing `?? true` (the natural way to get the nullable form backwards) **leaves every cell of the core bench unchanged**, which is why http2 got its own witness. Bench **P-36**. Keeper: **the smallest shape was measured first and read as the whole** — unary is the only shape with no per-message loop; when a defect is per-call, measure the most expensive call before deciding what is left
- **[336](336-the-unary-call-that-was-never-answered.md)** FIXED, rpc_dart — 335 checked one property of the pipeline's zero-copy/serialized branch pairs; this read the rest. The three streaming pairs are symmetric. The UNARY pair is not a pair, and the zero-copy branch carries a comment from an earlier fix: *"the three streaming shapes already route their request-stream errors, and this one did not"* — true, and it **excluded the fourth shape from its own count**. `UnaryResponder`'s `onError` only logged, and it is the default for every codec-based unary method. Measured on a request stream that errors before a request arrives: **`ServerStreamResponder` → status 13, `UnaryResponder` → NONE** — the caller waits for a response that never comes and eventually reports UNAVAILABLE, which says nothing about the cause. Its own `onDone` eight lines above already answers with `invalidArgument`, so the method both knew how and failed to. Fixed to answer with `wireStatusFor(error)`; **`+1440` → `+1443`**, ablation red with both guards green. Keeper: **a comment saying "the others already do this" is a claim about the others, and it names them — re-read it as a CHECKLIST, because every shape it does not name is one nobody checked**
- **[335](335-one-in-twenty-seven.md)** CLEAN, rpc_dart + isolate + http2 — the detector round 334 produced, run over the corpus it came from, because a criterion invented on one instance is a guess about a class. Every class in core and transport constructed in more than one place: **27 sites, 1 defect, and it was 334's.** `CallProcessor` (4), the seven stream responders (8), the stream callers (4), `RpcMessageParser` (3), `RpcChannelTransport` (8) all agree. The keeper is why the **two textual differences are not defects**: core's `policy.effectiveMaxBufferedBytes` versus http2's raw nullable `_policy.maxBufferedBytes` resolve identically through the parser's own `?? (maxMessageLength + prefix)` fallback, and http2's missing `decompressor` is deliberate layering — with none, the parser re-encodes the compressed frame so the ENDPOINT parser decompresses, once. **Round 334 is the control** for the detector's sensitivity; a 1-in-27 sweep with that provenance is a measurement, without it hope. Does not generalise to a regex: 2 of its 3 textual findings would be false positives → **C-36**
- **[334](334-the-caller-that-logged-to-nobody.md)** FIXED, rpc_dart — finishing 333's "Not fixed": guard the remaining 25 discarded log messages per round trip. Mechanical, until **extending the witness to the newly-guarded files went red**: `Expected: true / Actual: <false> — the unary caller was muted`. Not muted by a guard — **`UnaryCaller` was constructed with no `logger:` at all**, so it fell back to `LogScope.noop` and its twenty-odd `internal(...)` calls had been dead for every user since the class was written; attaching a `LogController` did not make them appear. The two branches of one `if`, eight lines apart: the zero-copy branch passes `logger: _log`, the serialized branch — **the default for every codec-based unary call** — passes nothing. Every other site in both pipelines (three stream callers, seven responders) passes its logger. **35.0 → 6.0 discarded calls and 1566 → 263 characters** across 333+334, 83% of the volume, ~1.7 us of CPU per round trip, and the unary caller's diagnostics exist for the first time. Keeper for RPC-04: **two branches of one `if` are two call sites, and the shorter one is the default** — no type differs, no analyzer rule fires, both compile
- **[333](333-strings-built-for-a-logger-that-discards-them.md)** FIXED, rpc_dart — the owner's target: log strings in core are built even when the level would discard them. True and now priced. `internal(String)` takes a STRING, so the interpolation runs at the call site before the empty `LogScope.noop` body: **35 discarded messages and 1566 characters per unary round trip** (70000 calls / 3.13 MB over 2000), costing **~2.0 us of CPU on the VM, ~3.5 us on dart2js**. The wall clock CANNOT see it — two runs of identical code gave 174.4 and 184.9 us, a 6% spread against a 1.2% effect — and a histogram showed the cost is flat across ~30 sites, so there is no cheap hot spot. The enabling defect was prose: `LogScope.noop` said **"All methods are empty, zero cost"**, which is exactly what tells an author not to guard — 25 of 58 interpolating sites used the existing `isInternal` guards, and the per-call unary responder had 38 calls and **zero**. Sixteen guarded (**35 → 25** messages, **1566 → 1187** chars), doc corrected with the measurement, witness added that a muted guard goes red while the call still succeeds. **25 per round trip remain**, ~1.4 us, named. Methodological keeper: **the instrument changed between before and after** — a probe histogram's `Map` insert cost 18 us per round trip, nine times the effect, and it took a *contradictory* number to notice
- **[332](332-the-shared-class-nobody-tested.md)** FIXED, rpc_dart — round 331 invented a criterion on one file, so this pointed it at the lens's own output. **`RpcStreamRouter` — the class round 308 extracted from four hand-rolled copies because one had DRIFTED — had no test file at all, 24 rounds later.** Ablating the rule it exists to enforce (`operator []` returning the SAME stream on a repeated lookup, exactly what http2's caller had got wrong): `rpc_dart_http` **+123 all passed**, `rpc_dart_http2` **+204 all passed** — 327 tests, neither notices. Then round 330's lesson applied rather than re-learned: instrument instead of assume, and the reuse branch is reached **0 times in http, 1 time in http2** — reachable and watched by nothing, L-04 case 1, settled with one `print`. Four tests written against the class (`+1435` → **`+1439`**), and the first version of the witness was itself wrong — it tried to `listen` twice and got `Bad state: Stream has already been listened to`, because the controllers are single-subscription and "both lookups can listen" was never the rule. Keeper: **extracting shared code moves the code but not the tests — after an extraction, ask what test exercises the NEW unit**
- **[331](331-deduplication-that-extends-coverage.md)** FIXED, rpc_dart — round 330 noticed three bridges in `caller_pipeline.dart` and compared exactly one property; RPC-25's bar is the drift, so the comparison was owed. **No drift at all**: the two outer bridges are identical modulo a type parameter and a message string, 37 non-comment lines twice — which by the lens's own rule (316, 317, 318 all declined on it) means decline. **The reason not to is coverage, and it is measurable.** The one test pinning the `if (!finished)` rule — the one that stops a normal completion poisoning a REUSED context's token — has no bidi arm, so on the pre-extraction tree: ablating serverStream's copy gives **`+1434 ~1 -1`, caught**; ablating bidirectionalStream's **identical** copy gives **`+1435 ~1`, nothing caught it.** Extracted to `_bridgeCallerResponses<T>` so the existing test now covers both shapes; 856 → 838 lines, suite unchanged, and the extracted guard re-ablates red. Genuine extension to RPC-25: **duplication with NO divergence is still worth removing when the copies are unequally covered — ask which copy the tests reach, not only whether the copies agree**
- **[330](330-the-class-the-floor-leaves-to-the-loop.md)** CLEAN, rpc_dart — round 329 showed the lint covers only the async half of RPC-13, so this swept the half left to the loop: **13 `.then(` sites, 6 handled, 7 not.** Five resolve on reading; the seventh, `caller_pipeline.dart:644`, has every ingredient the lens asks for — `enqueue` is a **void** function (lint blind), its `sendSeq` is **declared, assigned and never read**, and the catch blocks call `controller.addError` unguarded while `cleanup()` closes that controller, where **the two server-stream bridges in the same file guard every controller op**. It still cannot fire: `caller.send()` never throws, because `CallProcessor.send` queues through `_transmitRequest`, whose callback ends in a bare catch. **Two probe rebuilds chased scenarios before the cheaper move — instrument the guard, which is L-04's own amendment, written down two directories away.** A `print` in the catch answered in one run: three arms (drain, cancel-mid-parked-send, oversize frame), **not reached once** → **C-35**. The guard stays unfixed on L-04 case 2, and what is uncomfortable is recorded: the safety belongs to a different file, one narrowed `catch` away
- **[329](329-what-the-floor-does-not-catch.md)** CLEAN, rpc_dart — RPC-26 was four rounds old, three-for-three, and carrying a `breaks:` line nobody had tested; RPC-15 pointed at a lens created inside the same ten rounds rather than at a stale one. **Both halves were overclaims.** No live defect among the **534 issues** fixed in 325/326/328 — every test count in the workspace unchanged at each (`+1435 ~1`, `+204`, `+137`, `+74`), so the surface was latent throughout. And the floor does **not** catch the classes the loop hunts: round 242's defect — an unguarded `.then()` running user code, which ends the isolate — is still at `client_connection.dart:432` in RPC-13's shape and analyses **green**. A three-case probe says why: `unawaited_futures` fires only inside an `async` body, not in a `void` method, which is where a fire-and-forget future is most likely to be written. Nothing in the floor touches RPC-01, RPC-05, RPC-14, RPC-16 or RPC-17 either. `breaks:` corrected to "latent surface, not a live defect", and the keeper for RPC-15: **the record most worth re-measuring is not always the oldest — ask which was most EXPENSIVE and whether anyone tested it**
- **[328](328-the-two-packages-326-excluded.md)** FIXED, rpc_dart_generator + rpc_dart_wasm — the two packages round 326 named and left off the floor, both in scope. Two hypotheses, **both wrong**: they hold **3 issues between them** (against 211 for the other nine), so the packages nobody had raised were the *cleanest* in the repo; and the generator's `package:lints` resolution failure is **not** the gate hole it looks like. Measured by setting `page_width: 100` in the base — `rpc_dart_log` (resolves) reformatted 9 of 11 files, `rpc_dart_generator` (does not) reformatted 1 of 3, so the `formatter:` section arrives regardless. The cause is that the generator has no per-package `.dart_tool/package_config.json` (the workspace centralises it) and `dart format` does not walk up; only the half the ANALYSER needs is unresolvable, and `dart analyze` resolves it. **Round 326's own comment in the base file was wrong and is corrected here** — "a relative include always resolves" is false, since it chains to a `package:` URI one level down. Canary: `Future<void> close()` narrowed to `Future close()` in wasm — `No issues found!` under the old floor, `strict_raw_type` under the new. **All 22 packages now on one floor, nothing excluded**
- **[327](327-the-ablation-rpc-02-was-owed.md)** CLEAN, rpc_dart_http2 + rpc_dart — the debt round 320 wrote into RPC-02 and rounds 322, 323 and 324 each restated without paying: three consecutive READINGS (216 measured, 243 read, 320 read). `P-08` re-run whole and **all four rows are identical to round 216's**, including the ablated one — `maxMessageLength` removed from `_fcRefuseOverrun`'s trailer turns `status 8` into a raw `ArgumentError` at a cap of 64, while the other rows do not move, so the bench still isolates the trailer from the request headers **111 rounds and five doc-and-lint sweeps later**. The sweep count holds at **16/12/4**, but only with care: 4 of the 20 grep hits are the declaration and three COMMENTS, and an 8-line window reports **11 of 12** because `frame_multiplexed_channel.dart:261` keeps its cap eleven lines down behind the comment explaining why an INBOUND trailer needs one — the best-documented site is the one a naive detector calls broken. What kept the debt open for seven rounds was not cost (one probe run, one edit, one revert) but that each round preferred a fresh target to an owed one
- **[326](326-three-packages-had-no-lints-at-all.md)** FIXED, nine packages across core and transport — RPC-26's own closing line: 325 raised the floor on `rpc_dart` and left 21 packages uncounted. The hypothesis (they all sit on `lints/recommended` like core did) was **partly wrong, and the wrong part is the finding**: there were THREE floors and nobody chose any. **`rpc_dart_framework`, `rpc_dart_grpc_reflection` and `rpc_dart_websocket` had NO `analysis_options.yaml` at all** — they fell through to a root file configuring only the formatter, so `analyze --fatal-infos` printed `No issues found!` for a priority transport with 137 tests because nothing was switched on. The floor found **211 issues**, and **ten violate `lints/recommended` ITSELF** — the baseline, not the strict extras — all ten in two of those three, including two `curly_braces_in_flow_control_structures`, the lint CLAUDE.md records as having once shipped into a published release. Fixed as ONE floor (`analysis_options_base.yaml` + `analysis_options_test.yaml`, included by relative path since `package:lints/...` does not resolve from every context). Canary: the same file, one `while` body unwrapped — `No issues found!` under the old config, 1 issue under the new. Gate green, every test count unchanged. `rpc_dart_wasm` and `rpc_dart_generator` deliberately left off, with reasons
- **[325](325-the-gate-was-green-because-it-was-not-looking.md)** FIXED, rpc_dart — the owner asked for cleaner core, then for the strictest linter. Core's doc ratio was already **17.2%** (down from RPC-23's 18.4%) and its abstractions swept at 308-318, so cleanliness had to come from the INSTRUMENT. `melos run analyze` runs `--fatal-infos --fatal-warnings` and was green; core inherits `lints/recommended` and nobody had asked what the preset omits. Raising both dials — `analyzer: language:` (strict-casts/inference/raw-types, which **no preset enables**) and 14 rules over the preset — reported **320 issues on that green tree**: 112 in `lib/`, 208 in `test/` + `example/`. **69 of lib's 112 collapsed into ELEVEN edits**, every one an untyped `onError: (error, stackTrace)` making both parameters `dynamic` — RPC-13's own subject, unchecked in 16 handlers. **123 of the test tail was one pattern**, `Future.delayed` untyped where `lib/` already writes `Future<void>.delayed`. Two rules measured and left OFF (`close_sinks` 13, `cancel_subscriptions` 6 — all field-held false positives), and `unnecessary_lambdas` scoped out of `test/` after an automated pass **deleted a measured deadlock record** (`CANCEL DEADLOCKED (received=0)`) to save one closure; reverted. All 320 fixed, gate green, `+1435 ~1` unchanged. New lens **RPC-26**; only 1 of 22 packages raised
- **[324](324-the-flag-was-never-the-risk.md)** FIXED, rpc_dart — RPC-19 re-swept over **21 moved files**, taken as the lens's own Ask (drive the recovery API twice) rather than a re-grep. Counts hold: `_disconnected` is **14 code lines** (a `grep -c` says 15 — one is a comment), and of **11 lifecycle flags 10 have exactly one writer** and are retired by the lens's own rule. The eleventh, `RpcClientConnection._isStopped`, has four writers and carries one meaning, with `_disposed` carrying the terminal one. Then the ablation that matters: **writing `_isStopped = true` on the give-up path — this lens's literal defect, the one http2 had — changes NOTHING. `+120`, all green.** `connect()` clears the flag unconditionally, which is the `_isClosed = false` that `48847ffc` had deleted from http2; keeping it makes extra writers harmless. **The risk is the COUNTER, which a flag sweep never looks at**: remove `_reconnectAttempts = 0` from `connect()` and every later call gives up at once — the give-away shape — while all 119 other resilience tests stay green. Four ablations, only the fourth discriminating, and the budget overrun recorded rather than hidden. **`+1433` → `+1435`**. 319's queue is now empty
- **[323](323-the-guard-that-nothing-watched.md)** FIXED, rpc_dart_isolate — RPC-14 re-swept over **33 moved files**, and the round spent on the gap the lens names in its own *What the sweep does NOT establish*: every site guarded, and **`+73` still green with `teardownStartup()` deleted**. Detector unchanged — 4 isolate sites, plus core's **7 listed for the first time** (2 hold something and handle it deliberately, 5 wait on data; `base_endpoint.dart:203` is the near-miss, a timeout around `close()`, which produces no handle). The witness is now built: a synchronously blocking worker with a 500 ms `startupTimeout`, observed by whether the **child process exits**. Which deadline is reachable turns out to be decided by the bootstrap's ORDER — the handshake SendPort goes out at line 257, before the entrypoint at 285, so no user code can time out the FIRST handshake and a test aimed at 417 lands on 545. Both halves of that path canaried separately (`teardownStartup()` for the isolate and the error/exit ports, `hostTransport.close()` for `hostReceivePort`): either one removed leaves the child **still running**, and the suite reads **`+73 -1`** — the only red is the new test. **`+73` → `+74`**. First retirement of an L-04 instance
- **[322](322-the-ablation-three-rounds-owed.md)** CLEAN, rpc_dart — RPC-09 re-swept over **34 moved files**, and taken as an **ablation** because round 320 had just concluded that three consecutive readings are worth less than one re-measurement. RPC-09 is in the same position (210 and 221 measured, 244 re-read) and unlike RPC-02 it has a registered bench with a validated control sitting unused. P-10 re-run with `_fcOnGrant` refusing every grant: **all eight cells identical to round 221** — the draining call HUNG at 20 s / 16 pulled, the answered one back in 0.4 s. The independence claim holds, measured. The bounded inbound queue added since (`BufferedBroadcastController`, rounds `7cba007b`/`cd6ee68e`) does NOT extend the surface: it enqueues only while no listener is attached and its overflow is fatal-with-error, so it cannot produce a hang. **The finding is in the record, not the code** — 244 counted three wake paths and there are four; the missing one is `_fcForget`, the wake 206 added, 211 measured at 30 stranded senders against 0, and 210 ablated. A re-read would have copied the three forward again
- **[321](321-a-number-that-could-not-name-its-own-cause.md)** FIXED, rpc_dart_http — the owner pasted a CI failure and it took the round: the loop's own `aborted_body_frees_the_pipeline_stream_test` red again on `Actual: <0>`, the **second** CI failure with that exact message. Round 314's recorded cause — an aborted socket answered by a prompt read error rather than by `bodyReadTimeout` — measured **false**: both shapes cost the server the same **2009 ms**, and a 5 ms sampler hits **401 times of 1191**, so no version was ever losing a race. The real defect is the OBSERVABLE: a gauge that rises and falls can only be sampled, and `0` reads the same whether nothing arrived or the sampler never looked, which is why three shapes of the question all failed to end it. Replaced with an **edge-triggered read at the peer** — hold the socket, require `408`, which this transport emits at exactly one place after registration while every pre-registration refusal answers its own code. Three named outcomes where there was one number, both canaried (`closed`, `STILL DRAINING`). **The CI failure itself was not reproduced locally and the round does not claim its cause** → **L-11**
- **[320](320-fifty-one-files-later-the-count-is-the-same.md)** CLEAN, rpc_dart + rpc_dart_http2 — first item off 319's queue: RPC-02 re-swept over **51 moved files, 19 of them behavioural**. Count unchanged from round 243 — **16 sites, 12 with a message and all 12 capped, 4 with none**. The keeper is not "still clean" but that **the surface did not grow**: across rounds 244-319 not one new trailer site was written, which moves the lens's risk from existing sites decaying to a NEW refusal path arriving uncapped. Says plainly that this was the **third consecutive reading rather than an ablation** — round 216's cap-to-64 is what gives the claim meaning, and the lens now names re-ablation as its next action
- **[319](319-the-staleness-is-real.md)** CLEAN, rpc_dart — I had just declared core and transport converged after three quiet rounds. `loop.py stale`, which I had not run, says **71 of 88 records have aged**, including all four swept lenses. Hypothesis: noise from my own doc sweeps, since a record ages on any path change. **Wrong** — classifying RPC-02's 31 commits gives **19 behavioural against 12 docs**, and several land on its own subject (`bound the drain on a refused upgrade`, `charge the pre-method budget for metadata too`). The queue is four re-sweeps, ordered by churn: RPC-02 (51 files), RPC-09 (34), RPC-14 (33), RPC-19 (21). `curate` is overdue since ~234. Fifth instance in twelve rounds of concluding without the command — this time from the *impression* of three quiet rounds
- **[318](318-a-lens-derivation-stopped-by-scope.md)** DEFERRED, rpc_blob — a `lenses` derivation over B-10's three layers, opened on "you can invent new lenses" and **stopped mid-way by the owner narrowing scope back to core and transport**. The enumeration completed and is kept: **ten independent implementations of three interfaces across ten packages**, with a third of them (`*_postgres`, `*_minio`) outside `melos run test:unit`. The first two adapters compared already disagreed — `IBlobRepository.deleteBlob` promises only "returns `true` when something was removed", and on a missing blob with `expectedVersion` set `in_memory` **throws** while `webdav` **returns false**, though they are interchangeable by design → **B-33**. No lens filed: a lens needs a detector proven on the corpus it covers, and the corpus is out of scope
- **[317](317-two-preambles-one-lifetime-apart.md)** CLEAN, rpc_dart — closes the last thread from the core sweep: `RpcCallerContract`'s preamble looks identical to the responder's, minus the duplicate check, and 315 left "should it refuse duplicates too?" open. **It should not** — the caller holds NO registration map at all; its four `call*` methods dispatch through the endpoint on the spot, so a repeated name is two calls. Same computation, different LIFETIME: once per registration there, once per call here. Merging would carry a registration rule onto a call path — dead at best, refusing a second call at worst → **C-34**. Second CLEAN in a row, and the pair shows the lens DECLINING, which is what its "the finding is the drift" bar protects
- **[316](316-a-convincing-argument-that-was-wrong.md)** CLEAN, rpc_dart — takes 315's open thread (the zero-copy generic asymmetry) and reads the dispatch. The reading produced a convincing defect: the registry's static type is `<Object, Object>`, Dart function parameters are contravariant, so the `as` in `callUnaryHandler` should throw and zero-copy unary should be broken outright. **It is wrong — Dart generics are REIFIED**, so the method runs with the instance's real type arguments and the cast is an identity. One grep found `unsendable_direct_object_test.dart:120` already exercising the exact shape predicted to fail, green. The asymmetry is cosmetic; no code changed → **B-32**. Fourth time in eight rounds that a conclusion was reached by reading where a grep was cheap
- **[315](315-eight-places-for-one-rule.md)** FIXED, rpc_dart — RPC-25's **first application to core**, on the owner's target: the responder contract's four `add*Method` registrations. `_rejectDuplicate` — "is this method name taken", a check that reads BOTH maps and so cannot depend on the branch — was called from **eight** places, once per branch per method, alongside four byte-identical preambles. **8 -> 1**, behaviour-preserving (validate still runs first, so a codec-invalid duplicate still reports the codec error). No drift found, and reported as such: this is the *rule* exception the lens now carries — eight copies are eight chances to answer it differently later, and enforcing it on three paths of four would compile silently. Left measured and untouched: the caller contract's identical preamble minus the duplicate check (a semantics question, not a tidy-up) and the asymmetric zero-copy generics
- **[314](314-the-window-that-opened-too-late.md)** FIXED, rpc_dart_http — a red gate the owner pasted, taken ahead of the backlog and checked FIRST against rounds 308-313, which had touched this exact transport. **Not a regression**: the failing assertion reads `_pending.length`, a field the 308 router swap never went near. The bench was wrong — `_abortMidBody` destroys its socket immediately, so a request can be answered by a read ERROR promptly instead of by `bodyReadTimeout` at 2 s, and the test opened its observation window AFTER the four aborts. Under load all four are answered before the first poll and the guard reads 0 on a server that did everything right. The window now starts before the aborts. **Canary reproduced the reported failure exactly** — same message, same `Actual: <0>`, same ~12 s shape. Round 271's earlier de-flake had fixed the QUESTION (peak, not simultaneous count) and left the WINDOW; both print identical text, which is why it looked done
- **[313](313-two-of-three-were-testable.md)** FIXED, rpc_dart + rpc_dart_http2 — takes round 312's own "Not fixed" list of three unwitnessed fixes and finds **two of the three claims were wrong**. 309's "nothing automated reads log levels" — `LogController` exposes a public record stream, so four tests now pin the unified drain logging at its source, where all three servers inherit them. 311's "dead code has no runtime witness" — true of the deleted clause, false of the FUNCTION's contract, so four more pin `unconsumedWindowFor`, including the surprising clause that `null` does not disable the bound. Only 310 survives, and for a reason that is not the platform: `_WebMultiplexedChannel` is library-private and Dart privacy is per-library, so `src/`-importing cannot reach it the way it did in 307 → **B-31**. core 1429 -> 1433, http2 200 -> 204
- **[312](312-the-witness-308-shipped-without.md)** FIXED, rpc_dart_http2 — writes the regression test round 308 admitted it shipped without ("the test suite is not the witness here"). Drives the caller transport directly, since the endpoint asks for a stream once and cannot reach the second-call path — which is why 199 tests passed over the defect. Canary with the pre-308 form restored: **RpcStatusException(8) "Response exceeds the un-consumed window (4298316 > 4194304)" after consuming 4102938 bytes the reader had actually read**. Two traps found in the writing: the first version yielded `'z' * 256KiB` and passed on broken code because a one-character run deflates ~900:1 and never reached the bound, and `grpc-status 0` with 6060 bytes is not a passing call. Suite 199 -> 200
- **[311](311-a-fallback-that-could-never-fire.md)** FIXED, rpc_dart_http2 — asked whether more refactoring remains; ran RPC-25's step 1 across the transports and reports **two candidates DECLINED** (`RpcMessageParser`, `_notify` — identical, no drift) against one taken. `_fcWindow` was byte-identical in both http2 transports too, so the drift test cleared it — and reading it against the POLICY rather than its twin found a third `??` clause that **can never evaluate**, because `RpcSecurityPolicy`'s const default for the field is non-null. Two copies of dead code each restating the default as a literal, invisible to `dead_null_aware_expression` because the field is `int?` though the const value never is. Lens gains: **identical copies can both be wrong**, and most step-1 results are not findings
- **[310](310-the-lens-applied-after-all.md)** FIXED, rpc_dart_isolate + rpc_dart_websocket — **corrects round 309**, which closed isolate with "RPC-25 does not apply" by ARGUING about the mechanism instead of running step 1. Run properly, the grep returns four identically-named fields and a byte-identical `close()`: the two channel variants are one lifecycle written twice. Two drifts, and the first is a live defect — the web copy answers a send failure with `await close()`, killing every in-flight call over one non-cloneable payload, which is exactly what the VM copy's own comment documents as wrong. Also finishes websocket's boundary: `grpcStatusFromWebSocketCloseCode` hidden, matching what 307 did to its http2 twin (surface 5 -> 4, 10 errors all in tests). **Different mechanism is not different abstraction**
- **[309](309-three-drains-one-of-them-quiet.md)** FIXED, rpc_dart + rpc_dart_websocket + rpc_dart_http + rpc_dart_http2 — RPC-25's second application, and the one that reaches **websocket**, which until now had been touched for documentation only. Three servers each polled a count to zero; **65 lines -> a 36-line `drainUntilIdle`**, net **-38**. The drift was in the LOGGING rather than the logic: start-log `debug` on http against `info` on the other two, and only http2 ever reported the drain CONVERGED — so on two servers success was signalled by an absence. `_notify` was the other named candidate and is deliberately NOT merged: no drift, eight lines, and merging would add a core promise to save them. Also finds that RPC-25 **does not apply** to isolate, which has no siblings
- **[308](308-four-routers-and-the-one-that-drifted.md)** FIXED, rpc_dart + rpc_dart_http + rpc_dart_http2 — the mandate's THIRD clause, code and abstractions, which 293-307 never touched and no lens covered. Derives **RPC-25** and applies it: four transports each hand-rolled the same per-stream router, **121 lines -> a 57-line `RpcStreamRouter` in core**, net **-64** in the transports. **The extraction is not the finding — the drift it exposed is**: the http2 caller returned an EXISTING stream unmetered where its own responder metered both paths, so a second consumer never discharged its flow-control budget and the call would be refused at the window for bytes it consumed. Two files that never import each other; the analyzer and the suite both green. The mandate's three clauses now each have a lens
- **[307](307-seventeen-promises-nobody-made.md)** FIXED, rpc_dart_http2 — the BOUNDARY half of the mandate, and RPC-24's first application outside core. One line, `export 'rpc_http2_common.dart';`, published **18 declarations of which 17 nothing outside the package used** — header conversion, frame validation, the status mappers, `disableNagle`, the pump. Surface **21 -> 4**. Blast radius MEASURED rather than predicted (the correction 292 wrote into this lens): **30 errors, 5 test files, 0 in lib/, 0 in any other package**. The other three transports were checked and are clean, so unlike RPC-23's fusion this is a property of ONE barrel, not the corpus
- **[306](306-the-mandate-finishes-in-one-language.md)** FIXED, rpc_dart_http2 — finishes the caller transport and with it the DOC half of the owner's mandate: **-73 lines, Cyrillic 0**, and `grep -rlE "[А-Яа-яЁё]" packages/core/*/lib packages/transport/*/lib` now returns nothing — every `lib/` in core and all six transports is in one language. 14 rounds (293-306), six packages, **zero code lines changed**. Also states plainly what the mandate still lacks: the API/boundary sweep (RPC-24 never applied to a transport) and the code/abstraction work, neither blocked, each a lens of its own
- **[305](305-the-public-half-of-the-last-file.md)** FIXED, rpc_dart_http2 — the caller transport, the mandate's largest file (675/1989) and last, taken as its PUBLIC half on purpose: the doc surface plus the adjacency defect, leaving the internal log strings named exactly. The **eighth** declaration fusion, and the one that completes the argument: `_connectH2ViaProxy`'s doc was eaten by a `const` 67 lines above it, so the file's most intricate function had none. Across 296-305 the shape has landed on a class, a method, a local block, a typedef and a static function — independent of declaration kind, of density and of package. **-28 lines, 26 of 97 Cyrillic lines**; the remaining 71 are internal diagnostics and are one round from done
- **[304](304-the-doc-that-outlived-its-code.md)** FIXED, rpc_dart_http2 — the responder transport, second of three http2 batches. **A doc that outlived its code**: `RpcHttp2OutgoingPump` moved to `rpc_http2_common.dart` and took its doc, and the `typedef` left behind kept a 29-line VERBATIM copy whose last lines say the real one is elsewhere — the same explanation in two files, the copy on a one-line alias. That gives the lens's duplication shape a cause it did not have: a MOVE leaves the doc behind. Also the language defect the server had (log strings, field docs, an assert message) and a comment about a method deleted years ago. **-97 lines**, Cyrillic 0
- **[303](303-a-server-written-in-another-language.md)** FIXED, rpc_dart_http2 — the mandate's last and largest package (1762/4826), taken as two rounds; this one sweeps server, common and the header guard. A SEVENTH declaration fusion (`_releaseEndpoint`'s doc on `_drain`, leaving it undocumented 140 lines below) — and a finding that was not on the lens at all: **`rpc_http2_server.dart` was written in Russian**, class doc, parameter list and every runtime log string, against the root CLAUDE.md's "English for code, comments, and logs". Also a misspelling in a public doc (`"Gárrantees"`) and three paragraph fusions. **-136 lines**. The language check that confirmed the file clean found 25 such files repo-wide → **B-30**
- **[302](302-the-shape-is-not-a-density.md)** FIXED, rpc_dart_isolate — the first package the detector does NOT flag (18.2%, under threshold), taken precisely to test whether the fusion shape needs bloat to exist. **It does not**, and the instance here is the worst of the series: one `//` block carrying two subjects, the first describing code **76 lines below it**. Also a rule-one divergence the sweep found and no gate can: the stub documented itself as the fallback "e.g. web" while the conditional export three lines away routes web to a real Worker implementation. **-29 lines**, and the stub GREW 5->9 — fixing a false statement costs lines, so the metric is a detector, not a target
- **[301](301-the-language-server-saw-it-too.md)** FIXED, rpc_dart_http — the mandate's third package: **-178 comment lines**, 39.2% -> 33.0%, no code changed. A SIXTH fused doc, and the round that finally measured what the shape costs: `_readBounded`'s doc — the bound on what a hostile server can make the client allocate — sat on `_readErrorBody`, and the **LSP hover returned signature only before the fix and the docstring after**. A fused doc is not hard to read, it is INVISIBLE to every tool that reads docs by symbol. Also names a new sub-shape, PARAGRAPH fusion (three in this package), which the compiler cannot see at all
- **[300](300-a-class-doc-that-documented-a-function.md)** FIXED, rpc_dart_websocket — the mandate's second package, swept whole: **-205 comment lines**, 45.5% -> 37.1%, no code changed. The adjacency shape a FIFTH time and the first OUTSIDE core, which settles that it is a property of the corpus: `RpcWebSocketChannel`'s doc and code sample had fused onto `grpcStatusFromWebSocketCloseCode`, so the **public exported class** the library doc tells you to construct had none, and the close-code mapper wore a `RpcChannelTransport.fromChannel` sample. Same file also carried the close-code narrative twice, and the keepalive/deflate-bomb tables were duplicated across two files. `license:check` went red on an untracked `.serena/`, now annotated in `REUSE.toml` beside `.claude/settings.json` and configured
- **[299](299-a-token-and-its-generator.md)** FIXED, rpc_dart — five more files, **-65 lines**, core 25.9%; **-550 across 297-299 over seventeen files**. The adjacency shape a FOURTH time, and this one HID something rather than misattributing it: `_uniqueToken`'s doc sat on `_strongRng`, so the function minting every request and trace id had none — and the fact that `Random.secure()` THROWS on node, making those ids non-cryptographic, was buried under a heading about a different member. Core is past the point of diminishing returns; websocket is next
- **[298](298-a-doc-block-on-two-declarations.md)** FIXED, rpc_dart — six more files at the batch unit, **-193 comment lines**, core 26.7% -> 26.1%. The adjacency shape appears for the THIRD round running and in its clearest form: `_normalize` carried `register`'s entire doc block, code sample included, and `register` carried the same nine lines again twenty lines below — the same instruction twice, with no way to tell which was current. Also: a doc correcting an earlier revision of ITSELF (`retryOn`), and 74 lines of class doc whose four samples differed only in which argument was set
- **[297](297-the-detector-counted-a-third.md)** FIXED, rpc_dart — **the owner said the mandate covers `//`, not just `///`, and re-measuring showed the lens had been ranking on a third of the evidence.** Every file rounds 293-296 reported finished still sat at 42-49% comment; core's real baseline is **6539 / 23718 = 27.6%**, not the 18.4% the doc-only metric gave. Six files swept whole for **-292 lines**, and adjacency found three more defects: eight lines duplicated VERBATIM in `getMessagesForStream`, `_reclaimGrace` wearing `_onDeadlineExceeded`'s doc, and one comment citing `":1166-1174"` as a reference. Unit raised again, to a BATCH of files
- **[296](296-a-comment-on-the-wrong-declaration.md)** FIXED, rpc_dart — first round at the file-sized unit, and it justifies the unit: sweeping `channel_transport.dart` whole found **two doc comments fused with no declaration between them**, both attached to the constant, so `_validateInbound` had no doc and the constant's read as if the first half described it. Only adjacency shows that. 303 -> 245; core 4281 -> 4223
- **[295](295-the-invariant-not-the-evidence.md)** FIXED, rpc_dart — `responder_pipeline.dart` 308 -> 270, after settling in the lens how to judge INTERNAL code: a private field has no caller, it has a maintainer, so the keeper is the INVARIANT and the measurement that proved it is still journal. Core 4430 -> 4281. **The owner stopped the pace mid-round** — four blocks per round does not finish a 4430-line audit; from 296 the unit is a FILE
- **[294](294-four-copies-of-one-rule.md)** FIXED, rpc_dart — `core/transport.dart`, chosen by RPC-23's ranking rather than by eye: 238 doc lines in 559, and one paragraph copy-pasted into **all four** capability interfaces. Copy-paste is what a comment does instead of a concept — stated once, referred to from the rest. 238 -> 212; core total 4430 -> 4319
- **[293](293-lenses-for-the-mandate.md)** FIXED, rpc_dart — the lens set was all defect-shaped, so rounds 289-292 picked targets by instinct. Adds catalog shapes **U-22** (the narrative beside the code) and **U-23** (public by omission) in a new `refactor` pack, instantiated as RPC-23/RPC-24. Applied once: `RpcSecurityPolicy` 236 -> 151 doc lines. Baseline for the mandate: **4430 / 24049 = 18.4%**
- **[292](292-nobody-was-using-the-re-export.md)** FIXED, rpc_dart — `export 'dart:typed_data'` deleted from the public barrel. Rounds 289 and 291 both planned around "one added import per affected file across five packages"; measured, it is **zero** — everyone who uses `Uint8List` already imports it. Two rounds planned around a cost nobody had measured
- **[291](291-the-boundary-drawn.md)** FIXED, rpc_dart — steps 0 and 1 as one edit: `lib/src/_internal.dart` gives the implementation its own barrel, the eleven `lib/` files stop importing the public one, and the public barrel then hides 13 machinery types. **152 -> 139 public types, 11 -> 0 internal imports of the public API, 21 packages + wasm analyse clean** — no dependent package used any of them, confirming 289's scoping
- **[290](290-the-boundary-cannot-be-drawn-yet.md)** INCONCLUSIVE, rpc_dart — tried 289's step 1 and it cannot be done yet: **core's own implementation imports core's public barrel**, eleven files under `lib/`, so `hide` deletes names the library needs from itself. 78 analyzer errors, 7 inside `lib/`. Reverted, tree green, and the export now carries a one-line comment so the next attempt does not rediscover it by breaking the build
- **[289](289-what-the-core-actually-exports.md)** DEFERRED, rpc_dart — the refactor mandate's first measurement: **152 public top-level types** reachable from `package:rpc_dart/rpc_dart.dart`, because the barrel re-exports nine subdirectory barrels wholesale. ~15 are pipeline machinery (this loop used two of them from probes), ~18 are logging that `rpc_dart_log` also owns, and line 8 re-exports all of `dart:typed_data`. Four-step plan recorded; nothing changed, because a half-applied boundary is worse than none
- **[288](288-the-websocket-half-of-the-same-default.md)** FIXED, rpc_dart_websocket — the same default one transport over: 20 peers that upgrade and go silent hold 20 endpoints and 0 disposals; with keepalive, 0 and 20. Exists because the previous answer QUOTED the function's own doc instead of measuring. Also cut that doc from ~150 lines to ~40
- **[287](287-the-default-was-the-decision.md)** FIXED, rpc_dart_http2 — `pingInterval` defaults to 30s. It is the only thing that reclaims a peer which spoke the preface and went silent, and it shipped `null`, so the DEFAULT decided whether a server had any bound on held endpoints (200 vs 0). The gate caught a defect the round introduced: `pingTimeout`'s null means "follow pingInterval", so defaulting it broke a caller passing 2s. B-27 closes
- **[286](286-twelve-milliseconds-against-sixteen-seconds.md)** DEFERRED, rpc_dart_compression — B-29 measured: the same gzip payload is refused in 12 ms on the VM and **15980 ms on dart2js**, 1332x, because `boundedInflate` is `=> null` there. ~65 KiB of wire buys 64 MiB and sixteen seconds of the event loop. No fix worth proposing; the round shipped the cross-runtime test instead
- **[285](285-the-mitigation-that-is-vm-only.md)** INCONCLUSIVE, rpc_dart_compression — the ISIZE-wrap bomb's fix is `boundedInflate`, which on web is `=> null`, and the one test exercising that bomb is `@TestOn('vm')`. The platform lacking the defence is the platform the test cannot run on. Established by reading, NOT measured — B-29 carries the dart2js probe design
- **[284](284-the-reflection-service-holds.md)** CLEAN, rpc_dart_grpc_reflection — the service that answers UNAUTHENTICATED peers and decodes their protobuf by hand, in one round file before this (219, a gate census). Seventeen malformed requests: 0 leaked Errors, 0 leaked Exceptions. The un-typed `catch (_)` is what holds. Only the SIZE column found anything — a 1 MiB symbol echoes back 1 MiB
- **[283](283-the-reset-is-the-cheap-arm.md)** CLEAN, rpc_dart_http2 — C-32's uncovered CPU half, refuted by the control moving FURTHER than the subject: the same 2000 streams not reset stall an unrelated connection 679 ms against the reset arm's 373 ms. Resets buy an attacker LESS work than a legitimate client. Only the worst case moves; a median-only bench would have called all three arms identical
- **[282](282-metadata-is-never-paced.md)** DEFERRED, rpc_dart — metadata is never paced AND nothing catches it: 4000 sends, 32 MiB, none blocked, against a control that parks at exactly the 64 KiB window. The queue's byte bound is on the broadcast; these land in the per-stream `StreamController`, unweighed and uncapped. Flow control is the only thing in front of it. B-28, owner decision
- **[281](281-the-asymmetry-that-is-not-there.md)** INCONCLUSIVE, rpc_dart — flow control is NOT the third instance of 279/280: `sendMetadata` spends no window and `_fcOnConsumed` returns on `bytes == 0`, so the two ends agree and nothing leaks. What the reading leaves — metadata is unpaced entirely, and a flood now dies on the queue's HARD bound rather than being throttled — needs a send-path bench this round could not build. B-28
- **[280](280-the-second-budget-that-weighs-payload-only.md)** FIXED, rpc_dart — the pre-method budget charged `payload?.length` only, so a frame of one payload byte and 2000 headers cost it ONE BYTE: 789.2 MiB parked against a 16 MiB ceiling that read 0.00, with no event cap behind it. 4000 parked -> 106. Flow control charges the same way at six more sites, named for the next round
- **[279](279-a-header-weighs-more-than-its-characters.md)** FIXED, rpc_dart — round 278's own leftover, which 278 dismissed in prose instead of measuring. The queue's byte bound weighed a header by its CHARACTERS: 186 MiB retained against a 16 MiB bound at three scales, and at 500 headers per frame the byte bound never engaged at all. 190.8 -> 20.8 MiB. RPC-17's dimension clause, wrong for the third time in the same file
- **[278](278-seventeen-hostile-frames.md)** CLEAN, rpc_dart — C-02 re-measured: it had read "a battery of nine malformed frames, the codec is clean" and named none of them. Seventeen named cases now: 11 typed refusals, 3 short-reads, 3 legal-but-unusual accepted, **0 leaked Errors** — including 20000-deep JSON, the one that could have been a StackOverflowError
- **[277](277-rapid-reset-dispatches-nothing.md)** CLEAN, rpc_dart_http2 — HTTP/2 Rapid Reset (CVE-2023-44487), never measured here before: 0 handlers from 200 resets against 4 from 200 normal calls at a ceiling of 4. The control earned its place — BOTH arms first read 0, because the hand-built body was JSON and the wire format is CBOR (L-10)
- **[276](276-the-same-defect-in-the-sibling.md)** FIXED, rpc_dart_websocket — round 272's defect in the sibling: `_refuse` drains with no deadline and is `unawaited`, so any number run at once. Reachable only when the origin check is CONFIGURED — turning the security control on is what opened it. The trick is the request shape: dart:io gives an upgrade request no body, so the holding attack is a plain POST
- **[275](275-a-deadline-on-saying-nothing.md)** FIXED, rpc_dart_http2 — the owner chose B-27's deadline and this ships it: `prefaceTimeout`, 30s by default, armed on accept and disarmed by the connection preface. 200 silent sockets 200 -> 0 endpoints. It also corrects the option as it was put — 24 preface bytes take the hold straight back, so this bounds scanners, not a determined peer
- **[274](274-work-before-the-peer-speaks.md)** DEFERRED, rpc_dart_http2 — a TCP SYN builds an endpoint and runs the application's `onEndpointCreated`: 200 sockets sending ZERO bytes gave 200 endpoints and 200 contract constructions, against 0 and 0 on the websocket sibling. Every limit this server has is per connection, so all of them are downstream of it. B-27, owner decision on the default
- **[273](273-the-twin-defect-that-is-not-there.md)** CLEAN, rpc_dart_http — B-26 refuted one round after it was filed: `.timeout()` on the body read really does stop the read, because dart:io detaches the body of a finished exchange. 384 KiB accepted after the 408 against 16384 KiB in 49 ms with the deadline off
- **[272](272-refused-is-cheaper-than-accepted.md)** FIXED, rpc_dart_http — being REFUSED was cheaper than being accepted: `_reject` drains the body with no deadline and before any counter exists, so 16 sockets sending 5 bytes each held 16 endless read loops while `pendingRequests` read 0. New lens RPC-22
- **[271](271-the-stream-opened-before-the-request-existed.md)** FIXED, rpc_dart_http — the responder announced a stream to the pipeline before reading the body, and its error path cleans up only its own map: 8 aborted requests parked 8 pipeline streams for 60 s WITH the documented `bodyReadTimeout` on, wedging a budget every client of that server shares
- **[270](270-a-gate-that-cannot-fail-is-not-evidence.md)** RETRACTED, no packages — `test:wasm` resolves the LOCAL core after all; `pubspec_overrides.yaml` was one directory listing away. Filed retroactively in 271
- **[269](269-what-test-wasm-actually-resolves.md)** RETRACTED by round 270, no packages — the claim 270 retracted, and B-23's dossier half sized at 1653 lines. Filed retroactively in 271
- **[250](250-the-ordering-coincidence-becomes-an-invariant.md)** FIXED, rpc_dart — the frame channel's inbound controller buffers like the other six; shipped with NO canary, on the owner's decision, and the record says so
- **[268](268-three-flags-three-meanings.md)** CLEAN, rpc_dart — RPC-19 re-swept over its one moved file: zero lines in that diff touch a lifecycle flag, and `_closed` / `_isStopped` / `_disposed` still mean three different things
- **[267](267-the-documentation-b-23-was-hiding.md)** FIXED, rpc_dart — B-23's architecture half: an inventory made from file names predicted five duplicates, the reading gave two additions and two documentation defects. Three docs shipped, one repaired (it taught `RpcLogger`, which does not exist), four notes retired
- **[266](266-the-guard-that-owed-the-pool-forever.md)** FIXED, rpc_dart — a paused consumer never receives `done`, so it never repaid the connection pool; `_fcForget` now repays unconditionally. 1024 -> 3072 KiB, wedged at call 4 -> never. One line, after eighteen rounds proving the danger it guarded against does not exist
- **[249](249-the-mark-works-and-was-reverted.md)** DEFERRED, rpc_dart — B-22's mark built and measured lifting the wedge (1024 -> 3072 KiB, suite green), then reverted: two of the three canary arms, and the missing one guards an inflating window
- **[248](248-the-obvious-fix-for-b-22-is-wrong.md)** DEFERRED, rpc_dart — B-22's mechanism localised to one condition, and the one-line fix it suggests refuted: it would credit the connection twice
- **[247](247-b-11s-blocker-still-holds.md)** DEFERRED, rpc_dart — B-11's blocker re-checked and CONFIRMED: no latency helper exists anywhere in the tree, so the fourth bench attempt must build the delayed link first (~25 lines, `_ManualChannel` as the model)
- **[246](246-no-new-timeouts-to-abandon.md)** CLEAN, rpc_dart — RPC-14 re-swept over its diff: zero new `.timeout(` sites, and the two awaits added on resource-holding paths are guarded, one of them being the lens's own adopt-the-abandoned-future fix
- **[245](245-the-dimension-236-excluded.md)** FIXED, rpc_dart — metadata weighed zero against the unlistened queue's byte bound: 4096 frames retained 256 MiB against a 16 MiB cap, now 255 and 15.9 MiB
- **[244](244-the-parked-sender-still-wakes.md)** CLEAN, rpc_dart — RPC-09 re-swept over 6 moved files: close() still wakes every parked sender, and round 240's buffering turned out to have SHRUNK the hazard this round set out to attack
- **[243](243-the-trailer-cap-held.md)** CLEAN, core and the three transports — RPC-02 re-swept over 10 moved files: every message-carrying trailer still capped, including both refusal paths added since
- **[242](242-the-callback-that-ends-the-isolate.md)** FIXED, rpc_dart — a throwing `onStateChanged` reached the root zone AND aborted the connect loop before it built anything: unhandled 1 -> 0, transports 0 -> 2
- **[241](241-the-discard-that-sometimes-does-not-land.md)** DEFERRED, rpc_dart_http2 — a sequential reconnect orphans a DISCARDED connection about 1.3% of the time; the candidate fix could not be witnessed, so it was reverted
- **[240](240-the-buffer-drained-into-nothing.md)** FIXED, rpc_dart — the reconnect proxy drained every transport's inbound buffer into a controller nobody was listening to
- **[239](239-finishing-what-234-opened.md)** CLEAN, no packages — finishing what 234 opened: six more memory dossiers into the journal, and the note graph repaired
- **[238](238-the-recovery-api-works-more-than-once.md)** CLEAN, the four transports and core — no third instance of the conflated lifecycle flag; the ablation is what makes that mean something
- **[237](237-the-dependency-shared-what-we-copied.md)** FIXED, rpc_dart_http2 — the dependency shared what we copied: 63 KiB of header block became 258 MiB in the adapter
- **[236](236-the-bound-that-counted-the-wrong-thing.md)** FIXED, rpc_dart and the three transport packages — the pending queue counted events while the damage was bytes: 4096 x 16 MiB admitted
- **[235](235-the-one-hop-nobody-guarded.md)** FIXED, rpc_dart — the one hop nobody guarded: an unguarded cancel between two guarded closes
- **[234](234-the-reconnect-nobody-drives.md)** FIXED, rpc_dart and rpc_dart_websocket — the reconnect nobody drives: a peer-started drop rewound the stream-id cursor
- **[233](233-websocket-rescan-the-first-third.md)** CLEAN, rpc_dart_websocket — websocket rescan, the first third
- **[232](232-the-selector-read-an-archive-as-a-decision.md)** FIXED, rpc_dart — the selector read an archive as a decision
- **[231](231-the-two-candidates-are-one.md)** DEFERRED, rpc_dart — the two candidates are one
- **[230](230-the-last-round.md)** CLEAN, rpc_dart — the last round; the swallowed grant failure is unreachable and load-bearing
- **[229](229-a-zero-grant-is-not-silence.md)** FIXED, rpc_dart — a zero grant is not silence
- **[228](228-the-branch-206-did-not-cover.md)** DEFERRED, rpc_dart — the branch round 206 did not cover
- **[227](227-the-web-guard-does-catch-it.md)** CLEAN, rpc_dart and rpc_blob — the web guard does catch it
- **[226](226-close-the-gate-over-wasm.md)** FIXED, rpc_dart_wasm — close the gate over wasm
- **[225](225-the-guard-nothing-reaches.md)** CLEAN, rpc_dart — the guard nothing reaches
- **[224](224-refuse-a-transport-that-cannot-carry-the-watermark.md)** FIXED, rpc_dart — refuse a transport that cannot carry the watermark
- **[223](223-the-isolate-exception-closed-and-a-pattern.md)** CLEAN, rpc_dart_isolate — the isolate exception closed, and a pattern
- **[222](222-every-site-guarded-the-guard-untested.md)** CLEAN, all five transport packages — every site guarded, and the guard untested
- **[221](221-the-sweep-that-could-not-see-a-hang.md)** CLEAN, rpc_dart and rpc_dart_http2 — the sweep that could not see a hang, now can
- **[220](220-the-package-the-gate-cannot-see.md)** CLEAN, rpc_dart_wasm — the package the gate cannot see
- **[219](219-what-the-web-gate-actually-covers.md)** CLEAN, rpc_dart — what the web gate actually covers
- **[218](218-generation-tagging-cannot-work.md)** DEFERRED, rpc_dart — generation tagging cannot work, and the measurement says so
- **[217](217-a-decorator-erases-the-stream-id-watermark.md)** DEFERRED, rpc_dart — a decorator erases the stream-id watermark
- **[216](216-a-refusal-survives-its-own-policy.md)** CLEAN, rpc_dart and rpc_dart_http2 — a refusal survives its own policy
- **[215](215-the-pre-method-budget-comes-back.md)** CLEAN, rpc_dart — the pre-method budget comes back
- **[214](214-handler-slots-come-back.md)** CLEAN, rpc_dart — handler slots come back on every teardown path
- **[213](213-a-slow-handler-is-killed-not-throttled.md)** DEFERRED, rpc_dart_http2 — a slow handler is killed, not throttled
- **[212](212-a-late-grant-resurrects-a-dead-stream.md)** FIXED, rpc_dart — a late grant resurrects a dead stream's credit
- **[211](211-the-wake-is-load-bearing.md)** CLEAN, rpc_dart — the wake is load-bearing after all
- **[210](210-the-answer-does-not-wait-on-the-send.md)** CLEAN, rpc_dart and rpc_dart_http2 — the answer does not wait on the send
- **[209](209-a-decorator-can-switch-the-bound-off.md)** FIXED, rpc_dart_http2 — a decorator that declares a capability can switch the bound off
- **[208](208-refuse-instead-of-pausing.md)** FIXED, rpc_dart_http2 — refuse the stalled call instead of pausing the read
- **[207](207-http2-cancel-kills-the-connection.md)** DEFERRED, rpc_dart_http2 — cancelling a stalled http2 call kills the connection for good
- **[206](206-connection-credit-never-repaid.md)** FIXED, rpc_dart and rpc_dart_isolate — connection credit is never repaid for bytes nobody consumed
- **[205](205-policy-limits-clean.md)** CLEAN, websocket and isolate — DoS limits really do bite on the channel transports
- **[204](204-retraction-listen-onerror.md)** RETRACTED, rpc_dart, `6ae31212` — the request stream carries cancellation, so listen needs onError
- **[203](203-failed-fix-attempt.md)** revised by round 204, rpc_dart — an attempt to fix a defect that did not exist
- **[202](202-clientstream-cancel-claim.md)** revised by round 204, rpc_dart — the claim that cancelling a clientStream kills the process
- **[201](201-cancel-told-server-first.md)** FIXED, rpc_dart, `fdab119f` — a cancelled call told the server before telling itself

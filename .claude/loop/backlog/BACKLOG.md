# Loop backlog

What a lead is and how it links to the rest — [../LOOP.md](../LOOP.md). The line
order below is the rank: **decided-and-ready first, then by damage class, then by
the age of the number.** Re-ranked in round 223, when the owner cleared the
entire awaiting-decision queue.

**B-56 sits at the head of the rank by the owner's explicit call (round 415),
ahead of the damage classes.** It is the one lead whose cost rises with delay
rather than staying flat: all five copies of the subscription discipline are
correct today, so the witnesses transfer unchanged, and the next copy to drift
turns a refactor into a refactor plus a bug hunt. Four consecutive rounds each
found a different copy missing a different clause, which is what the ranking is
paying to stop.

`(stale, sha)` means code under that lead's paths has changed since its number
was taken. The blocker and the number age separately — see U-21.

**Curate after round 327 classified all eight stale leads instead of trusting
the flag**, and the first pass at it was wrong — a guess that "most of this is
the lint rounds" survived until the `git log`s were actually read:

```
                 commits since its sha        verdict
  B-28                 2   both docs          mechanical
  B-29                 1   the lint floor     mechanical
  B-31                 1   the lint floor     mechanical
  B-32                 1   the lint floor     mechanical
  B-11                 7   2 behavioural      GENUINELY AGED
  B-21                12   8 fix() + 1 refactor!   GENUINELY AGED
  B-09, B-23          29   pre-201 shas       GENUINELY AGED
```

Four are `(stale, mechanical)`: their numbers stand and re-measuring them is work
with a known answer. **B-11 and B-21 are not** — B-21 in particular has eight
`fix(rpc_dart)` commits across `client_connection.dart` and `transport.dart`
since its number was taken, which is exactly the shape round 319 found under
RPC-02. They are ranked accordingly and marked the ordinary way.

## The leads

**State is on the line, not in a heading.** Each line opens with the checkbox
its `status:` demands — `[ ]` open, `[?]` awaiting owner, `[+]` decided and not
yet carried out, `[x]` closed and struck through — and `loop.py lint` checks the
two against each other.

The headings below used to group by state, and that is why this note exists:
nothing read them, so they drifted. "Awaiting an owner decision" held fourteen
leads while every one of them said `status: open`, and `loop.py status` printed
**"Awaiting owner: 0"** directly underneath. The three that genuinely awaited an
answer — B-37, B-43, B-47 — were indistinguishable from the eleven that did not.
The headings that remain group by SUBJECT or carry prose worth keeping; none of
them decides anything.

Re-sorted in the audit after round 287, which found this section holding one
CLOSED lead and a paragraph of orphaned prose while the two leads that actually
await a decision sat under "Open". They are MEASURED; what is missing is a
choice about what the library promises, not a number.

**Round 415 took the owner through every live lead in one pass — 23 of them, in
order.** Nothing is `awaiting owner` any more. The outcome was not what the
ranking predicted: six leads CLOSED without work (B-11, B-23, B-32, B-41, B-52,
plus B-09 answered in place), and the pass found **three things no round would
have**:

```
B-09  the round-89 revert's stated blocker is DEAD -- the test it
      named pins UNAVAILABLE today, not the StateError it rested on
B-21  its "take it at the next major" condition was already met by a
      breaking change shipped the same session
B-30  the three in-mandate files it named are gone, and the half it
      never counted (47 test/example) is larger than the half it did
```

All three are the same defect in a backlog rather than in code: **a lead records
the reason it was deferred, and nothing re-reads that reason when the world
moves.** `loop.py stale` ages a lead against its PATHS; none of these three
changed under its paths.

**Round 350 is the warning against over-using this section.** B-28 sat here for
68 rounds framed as "either pace metadata or bound the buffer, ask the owner" —
and one of the two was never available: HTTP/2 exempts HEADERS from flow control
for a deadlock reason the spec states. What looked like a trade about library
behaviour was a question the protocol had already answered. Before parking a lead
here, check that both branches actually exist.

- [+] **[B-56](B-56-five-copies-of-one-subscription-discipline.md)** — **THE PRIORITY. Ranked first by the owner in round 415, ahead of the damage classes below.** Decided: extract it. The same mechanic, *drive a user-supplied stream into a call*, exists **five times** — `ClientStreamCaller.call(Stream)`, `ServerStreamResponder`, `caller_pipeline`'s bidi bridge, `requestSink`, `responseSink` — and four consecutive rounds each found a DIFFERENT copy missing a DIFFERENT clause: tell the peer (384), close without throwing (386), stop on `done` (390), do not await the cancel (391). That is the whole case for one owner instead of five. **The timing is why it is first**: every instance is correct as of 391, so the witnesses transfer unchanged and the refactor is measured against a green suite rather than a moving one — the window closes the moment any of the five drifts again. Two constraints: **a helper taking a subscription plus callbacks, NOT a base class** (the five genuinely differ in what they own — two have a sink and three take a `Stream`, the responder IS the producer so it has no "stop on done", and the bridge owns both directions; a base class forces those into flags), and **one call site at a time with its witness green after each**, because five at once means a regression with five candidate causes. The helper owns: pause per send, cancel UNAWAITED (L-16), close only after that cancel, stop pulling on the call's `done`, route every failure away from the zone. **Swept in 415 and it is NINE, not five** (READ, not measured) — the criterion is whether the library OWNS the stream, since only a user-supplied one or a user's `async*` chain can park on cancel. Four more: `_bridgeCallerResponses` (`caller_pipeline.dart:250`), `CallScope.track` and `CallScope.listen`, `_wrapStream` (`circuit_breaker_interceptor.dart:203`). **Extract FROM `_bridgeCallerResponses`** — it is the most complete of the nine and nothing named it, carrying the clause the others lack entirely: fire the cancellation token only when the stream did not already finish, or a REUSED `RpcContext` is poisoned and the NEXT call on it throws. **Three defects fell out**: `CallScope.listen` awaits a cancel through a disposer (`:181` + `:224`, 5 s timeout) thirty lines under the comment saying not to; `circuit_breaker_interceptor.dart:282` drops a cancel Future bare, the exact shape `track`'s comment records as measured isolate-death; and neither `CallScope` method forwards pause at all. The transports are **NOT** swept — 17 awaited-cancel sites, almost all on streams the library built, and separating the exceptions is a different job
- [x] ~~**[B-43](B-43-ios-send-order-is-a-convention.md)**~~ open, owner decision (round 365) — iOS `_rpcWasmSendBytes` does an UNAWAITED `fetch` per frame, so N are in flight and the order the Swift scheme handler sees them is WebKit's dispatch order — conventional, not contractual. Ordering is not cosmetic here: `RpcChannelTransport` reassembles a byte STREAM, so two swapped frames are corruption, not a reordered pair. **Measured: the convention holds** — 10000 frames in order on both platforms, iOS in 5.0-14.7 s, the first time this has been checked rather than assumed. The proposed promise chain makes the guarantee contractual and **serialises every send**, removing the pipelining those figures rest on — a speed trade, and the config's standing requirement is to ASK before making one. Three options in the lead (leave it with the regression witness now in the suite; chain unconditionally; chain with a window plus a sequence number), no recommendation, because the choice turns on what the throughput is worth
- [x] ~~**[B-42](B-42-ios-strip-failfast-unwitnessed.md)**~~ **closed (round 365)** — round 363's module-syntax fail-fast shipped in BOTH plugins with only the Android half witnessed, on the argument that it adds a check to an already-failing path (so a false negative leaves today's behaviour) and that the dangerous direction — the false POSITIVE of refusing working glue — lives in the line-anchoring rule, which is identical in both languages and was measured on Android (`+21 ~2` → `+3 ~2 -13` when made naive). **A simulator came up and settled it: all five witnesses pass on iOS 18.6 unchanged (`+26`).** The check fires correctly on the other engine and the control still boots. The argument turned out to be right, which is recorded precisely because it might not have been. Still unmeasured, and not worth a device run of its own: nobody ablated the check ON iOS to confirm the 30 s watchdog is what it replaces there — a question about the old behaviour, not about the fix
- [x] ~~**[B-41](B-41-android-base64-on-the-main-thread.md)**~~ **closed (round 415)** — **the move is NOT shipped.** Moving work off a thread with no measurable improvement is a change nobody can defend later, and the emulator cannot measure this at all: its idle worst-case of 12-39 ms is the size of the effect, so any re-run there reproduces the non-result. The NEGATIVE is the deliverable and belongs in `checked/` — the occupancy is real and is not Base64, and the two remaining candidates cannot be moved. Reopening needs a physical device or an in-plugin timer, not another emulator. Original text: open, reason "bench" (round 362) — **the code fact is certain and the impact is not.** `CoroutineScope(Dispatchers.Main)` puts `Base64.encodeToString` per sub-64-KiB frame, `Base64.decode` over batches of up to 4 MiB per drain, and an `allocateDirect`+`put` per frame on the thread that dispatches every platform channel in the app. The fix was written, type-checks (`PASS swift / PASS kotlin`) and keeps the device suite green — and **five emulator runs could not tell it from its own ablation**: with Base64 put back on Main the main thread read 38 ms busy against 39 ms idle, no worse than the fixed state and better than the fixed state's own 70 ms. Idle worst ranges 12-39 ms, the size of the effect. **The negative is informative**: the occupancy is real (busy clearly worse than control in 3 of 5 runs) but does not come from Base64 — the remaining candidates cannot be moved, `evaluateJavaScriptAsync` marshalling 4 MiB across Binder and `messenger.send`, which must be on Main by contract. Also: the item's criterion ("no frames >16 ms") is unmeetable on this emulator, where the IDLE control alone exceeds it. Needs a physical device, or an in-plugin timer instrumenting both arms identically; the fix is written out in the lead
- [x] ~~**[B-40](B-40-method-path-from-key-drops-dots.md)**~~ **decided (round 415): apply the four lines now.** The bar argument was about whether to go LOOKING for this and is sound; the finding is already paid for, and holding a written four-line fix until some round happens to touch the method costs more in re-reading than in applying. Split on the LAST dot, unit test with `myapp.v1.UserService.Get` and the single-dot control. Original text: open, below the bar (round 360) — `_methodPathFromKey` splits a method key on EVERY dot, so `myapp.v1.UserService.Get` yields four parts and returns `/UnknownService/UnknownMethod`, while its sibling `_parseMethodPath` validates against `[A-Za-z0-9_.-]+` and admits those names DELIBERATELY — which is also what gRPC uses for fully-qualified protobuf services. Real drift, in one file. **The ordinary path is clean, measured**: dotted names round-trip, because `methodPath` rides on the frame and the formatter is consulted only when both retained messages are null while `methodKey` is set; round 360 did not build a witness that reaches it. The damage there is a wrong diagnostic STRING nothing routes on, so it is below the config's bar. The four-line fix (split on the LAST dot — a method name cannot contain one) is written out in the lead, to be applied by any round that touches the method for another reason
- [+] **[B-39](B-39-websocket-send-throws-into-the-root-zone.md)** **decided (round 415), jointly with B-35: zone-guard the CONSTRUCTION and ROUTE what it catches.** Only at the library's own entry points (`connect()`, the server's accept path); a user-supplied socket stays unguarded and that is documented. The handler reports through `LogScope` and surfaces the error on the affected channel, so a bare guard cannot turn a process death into a silent disappearance. **Accepted deliberately**: this reroutes EVERY async error from that channel, not only send failures. Do not re-attempt the two call-site fixes — both measured, neither works. Original text: open, owner decision (round 358) — `RpcWebSocketChannel.send` calls `_ws.sink.add` unguarded, and in the one turn between our OWN raw socket closing and `onDone` arriving it throws **into the ROOT zone**, which ends the isolate. **A peer close is NOT this defect** — the sink keeps accepting until it learns, measured across four arms — so the item's framing was wrong about both reachability and severity. **Both obvious fixes were applied and measured not to work**: `try { sink.add } catch (_) {}` changes nothing, and neither does `runZonedGuarded` at the call site, because `WebSocketSink.add` queues into a StreamController and the real `sendBytes` runs a microtask later in the zone that CONTROLLER was built in. What does work, measured: constructing the `WebSocketChannel` inside `runZonedGuarded` — the same arm returns normally and the construction zone receives the error. That makes it fixable at the library's own entry points (`connect()`, the server's accept path) and unfixable for a user-supplied socket. The decision is whether to zone-guard construction, which reroutes EVERY async error from that channel, not only send failures — **the identical decision B-35 is waiting on, one dependency over, and worth deciding together**. Guarded by `send_after_raw_socket_close_test.dart`, whose three GUARDs pin what is not broken
- [+] **[B-38](B-38-ios-recv-loop-dies-silently.md)** **decided (round 415): the OWNER boots the simulator, then the round finishes.** `xcrun simctl` and `open -a Simulator` are outside the agent's allowlist and five `flutter emulators --launch` attempts registered nothing, so this is not a blocker a round can retry through. Shipping on `analyze:native` alone was offered and DECLINED — round 348 established what a gate never shown to fail is worth, and this is a native change whose whole defect is that it fails silently. Run BOTH platforms: two boot scripts in two languages, so a fix to one is never a fix to the other. Original text: open, reason "bench" (round 357) — **the finding needs no device and is conclusive; the fix does and could not be run.** iOS drives host-to-guest with one long-poll `fetch('rpc-wasm:///recv')` whose `.catch` sets `_recvRunning = false` and returns — nothing restarts it, nothing reports it, so the direction is dead and silent while `recvQueue` grows and every in-flight call waits out an OPTIONAL deadline. **Android's driver loop already fixed exactly this**, and its comment describes the iOS behaviour: *"the isolate stayed in runtimes, nothing further was evaluated, and Dart was never told -- so every in-flight call hung"*; iOS does not even log. Reachable without the host stopping: `webView(_:stop:)` when WebKit cancels a scheme task, or a second `/recv` failing the first with "superseded by new recv". The fix was written and `analyze:native` returned `PASS swift / PASS kotlin` — **and was REVERTED**, because round 348 established what a gate never shown to fail is worth, and a native change nobody ran is what this project's config says must not ship. The lead carries the four-step patch, the witness design, and the two traps: back off through `_nativeSetTimeout` (the next tag declares a hoisted `setTimeout` POLYFILL that queues into the guest's own timer table) and report death by `postMessage`, not by a fetch of the scheme that just stopped answering. **Needs a booted simulator, not a decision** — five launch attempts and waits up to 240 s never registered a device, though one booted earlier in the same session
- [x] ~~**[B-37](B-37-endpoints-getter-excludes-peers.md)**~~ open, owner decision (round 354) — `RpcWebSocketServer.endpoints` is `whereType<RpcResponderEndpoint>()` over a list that deliberately holds BOTH kinds, so a server built with `onPeerEndpointCreated` reports zero endpoints however many connections it is serving. `stop()` is unaffected (it iterates the field, not the getter); what is affected is anyone asking the server what it is serving. **The type is the obstacle, not the logic**: the return type is fixed by `IRpcServer.endpoints`, `List<RpcResponderEndpoint>`, implemented by all three servers, and `RpcPeerEndpoint`/`RpcResponderEndpoint` are sibling subclasses with no narrower common type. So the choice is between widening a published interface (breaking, across three packages), adding a second getter (additive, leaves a getter that lies for one mode), and documenting the narrowing. No recommendation: the three differ in what they promise callers
- [x] ~~**[B-36](B-36-the-abandon-timer-fabricates-a-success.md)**~~ **decided (round 415): leave the breaker half-open.** `_releaseProbe()` instead of `resolve(success: true)`, so the next call takes its turn as the probe — an unobserved probe is not evidence of recovery, and this is the only ending in the class that invents a result rather than declining to state one. **The pinned test moves and the replacement is stronger**: its reason names the RELEASE while its assertion observed it through the CLOSE, and those came apart the moment the close became wrong. New assertion: `halfOpen` plus a following call that is ADMITTED, which checks the gate rather than a state a fabricated success also produces. Original text: open, owner decision (round 351) — the circuit breaker's probe abandon timer calls `resolve(success: true)`, so a probe that produced **no item, no error and no done event** is recorded as a recovery: `_onSuccess` zeroes the failure count and CLOSES the breaker, reopening full traffic to a service nothing observed. Not round 351's wedge class — this ending does release the gate, it just answers wrongly — and `audit_circuit_breaker_abandoned_probe_test.dart:155` pins `closed` there with a reason that names the RELEASE, so changing it means deciding which of the two the test was protecting. One line now that 351 added `_releaseProbe()`; the recommendation is to leave the breaker half-open, which is what every other inconclusive ending in this class already does
- [+] **[B-35](B-35-finish-throws-into-the-zone.md)** **decided (round 415), jointly with B-39: zone-guard the connection's construction and ROUTE what it catches** — the full statement of the decision is on B-39, because it is one mechanism and one trade. Here it means guarding where `RpcHttp2CallerTransport` builds its `ClientTransportConnection`. **This lead keeps its own qualifier**: nothing has yet produced a user-visible failure through `close()`, measured, so the carrying round is fixing a reachable-in-principle throw and must say so in its verdict rather than claiming a fix it cannot witness end to end. Original text: open, MEASURED (round 347) — `package:http2`'s `finish()` throws `StateError: Cannot add event after closing` into the zone **after its own future has completed**, so it is not a rejection and no handler at the call site sees it: `unawaited(terminate())` after finish, `terminate().catchError(...)` after finish, and **`finish()` with nothing after it at all** all produce it, which is what identifies the source. Three neighbouring states do not (terminate live, terminate twice, terminate after socket destroy). A lead rather than an incident because **the transport's own `close()` does not reach it** — measured over connect / call / close / close-again inside `runZonedGuarded`, nothing escapes, since close() RSTs every stream before calling finish() and rounds 346-347 could not make it time out even with the budget forced to 1 ms and to zero. Any fix is a design decision: only a zone contains it, and zone-guarding changes where genuine transport errors surface. **Do NOT fix it at the call site** — two attempts measured, both ineffective. Guarded by `finish_throws_into_the_zone_test.dart` so the lead closes itself if the dependency changes
- [+] **[B-33](B-33-blob-adapters-disagree-on-a-missing-blob.md)** **decided (round 415): unify all four.** In scope because B-10's deferral was NARROWED — it covers going looking in data/notify/blob, not fixing a defect already measured and written down there. Order matters: **measure the four-row matrix FIRST** (minio and sqlite were never compared, so "which answer is right" is otherwise decided on half the data), then write the answer onto `IBlobRepository.deleteBlob`, then one test per adapter. Needs the excluded services up, or it unifies two adapters and guesses about two. `IDataStorageAdapter` and `INotifyRepository` stay out — that is LOOKING. Original text: open, owner scope decision (round 318) — `IBlobRepository.deleteBlob` promises only "returns `true` when something was removed", and on the SAME input (blob missing, `expectedVersion` set) `in_memory` **throws StateError** while `webdav` **returns false**. These are interchangeable by design, so a caller written against one silently takes the other branch on the other. Two more implementations (minio, sqlite) were not compared; `IDataStorageAdapter` and `INotifyRepository` have the same structure and were not looked at. Aggravated by `*_postgres`/`*_minio` being excluded from `test:unit` — the least-exercised adapters are where this accumulates. Out of scope: core and transport only for now; sits with B-10
- [x] ~~**[B-32](B-32-zero-copy-unary-may-not-dispatch.md)**~~ **closed (round 415)** — bookkeeping caught up with the content: the record's own title ends "WRONG, closed" and its `reason` says "answered in the round that filed it", while `status` stayed `open` for a hundred rounds. Neither `lint` nor `sync-index` could see it, because the checkbox correctly matched the status field — only reading it does. The cosmetic remnant is the one RPC-25 declines. Original text: open, but the alarming half is ANSWERED and WRONG (round 316) — round 315 left the zero-copy generic asymmetry open; reading the dispatch produced a convincing argument that unary should throw, because the registry's static type is `<Object, Object>` and Dart function parameters are contravariant. It does not throw: **Dart generics are reified**, so `callUnaryHandler` runs with the instance's real type arguments, and `unsendable_direct_object_test.dart:120` already covers exactly the shape predicted to fail. One grep answered what a page of type reasoning did not. What remains open is only the cosmetic unification of the two registration shapes, which RPC-25 declines (no drift, no rule)
- [+] **[B-31](B-31-the-web-channel-has-no-reachable-witness.md)** **decided (round 415): route 3, `@visibleForTesting` plus the test that drives the channel.** The cheaper policy-extraction variant is declined on what it would witness — it proves the decision and leaves untested the place round 310 actually got wrong, which is where that decision is APPLIED: the `catch (_)` that swallowed the reason and the close that took every other in-flight call with it. The cost is contained: the test runs **on the VM**, because nothing in the failure path needs `dart:js_interop`. Both halves must show — the failing send names its error, AND a second call on the same channel still completes. Original text: open, owner decision (round 313) — round 310's isolate web `send` fix is analyser- and sibling-verified but has **no test**, and the only route to one widens the surface. Through the public API needs a real `Worker`, i.e. a browser the ordinary gate never runs. Directly on the channel would be one line — the class takes its transmit function as a constructor parameter, so a throwing stub reaches the defect with no Worker — but `_WebMultiplexedChannel` is library-private, and Dart privacy is per-library, so `src/`-importing does not help the way it did in 307. That leaves `@visibleForTesting`, which is an API-shape trade rather than a bug fix. A cheaper variant (extract the send-failure POLICY and test that) witnesses the decision but not the integration
- [+] **[B-30](B-30-russian-comments-outside-the-mandate.md)** **decided (round 415): sweep the 47 in-scope `test/` and `example/` files first; the 23 `lib/` ones wait.** Re-counted by hand in the review, because both numbers in this record had drifted — and the recount changes the shape: **the three http2 files it named are GONE**, swept incidentally, so the `lib/` half is now entirely outside the mandate (rpc_data 16, rpc_data_sqlite 3, rpc_blob 3, rpc_notify 1) while the in-scope half is the LARGER one, 47 against 23. `grep -rl "[а-яА-Я]" --include="*.dart"` is detector and check. Translate, do not delete. Original text: open, MEASURED (round 303) — the root `CLAUDE.md` says "English for code, comments, and logs"; **25 lib files are in Russian**, and only the three http2 ones are inside the owner's five-package mandate. `rpc_data` holds 16 of them, in `models.dart`, the contract and the repository interfaces — the API surface its users read first, on a package that is PUBLISHED, so dartdoc renders it. One is `.g.dart`, so that one means fixing the generator's source. Not swept in 303 because the mandate names five packages and these are none of them.
  **Curate after 327 re-took the count and the lead was measuring the wrong half.** `lib/` is 23 files today. `test/` and `example/` across core and transport hold **55 more** — more than twice the lead's whole number, never counted, and unlike the `lib/` ones they are squarely inside the owner's current core-and-transport scope. Rounds 325 and 326 read through several of them while raising the lint floor (`rpc_responder_endpoint_test.dart`, `responder_dispose_example.dart`, `http2_rpc_integration_test.dart`) and touched the code without touching the comments, because that was not the round's job. The in-scope half is the cheaper and larger one
- [+] **[B-29](B-29-the-isize-bomb-is-unbounded-on-web.md)** **decided (round 286+415): document the residual, change no behaviour.** The limit stays and keeps firing on `result.length`; what gets written down is the thing the API cannot show — **on web the bound is enforced AFTER the output is allocated**, so a hostile archive costs the memory and the event-loop time before it is refused. Both alternatives were declined and are recorded so neither returns as new: *fail closed on web* breaks every working deployment to stop a peer that must be hostile to get there, and *a lower web-only ceiling* shrinks the shape rather than removing it, by refusing large honest messages where the user cannot raise it back. Goes in the compression package's public docs, next to the limit it qualifies. Original text: open, MEASURED (round 286) — a gzip payload whose ISIZE understates it is refused in **12 ms on the VM and 15980 ms on dart2js**, because `boundedInflate` is `=> null` there and the limit runs on `result.length` after the output exists. ~65 KiB of wire buys 64 MiB and sixteen seconds of the event loop. **No fix is worth proposing** — a compressed-size heuristic would refuse ordinary traffic at deflate's 1032:1 ceiling, and `package:archive` has no incremental inflater on web — so what remains is documenting the residual or accepting a lower effective limit there

## Not numbered leads

- **websocket's `pingInterval` is still opt-in.** Not a numbered lead, because round 287 measured the http2 half and named this one rather than changing it blind; the websocket server's own doc carries the same shape (`pingInterval 3s : endpoints 0, contracts disposed 5 by t+10s`). The argument that closed B-27 transfers, and the change is one default on a different API surface

## Newer leads

- [ ] **[B-63](B-63-one-mechanic-many-homes.md)** open, cost (backlog review, commit `406f6536`) — **the largest item and item (1) closed by round 416.** `'Transport is closed'` is a TYPE now (`RpcClosedException`, FAILED_PRECONDITION, with a `what` field), `_isTransportClosed` is one `is` check, and the five `'Endpoint is closed'` guards use it too. The lead predicted a silent failure when the wording moved; it had ALREADY happened — `channel_transport.dart:437` threw a status where ten siblings threw `StateError`, and the matcher had been widened to accept both spellings rather than the drift removed. Item (1)'s stale `_ensureUsable` justification is rewritten (the status was right, the reason round 414 falsified is gone). **Still open: `_inFlightCalls()`, `_notify`, `_startKeepalive`, and the `wireStatusFor` → `sendError` quartet.** Original text: — the GENERAL case the owner asked for after ranking B-56: not the subscription discipline but anything that could be one thing the others use. **READ, not measured.** The bar is not similarity — RPC-25 declines cosmetic unification — so each of the six qualifies by having already DRIFTED or by failing SILENTLY. **The largest is `'Transport is closed'`: eleven production sites across six files and FOUR packages, consumed by literal string comparison** in `base_processor.dart`'s private `_isTransportClosed`. Change the wording at one producer and that transport alone stops matching, so an ordinary shutdown starts logging as a real failure — and the code already admits the coupling in prose, with `rpc_http_caller_transport.dart:579` saying it throws the message "matching every sibling" and `channel_transport.dart:430` explaining its spelling by pointing at a private function in another library. Two files describing a contract that has no declaration. **Sixth**: `wireStatusFor` → `sendError` written out in all four responders, one of which the code's own comment records as already repaired for being the odd one out. **(1) `_ensureUsable` has already produced a stale claim in two files at once**: both copies justify `FAILED_PRECONDITION` over UNAVAILABLE on the grounds that *"`RpcRetryInterceptor` retries that and never calls `reconnect()`"* — which round 414 made false, and the round that changed the fact updated neither home. That is the lead's whole argument arriving unprompted. **(2) `_inFlightCalls()`**, byte-identical in the http2 and websocket servers, takes its count from a stringly-typed map — rename `'activeResponders'` and both return 0, so every graceful drain completes INSTANTLY with no error; the failure looks like success. **(3) `_notify`**, byte-identical in both servers, is a process-death guard for user callbacks on detached paths. **(4) `_startKeepalive`** in the same package twice, already drifted by an `_isClosed` check the server half lacks. **Negatives worth as much**: `drainUntilIdle`, `BackoffPolicy`, `wireStatusFor` and the metadata policy check are ALREADY shared — and `drainUntilIdle` is the direct precedent for (2), since the loop was extracted and the count feeding it was not

**B-64 to B-70 are one intake, from a second duplication sweep the owner handed
in at `ff930001`** — 36 items, READ, nothing executed, and the pass that wrote
it flagged half as not double-checked. Six items were re-read against the code
before filing and carry their evidence (**9 of the 36 items**); items 15, 16 and
half of 32 were already B-63's and are not re-filed; the remaining **24 are
parked unverified in B-70** rather than spread across numbers they have not
earned. B-70 carries the full 36-item routing table, so the split loses nothing. **The order below is damage class**, and
B-70 is deliberately last. Three of the six were stated wrongly by the sweep and
are filed corrected, which is the argument for verifying an intake before
ranking it: the policy limit in B-67 does take effect, the isolate claim in B-70
does not survive at all, and B-64's worst consequence went unnoticed by the pass
that found the duplication.

- [x] ~~**[B-65](B-65-a-cancelled-unary-handler-still-answers.md)**~~ **closed (round 415)** — suppress, and say so. `_callIsOver` is checked after `await _handler(...)` on BOTH branches of `UnaryResponder`, and `_dropLateResponse` answers CANCELLED with the token's reason rather than going silent — dropping the response outright left the caller waiting out its own deadline for a call the server had finished with. The question the lead said had to be decided first was decided by the majority that already existed: letting it through would have meant changing four shapes to match one. Ablation: `Expected: not 'done anyway' / Actual: 'done anyway'`, with the CONTROL and the GUARD green. Original text: — **the one behavioural defect in the intake.** `StreamProcessor` gates `send`, `sendError` and `finishSending` on `_isActive` (`base_processor.dart:682-715, 848-860`); `UnaryResponder` is not on that path and has no equivalent. Both handler branches (`unary/responder.dart:438` codec, `:585` zero-copy) go straight from `await _handler(request)` to a DATA frame and `grpc-status: 0` with nothing in between, and every cancellation check in the file sits BEFORE the handler runs (`:308, :357, :526`). The monitor at `:139-155` only cancels the inbound subscription. So for the entire duration of the handler — the only slow part of a call — a cancel, a deadline or an `endpoint.drain()` goes unobserved, and the answer goes out on a stream the caller abandoned. **Drain is the case that matters**: it exists so a rolling deploy can stop cleanly, and a stream reporting OK after the window is what the window is for. The finding is the ASYMMETRY, not the send: the identical handler is suppressed under any streaming shape and under zero-copy unary, and answers under codec unary. A round decides which position is right once, for all shapes
- [x] ~~**[B-64](B-64-four-callers-disagree-about-what-ends-a-call.md)**~~ **closed (rounds 415, 416)** — 415 took item 1 (the destroyed trailer message); **416 took the other five and the missing witnesses.** `RpcCallerTrailer` owns the caller-side trailer rule for all five shapes: the status is PARSED (so a peer spelling OK as `00` is a success everywhere), "OK with no payload" is INTERNAL everywhere (the peer completed the call and broke the contract — a retry reaches the same broken peer), and the STATUS completes the call on all three shapes, so client-streaming and the zero-copy unary path now HOLD the payload instead of completing on it. `RpcLongTimer.timeout` replaces the four bare `Future.timeout` deadline bounds — and its GUARD caught a real unhandled-error bug in that new helper before it shipped. The pre-flight guard covers all five shapes, in `clientStream`'s returned CLOSURE rather than beside the lazy builder. Witness `one_rule_for_every_caller_shape_test.dart` against a hand-built peer, because rpc_dart's own responder derives payload and status together and cannot produce these sequences. Original text: — **item 1 closed by round 415**; `fromTrailer` owns the precedence and all seven callers pass the header through empty and all, so no production site substitutes a placeholder any more. `ping.dart` needed a DIFFERENT edit from the one below — it does not call `fromTrailer` at all, so changing its literal would have made the message worse with nothing gained; converted to the factory instead. **Six of the seven sites have no end-to-end witness**, and `server/caller.dart:199` is proven uncovered: ablating it changed no test result. Still open: the `!= '0'` string comparison, UNAVAILABLE against INTERNAL for "OK with no payload", the completion rule's fourth copy, the four bare `Future.timeout` sites, and the three-of-five pre-flight. Original text: — "a trailer arrived, make an exception" written **seven times** (`unary/caller.dart:317`, `client/caller.dart:111,123`, `server/caller.dart:131` and `:184`, `bidirectional/caller.dart:123`, `caller_pipeline.dart:816`, `ping.dart:149`), and three of the seven have already drifted into different observable behaviour. **The worst is silent and the sweep did not see it**: `RpcStatusException.fromTrailer` falls back to the message inside `grpc-status-details-bin` only when the trailer message is EMPTY (`errors.dart:123`, `message.isNotEmpty ? message : status.message`). Two callers pass `''`; the other five substitute the literal `'Unknown error'`, which is non-empty, so that branch can never fire for them and `details:` is dropped with it. A peer that puts its detail in `grpc-status-details-bin` and omits `grpc-message` — legal, and what `google.rpc.Status` is for — is read correctly on unary and client-streaming and reaches the application as `Unknown error` on the other five. Two more: `client/caller.dart:111` compares the status as a STRING (`!= '0'`) where six parse an int, and "OK with no payload" is UNAVAILABLE on unary and INTERNAL on client-streaming — **retried and not retried, for the same wire event**. The completion rule itself is a fourth copy (status vs first payload), and the unary comment records the drift history in the code's own words. Plus: `RpcLongTimer` exists because a dart2js timer past ~24.8 days fires immediately, and four caller sites still use a bare `Future.timeout` (`unary/caller.dart:479,570`, `client/caller.dart:249`, `ping.dart:238`); the `Endpoint is closed` pre-flight covers `ping`, `unaryRequest` and `serverStream` and not `clientStream` or `bidirectionalStream`
- [x] ~~**[B-66](B-66-two-http-status-tables-one-retryability.md)**~~ open, **owner decision** (intake `ff930001`, re-read) — two HTTP-to-gRPC tables, six rows apart: `_httpStatusToGrpcCode` (`rpc_http_caller_transport.dart:59-98`) against `grpcStatusFromHttpStatus` (`rpc_http2_common.dart:433-440`). **One row changes behaviour rather than wording: 504.** `RpcRetryInterceptor` retries `unavailable` and `resourceExhausted` and nothing else — the http2 table's own doc comment says so — so a gateway timeout is **retried over HTTP/2 and final over HTTP/1.1**, same deployment, same proxy, same application code. Swapping transport silently swaps the retry policy. 400, 429, 500 and both defaults differ in the status the application sees. **Neither table is simply right**: http2's is the better-argued (anything unmapped is `unknown`, not `internal`, which makes HTTP/1.1's `>=400 -> invalidArgument` default wrong) and HTTP/1.1's is the more complete (413, 401/403 are gRPC's own gateway mappings, and `504 -> deadlineExceeded` is the more honest status — at the cost of making a proxy timeout non-retryable). So the rows have to be settled by the owner before one table can live in core beside `wireStatusFor`
- [x] ~~**[B-69](B-69-pair-builds-a-client-production-never-builds.md)**~~ open, cost (intake `ff930001`, re-read) — **a defect in the witness, which is the class the loop is least able to catch.** `fromChannel` picks the oversized-frame policy by side and argues it at length (`channel_transport.dart:207-213`): a server closes, because dart:io has already buffered the peak before this class sees a byte; a client must not, because killing the connection takes every other in-flight call with it, and RESOURCE_EXHAUSTED on that one RPC is gRPC's answer. **`RpcChannelTransport.pair()` makes no such choice** — it delegates to `RpcFrameMultiplexedChannel.pair()` (`:518-531`), which uses the plain constructor and so takes the default `closeOnOversizedFrame = true` (`:78`) on BOTH halves. The client returned by `pair()` therefore has the server's policy, and `pair()` is the frame-codec harness: every suite built on it believes it is testing a client and on this axis is testing a server. A change that broke the client's "fail the stream, keep the connection" behaviour would pass. Bounded surface — the flag reaches `_refusedFrameHeader` (`:197`) and `onMalformedMetadata` (`:409`), both the refuse-the-stream half, neither reachable from a `pair()` client. `memoryPair()` is unaffected. The fix is two lines; the work is re-reading what the existing `pair()` suites were actually asserting
- [x] ~~**[B-68](B-68-irpcserver-stop-cannot-express-a-drain.md)**~~ open, **owner decision** (intake `ff930001`, re-read) — `rpc_http2_server.dart:456` and `rpc_http_server.dart:275` implement `stop({Duration? drainTimeout})`; the interface they satisfy declares `Future<void> stop()` (`rpc_server_interface.dart:37`). `RpcApp` holds an `IRpcServer`, so the widened signature is unreachable and `rpc_app.dart:200` calls the hard stop. It compensates by draining itself first (`:176` → `ep.drain(timeout:)` at `:428`) and gets the order wrong twice: **nothing has stopped the listener**, so a connection arriving mid-window gets an already-draining endpoint; and `endpoint.drain()` is the heavier operation that CANCELS contexts — the websocket server documents exactly that at `rpc_websocket_server.dart:215` as the reason it calls `markDraining()` instead, and `RpcApp` calls the one the comment warns against. **`markDraining()` has one caller in the repo** (`:219`); `responder_pipeline.dart:489` is the real implementation and `base_endpoint.dart:36` a no-op base. So "stop admitting, let what is in flight finish" exists, is correct, and is reachable from one of three servers. **Distinct from B-63 #2/#3** — those are byte-identical copies that agree, this is a capability that is missing. Waits because `IRpcServer` is public and every external implementor implements `stop()`
- [x] ~~**[B-67](B-67-three-method-path-limits.md)**~~ open, cost (intake `ff930001`, re-read) — `/Service/Method` is length-checked three times with three numbers that reference nothing: **512 hardcoded** (`responder_pipeline.dart:2085`), **`maxMethodPathLength`, default 1024** (`security_policy.dart:331-338`), and **258** (`metadata.dart:414-420`, `_maxMethodTokenLength * 2 + 2`). **The sweep's claim that the policy knob has no effect is wrong and is corrected in the lead** so nobody re-derives it — it is enforced by every transport that validates inbound metadata (seven call sites through `validateMetadata`). What is true is narrower and still a defect: **the knob is monotone downward only.** Raise it past 512 and nothing changes, because `_parseMethodPath` refuses afterwards and returns `null`; lower it and it bites. A setting that silently ignores half its range. The 258 is a third answer on the OUTBOUND side (`forClientRequestWithPath`), so a client built through that constructor can never send a path the policy's own default describes. The three also disagree on the GRAMMAR — exactly-three-parts plus a token regex, versus non-empty-and-leading-slash, versus a third per-token rule — which means **the layer the embedder can configure is the loosest and the strictest has no configuration at all.** What a round settles is which layer owns the grammar
- [ ] **[B-70](B-70-the-unverified-half-of-the-duplication-sweep.md)** open, cost (intake `ff930001`, **all 24 re-read**) — **four closed by round 415** (9, 22, 28, 30), **19 remain**. Item 9's fix made a LATENT `RpcCallScope.close()` race reachable — the scope self-closes on cancellation, so the teardown's own `close()` became the second call and the early return left the disposer loop running detached; `close()` now joins. **34** (a web worker silently running at the default security policy) and **23** (one `reconnect()` on a `viaSocket` transport destroying a working connection) are the two worth taking next. Original text: — the sweep's tail, kept under one number because none of it justified its own; **nothing here has a probe — READ is still all it is.** Five of the confirmed are defects on their face and would each stand alone. **(30 + 28) the CORS header lists are a stale copy of what core sends**: `caller_pipeline.dart:176,330` put `x-route-service` on every call and every ping, and `_requiredGrpcAllowedHeaders` (`rpc_http_cors_policy.dart:20`) is only `grpc-timeout, grpc-encoding, grpc-accept-encoding` — so a cross-origin browser call sends a header the preflight refuses, with nothing on the server side looking wrong; the exposed list omits `grpc-status-details-bin`, which `forTrailer` writes, **compounding B-64 #1 — two independent paths dropping the same field.** **(22) the http2 responder awaits a bare `_connection.finish()`** (`:1114`) where the caller bounds it with a timeout and a `terminate()` fallback (`:1943`) above a comment saying that call throws from package:http2 **into the root zone** — same process-death class as B-63 #3. **(9) `closeResponderResources` (`responder_pipeline.dart:452-465`) omits the token cancel** that four sibling teardowns perform (`:441, :523, :1114, :1975`), so `endpoint.close()` never tells a polling handler to stop. Then 13 and 36, both the same shape as B-67 — `RpcContext`'s four private header constants are numerically equal to the policy defaults and wired to nothing, truncating on a silent `break` (`contracts/context.dart:11-14, :238`), and every policy default is written twice, in the constructor and in `fromMap`. **(34) a web worker silently runs at the DEFAULT security policy**: the VM ships the spawner's with `policy.toMap()` as `args[4]` and rebuilds it via `fromMap` (`isolate_transport.dart:318, :230`), while `runRpcIsolateManagerWorker` takes `RpcSecurityPolicy policy = const RpcSecurityPolicy()` as a **default parameter of the worker-side function** (`isolate_transport_web.dart:429-431`) and `spawn()` applies the caller's only to the host transport — so a raised limit holds on one side of the boundary and not the other, with no error anywhere. **(23) one `reconnect()` on a `viaSocket` transport destroys a working connection to learn a fact fixed at construction**: it cancels every subscription, disposes every pump and clears six per-stream maps (`:1735-1750`) and only THEN calls a factory that is literally `() => throw StateError('does not support reconnect')` (`:399`). Then 13, 36 and **10** — the same shape as B-67: `RpcContext`'s four private header constants equal the policy defaults and are wired to nothing, truncating on a silent `break`; every policy default is written twice; and `_setupDeadlineMonitoring` exists once, in `CallProcessor` (`base_processor.dart:1331`), while `StreamProcessor` (`:188`) has no deadline disposer at all — its own doc says why that matters ("a bare close is indistinguishable from the server having finished"). Also confirmed: 5, 6 (**both cancel orderings carry a comment justifying themselves, and one describes the other's bug**), 7 (**eleven** case-sensitive `identity` comparisons against a registry that normalises), 11, 14, 17, 18 (**the http caller has no `maxActiveStreams` check at all**), 19 (http2's HEADERS path), 20, 21, 24, 26, 29, 31, 36. **Six of the sweep's claims died on the re-read and are recorded so nobody re-derives them**: 25 is STALE (round 413 made `RpcHttp2StreamError` extend `RpcStatusException`; B-62 closed it), 34's `methodPath` headline is WRONG, 23's drainSignal-through-a-proxy is WRONG (the same closure passes it), 24's counts were 5 and 3 and are 3 and 4, 7's count is 11 not ~12, and 19's HTTP/1.1 half is unproven. **14 is already one of B-56's nine**, and **17 is adjacent to B-21 but not covered by it** — B-21 is about the capability TYPE, not about three reconnect machines answering id-reuse three different ways. **18 should check RPC-05 and C-29 first.** The one thing still unchecked in this lead is the sweep's own already-shared list

- [x] ~~**[B-62](B-62-the-envelope-nobody-unwraps.md)**~~ **closed (round 413)** — **a third option neither of the two framings below describes, and it needed no measurement.** This lead weighed "restore `filterStreamEvents`" against "drop the envelope" and called it even, because both are about ROUTING. Round 412 then gave every library error the status that fits, and the envelope — a plain class — started destroying all of it: `wireStatusFor` is default-deny, so a wrapped RESOURCE_EXHAUSTED reached the peer as INTERNAL "Internal server error". It now `extends RpcStatusException`, deriving its status THROUGH `wireStatusFor(error)` rather than copying the inner's fields, which is what keeps a FOREIGN inner redacted — copying would have opened the deny for everyone else's errors. `streamId` and `error` untouched, so routing is unchanged; four tests, two of them guards. What remains is only the missing call site, and it now costs precision rather than correctness. Original text: — `RpcHttp2StreamError` is not an error but an ENVELOPE, whose doc says `filterStreamEvents` unwraps it and re-throws the inner error on the matching stream. **`filterStreamEvents` has zero production call sites** — its only references are its own declaration and doc, a CHANGELOG line, and five uses in one test against a hand-made `StreamController` — against TWO production sites that create the envelope. So `wireStatusFor`, which is default-deny, sees a non-`RpcException` and redacts: whatever the inner error was, the peer gets INTERNAL "Internal server error". The envelope hides its contents from the one function whose job is to decide what may be forwarded. Per-stream routing does not need it — both transports already `_streams.addError(streamId, error, stackTrace)` with the raw inner error beside the enveloped broadcast add. **Two opposite fixes and no number to choose between them**: wire `filterStreamEvents` in and restore the design, or drop the envelope so the broadcast carries a classifiable error — the latter tempting until you notice connection-level fatal errors are added UNENVELOPED on purpose, which is exactly the distinction the envelope draws. The deciding measurement: what a caller is told for a stream-level transport error that is NOT a framing violation, since the framing path answers separately (round 397) and masks this one
- [x] ~~**[B-61](B-61-http2-never-notices-it-is-disconnected.md)**~~ **closed (round 414)** — the owner chose the option that overturns neither transport: `RpcRetryInterceptor` now calls `reconnect()` before an UNAVAILABLE retry, so a dead connection is re-established instead of every attempt hitting the same one. **The test that failed was measuring the hollow version**: `a drained connection is retried as the retry doc promises` went `3 -> 2` attempts, because its `drain()` GOAWAYs one connection and leaves the listener up — so the retry now RECOVERS on attempt 2. Counting three failures was a proxy for "a retry happened", meaningful only while no attempt could pass; re-pointed at the stronger property. UNAVAILABLE only: RESOURCE_EXHAUSTED means the peer is overloaded and its connection is fine. Original text: — the only option that overturns neither transport's tested decision: UNAVAILABLE becomes honest on a bare transport, so http2's *"a drained connection is retried as the retry doc promises"* starts meaning what it says, and websocket's non-retryable refusal for the reconnect WINDOW is untouched. — **the TYPE is unified and shipped**: both `_ensureUsable` implementations throw `RpcStatusException(failedPrecondition)`, so one `catch` covers every transport, and that code is not retried by `RpcRetryInterceptor` so the earlier round's argument survives. **The BEHAVIOUR is not, and cannot be without overturning a tested decision.** Making the http2 guard fire broke *"a drained connection is retried as the retry doc promises"* and *"GUARD: a genuinely dead connection still reports UNAVAILABLE and down"* — the latter being exactly the ending this lead named, and requiring `createStream()` to hand out an id on a dead connection. So http2 does not fail to notice; it answers UNAVAILABLE-and-retryable on purpose while websocket refuses non-retryably on purpose. The real question is which retry semantics win, and the only option that picks no side is teaching `RpcRetryInterceptor` to call `reconnect()` — a core change, not a transport one. Original text: — `RpcHttp2CallerTransport._ensureUsable` exists to "refuse work the transport genuinely cannot do, naming which state it is in", and with the server gone it never fires. Both transports gated on their own `health()` first, so neither arm can be accused of racing a dying socket: `websocket degraded / createStream() StateError / unary StateError` against `http2 degraded / createStream() **no throw** / unary RpcStatusException`, `isClosed false` on both. **Both reached the state, only one guard fires.** `_disconnected` has exactly two writers on http2 — the KEEPALIVE failure path and the catch inside `reconnect()` — and `pingInterval` is opt-in, so the ordinary ending (server goes away, nobody pinging) reaches neither and the flag stays false forever; `createStream()` hands out ids on a dead connection. The websocket sibling sets it from its channel's `onDone`. Filed rather than fixed because the fix must pick which sibling is right and **the websocket side's choice was argued for** — its test header records that a synthetic UNAVAILABLE "is RETRYABLE, so the caller is invited to try the thing that cannot work", and `RpcRetryInterceptor` retries only UNAVAILABLE/RESOURCE_EXHAUSTED while never calling `reconnect()`, so a retry spins. The owner has now called for unification; the remaining question is which type, and it is asked in the lead
- [x] ~~**[B-60](B-60-the-websocket-drain-admits-everything.md)**~~ **closed (round 414)** — `markDraining()` added and called from the websocket drain: **1347 calls admitted after shutdown began became 3**, with 472 refused instead of 3, matching http2's `4 / 540`. Those refusals are the other half of the reading — the calls are ANSWERED UNAVAILABLE rather than dropped. **Where the method lives was not where it first went**: the mixin does not compile at the call site, because `_endpoints` is `List<RpcEndpointBase>` deliberately (peer endpoints serve calls too) and `RpcResponderPipelineMixin` is hidden from the public surface by round 291's measured RPC-24 work. So it is declared on the base as a no-op and overridden in the mixin — the shape `collectEndpointMetrics()` already uses. Original text: — `drain()` keeps both halves for callers that want them; the new method sets only the reject-new-streams flag, which is the GOAWAY websocket has no protocol equivalent for. — `RpcWebSocketServer.stop(drainTimeout:)` stops accepting new CONNECTIONS and then only WAITS: it never tells an already-connected peer to stop opening streams, so it does not drain. With eight parked server-streams holding the count above zero so both transports must spend the full budget and admission is the only variable: `websocket 3006 ms / **1347 served after shutdown began** / 3 refused` against `http2 3016 ms / **4** / 514 refused` — **337x**. **The damage is not the waiting**: the endpoints close when the budget expires, so a call admitted at 2.9 s into a 3 s drain is killed at 3.0 s, and the calls most likely to be cut mid-flight are exactly the ones the server accepted after deciding to shut down. A rolling deploy reaches it every time. **The mechanism exists one layer down and neither server uses it**: `responder_pipeline.dart:592` answers a new stream UNAVAILABLE while `_respIsDraining` is set and lets existing ones finish — the rpc-level GOAWAY. Filed rather than fixed because the only public route to that flag is `RpcEndpointBase.drain()`, which ALSO cancels every active context with reason `server draining` — the opposite of what `stop(drainTimeout:)` promises. **Owner decision**: should `RpcEndpointBase` expose "reject new streams" separately from "cancel the active ones"? Every graceful drain wants the first without the second, and today only `rpc_app.dart` calls `drain()` at all while both servers hand-rolled their own polling loop
- [+] **[B-59](B-59-a-peer-arriving-while-stopped-is-abandoned.md)** **decided by owner** — a REFUSING state, the third state the record says the two halves cannot be had without: `stop()` does not cancel `_connectionsSub`, it switches to closing what arrives, and the subscription is released only at final teardown so `start()` has one to switch back. Available because the record's table conflates two things — what makes the stream unrestartable is CANCELLING, not holding, and what the load-bearing comment protects against is a connection landing in `_endpoints` after the clear, which a refusing mode never lets happen. **Proposed by the agent in review, not established by a round**: the carrying round must measure that claim first. Original text (round 401) — `RpcWebSocketServer.stop()` cancels its connections subscription, but the `HttpServer` underneath is not the server's to close, so it keeps accepting and `rpcWebSocketConnections` keeps upgrading — and on the broadcast stream the class's own error message recommends for restartability, an event with no listener is DROPPED. A peer arriving in the gap gets `handshake accepted / closed 3 s later NOTHING / a call over it HUNG / after restart served`: no error, no close frame, no status, waiting on its own deadline, and `connectTimeout` cannot help because the CONNECT succeeded. Reached by an ordinary rolling restart. **Filed because every fix is a trade**: the only shape that answers the peer is "keep consuming after stop, close what arrives", which inverts the `_connectionsSub?.cancel()` that `stop()`'s own comment calls load-bearing (*"a connection arriving after the endpoints are closed still gets handled, and lands in `_endpoints` AFTER the clear below"*) and costs restartability over a single-subscription stream for good. Closing the HttpServer is not available — the server does not own it. Round 401 changed the error message to say the gap exists, which is honest and is not a fix. Baseline pinned in `restart_needs_a_new_stream_test.dart`, whose expectation carries its own inversion instruction
- [+] **[B-58](B-58-a-framing-violation-counts-toward-nothing.md)** **decided by owner** — yes: split the branches and give the malformed-framing one exactly what its header-level sibling has — the 256 backstop and `closeOnProtocolError`. A frame the transport cannot decode IS the protocol violation that field names. The resource-limit branch must never count: that peer is misconfigured rather than broken, and RESOURCE_EXHAUSTED is what invites it to correct itself. The knob defaults to `false`, so nobody who did not opt in sees a change. **Round 399's cost numbers are not what this turns on** — the backstop was never a DoS defence here. Original text (round 397, **cost half answered in 399**) — **the measurement argues AGAINST the backstop.** P-85, 2000 operations each on one connection through a byte-counting relay: the server spends **548 ms on 2000 refusals against 873 ms on 2000 honest calls**, so grinding refused frames does less damage than simply calling, which nothing bounds either. `:method GET` — the sibling site that HAS the backstop — closed the connection after the 256th violation (300 attempted), which is exactly the contrast this lead asked for and exactly what nothing now shows a need for. The one axis where the refusal is worse is **amplification, 1.61x, the only path here writing more than it reads** — ablated by `maxHeaderValueBytes: 24` at the same site (`grpc-status 8`), **216 B down becomes 131 and it goes**, so the cause is the parser's diagnostic text, which is the thing that lets a client fix its own request. Recorded, not changed. **What remains is not measurable**: should `closeOnProtocolError` fire for malformed framing (the `RpcStatus.internal` branch), as opposed to the resource-limit one? A question about what that field means. Original text: the two refusal sites in the http2 responder account for a refusal differently: `_answerRejectedStream` increments `_policyViolations` against the 256 backstop and honours `closeOnProtocolError`, `_answerFramingViolation` does neither. Round 397 fixed the RELEASE half of that asymmetry and left this one deliberately, because the site refuses two different kinds of event under one catch: an `RpcException` is a resource limit the sender can correct and retry (a client with a larger send limit than the server's receive limit hits it every call, and ending its connection every 256 is a different contract from the one the field documents), while anything else is malformed framing and arguably should count. So the fix is not "add the counter" but "split the branches", and whether `closeOnProtocolError` covers framing at all is what that knob MEANS. No number yet on the cost side either — P-84's arm is five bytes per stream, and the question here is one stream and N refused frames, now that each refusal also tears the call down
- [+] **[B-57](B-57-every-unary-handler-subscribes-to-the-connection.md)** **decided (round 415): spend a round looking for a THIRD route** — one that removes the per-handler subscription while keeping an answer path for a transport error that does not close the stream. Both known routes are closed, which is what makes this a search rather than an attempt: keeping the subscription leaves the fan-out O(N), and `getMessagesForStream(id)` is REFUTED below. **Producing that transport state is a legitimate way to FINISH the round, not to fail it** — nobody has made an error of that shape yet, so what the current `onError` saves is unmeasured, and the number decides. Do not ship a removal resting on the assumption alone. Original text: **the COST half fixed in round 393** (48/126/333 ms → 38/42/94 for 3000 frames at 1/50/200 parked handlers, P-82) by making the listener synchronous with the id filter first; plus the `IRpcAdvisoryChannelError` check, which is U1's second half. **The fix direction this lead recorded was WRONG and is struck out**: `getMessagesForStream(id)` creates a per-stream controller, and `_admitToStreamBuffer` can refuse a frame and `return` *before* the unconditional `_incoming.add` — so a large unary request would be dropped from the pipeline too, by a bound that does not apply to unary today. **Still open: the fan-out COUNT** — 200 handlers still mean 200 listener invocations per frame; removing that needs the subscription itself to go, and the only per-stream route is the refuted one. Original text (round 392, the owner's U1) — `UnaryResponder` subscribes to the **whole connection broadcast** and then filters by stream id, while the pipeline builds it with `id: streamId` and feeds the request itself via `handleMessage` — so for every pipeline responder the subscription delivers NOTHING and costs one `async` callback, one Future and one microtask **per inbound frame per live unary handler**. O(N) per frame. Every code fact confirmed by reading. **Not fixed because dropping it also drops its `onError`**, which answers waiting callers with a trailer, and the pipeline's own `onError` (`responder_pipeline.dart:403`) **logs only** — only `onDone` aborts streams. So the loss is real for a transport error that does not close the stream, and that state has not been produced. **A better fix than the one proposed**: subscribe to `getMessagesForStream(id)` — per-stream kills the fan-out, and `channel_transport.dart:182` already withholds ADVISORY errors from per-stream controllers, which fixes U1's second half (an advisory error wrongly failing a unary call) at the same time
- [x] ~~**[B-55](B-55-the-response-sink-swallows-its-source-error.md)**~~ **closed (round 389)** — measured, and the title was wrong: the client is not told OK, it is told **nothing for ever** (`2 payloads, NEVER ENDED`). `addStream` does not close its target controller, so a failing source hit an `onError` that only logged and the controller stayed open — `onDone` never ran, `finishReceiving()` was never called, no trailer went out. Fixed the way `ServerStreamResponder` already ends a failed handler: `wireStatusFor(error)` then `sendError(...)`. **And a witness lesson**: the first version asserted only `isNot(contains('ended OK'))` and PASSED on the unfixed tree, because "never ended" is also not "ended OK". Original text: round 384's defect MIRRORED onto the server: `BidirectionalStreamResponder.responseSink`'s `onError` logs and returns, so the source's `onDone` follows with `finishReceiving()` and the client is told a clean **OK** for a handler whose own source failed part-way. The caller's sibling now aborts and closes on this path; the endpoint pipeline relays handler errors into a trailer; `responseSink` is the one copy still swallowing, and it is public. **Established by READING and not measured** — the probe built for it hangs before its first arm reports, so the rig needs diagnosing before the fix does. `payloadResponses`' unreachable grpc-status branch rides along (`responses` is already piped through `_grpcStatusErrorTransformer`)
- [x] ~~**[B-54](B-54-the-producer-runs-on-after-the-call-ends.md)**~~ **closed (round 390)** — the signal was already there and nothing exposed it: **every ending closes `_responseController`**, so its own `done` future covers all of them. `CallProcessor` now exposes `Future<void> get done` and `requestSink` cancels on it; **11 → 32 became 2 → 2**. Which is why both earlier candidates failed — they asked the wrong object (`send()` never throws, and `isActive` describes the processor rather than the call). Original text: when the server ends a bidi call, the caller's `requestSink` keeps pulling: **11 messages produced at the end, 32 a quarter-second later**, each one caught, logged at ERROR and resumed. An endless producer is an endless error log. `ClientStreamCaller.call(Stream)` races `Future.any([drained, _responseCompleter])` and the endpoint bridge owns both subscriptions; `requestSink` is the third copy and the only one that cannot see the end. **Two candidates written and refuted by measurement, recorded so nobody pays twice**: stop-on-send-failure never fires (C-35 — `send()` does not throw), and gating on `_processor.isActive` never fires either (it stays true after a normal end). The real signal is the response stream completing, which the CONSUMER owns — so this wants a `done` future on `CallProcessor`, the way the responder already has one
- [x] ~~**[B-53](B-53-an-http2-reset-racing-responses-kills-the-connection.md)**~~ **closed (round 398)** — **`http2: ^3.1.0` shipped the fix.** Its changelog's first line, *"gracefully handle receiving headers on a stream that the client has canceled (#1799)"*, is the mechanism rounds 385 and 387 spent themselves narrowing to: the SERVER writing a response to a stream the client has just reset. P-73 reused unchanged with the resolved version as the only variable — **`abort racing responses` 5 of 5 DEAD on 2.3.1, 5 of 5 pong on 3.1.0, both the awaited and unawaited arms**, and the six already-clean arms untouched. Neither behaviour trade the owner was asked to decide had to be made, and neither upstream route (a stream-state getter, an ordered reset) was needed. Guarded by `abort_racing_responses_keeps_the_connection_test.dart`, which fails on 2.3.1; `request_sink_error_over_http2_test.dart` unskipped, rpc_dart_http2 **+230 with no skips**. **The round nearly missed it and that is L-17**: the skipped witness was run ALONE — the one condition this lead's own text says it passes under — five green runs across two versions and a stated conclusion that the upgrade changed nothing, until the owner asked *maybe the test is wrong?*. The deterministic answer was in the lead's own `probe:` field. Original text: **the ordinary half FIXED in round 388** (round 384, widened in 385, trigger proved in 387). `package:http2` already sends the reset itself when our incoming subscription is cancelled on a `HalfClosedLocal` stream (`stream_handler.dart:332`), through the stream's ORDERED outgoing queue; `terminate()` wrote a second one directly and the server answers a double reset with GOAWAY `errorCode: 1` (PROTOCOL_ERROR). The guard is the one `releaseStreamId` has used all along, 130 lines up. `50 ms / let go at the last payload` **DEAD from call 2 → 5 of 5 clean**, and http2's duplex column is clean on both links where 385 read eight MISMATCHes. **What remains**: a stream that was never half-closed — the producer ERRORS — where the dependency sends nothing and our RST_STREAM is the only signal that stops the handler; mid-response it still costs the connection. Needs a stream-state getter from package:http2, or an ordered reset for a not-half-closed stream. Original text: on HTTP/2 a client-side stream reset that RACES responses still in flight destroys the whole connection — **round 385 made this much worse and answered its first question.** The trigger needs no `abort()` and no erroring sink: an ordinary consumer that stops reading once it has the messages it wanted (`.take(n)`, a `break`, a UI closing a subscription) cancels while the trailer is still on the wire, and over a link with any round trip that costs the whole connection — `direct/early 5 of 5 clean`, `50ms/early DEAD from call 2`, `50ms/late 5 of 5 clean` (P-78). **The SERVER closes it**, per the relay's attribution, and **rpc_dart logs nothing**: the direct arm prints five handled `peer sent RST_STREAM (errorCode: 8)` and survives, the latent arm prints nothing and dies — so the cause is inside `package:http2`, B-12's address. A candidate (the outgoing pump's unhandled `addStream` future) was tried and refuted. Original text: on HTTP/2 a client-side stream reset that RACES responses still in flight destroys the whole connection: every other call on it then fails `RpcStatusException(14) ... no longer active`. **Reachable from the public `abort()` with none of round 384's code** — P-73's eight-arm matrix eliminates the await and the sink path, and what every dead arm shares is an answering handler and no settle. It matters now because 384's fix for an unbounded server-side leak makes that sequence automatic when a request sink errors: adding `close()` after the notice (what `ClientStreamCaller` does) narrows the window enough to pass alone and **not** under load, so the http2 witness is `@Skip`ped naming this. The trade is the owner's, as in B-12: *a permanent leak on a live connection* versus *no leak on a connection that can still die*. First step is not a fix but an observation — which SIDE closes it; `releaseStreamId` already carries a comment about `package:http2` turning a frame on a half-closed stream into a CONNECTION error thrown asynchronously, which no try/catch here can see. C-32 is not a counter-example: its 200 resets landed before dispatch, so the server never wrote
- [x] ~~**[B-52](B-52-the-concurrent-reconnect-witness-was-deleted.md)**~~ **closed (round 415)** — re-asked and answered the same way: **the fix stays unguarded.** Both the two-line L-11 repair and a simpler unit-level alternative were offered a second time and declined, so this is now a recorded, ACCEPTED gap rather than an open question — the defect is real and fixed, the fix is in the code, and a future change to `_connection` assignment will not be caught. The distinction worth keeping: it failed for being written against a BUDGET instead of an EVENT, not for the thing it guarded. Original text: open, owner decision (2026-09-17) — `concurrent_reconnect_test.dart` was deleted at the owner's instruction after failing in the workspace run. It guarded a real, already-fixed defect: two interleaving reconnects both assigning `_connection`, so the second overwrote a live connection nothing could then close (`opened=3 closed=2 live=1`, the sibling of `473789b9`). **The fix is still in the code and is now unwatched.** Why it failed is not why it went: isolated it passes in 7 s, and under parallel load it hit `still live after 30001ms` — the full budget, which its own message calls the slow-teardown reading rather than the leak one. It waited for a BUDGET instead of the close EVENT (L-11), fixable in about two lines; that alternative was offered and declined. Precedent worth knowing before a third: `1f448150` took another test off the gate for a neighbouring 1.3% defect
- [x] ~~**[B-51](B-51-the-request-direction-of-the-bidi-pump.md)**~~ open, bench (filed from the consumer after 378) — **the one bidi direction four rounds did not measure is the one that carries bulk.** 374 fixed the response relay of `_pumpBidirectionalResponses` and named this in its own *Not fixed*; 377 still lists it. The request side reaches the handler through `_pipelineFedRequestStream`, a different mechanism with its own `deferFlowCredit` accounting. Three sibling mechanisms in the same file family were each found unbounded (P-61 `requestSink` 2000/31.3 MB, P-62 `responseSink`, P-65 the response relay 2000/31.3 MB against a 1 MB window) — a reason to measure, not a measurement. **Consumer stake:** rhyolite's blob upload is now a bidi contract handler taking 256 KiB frames into object storage, so roughly **8 MB in against 400 bytes out per call** — the direction proven bounded four times is the cheap one. If the request relay buffers like its siblings, an ordinary slow object-store write lets the pipeline pull a client's whole batch into server memory. Close it with P-65's rig pointed the other way, control = the client-stream shape whose pause is the value the other rounds landed on, ablation through toxiproxy per 378
- [x] ~~**[B-50](B-50-a-client-stream-cannot-report-a-short-read.md)**~~ **closed (round 375)** — decided by consumer evidence the owner attached: rhyolite moved the upload this record was written about off client-stream onto **bidirectional**, acking each blob on the response stream as it is durably written. **The trailer count is rejected because it answers the wrong question** — a count at the end reports what the library HANDED the handler, while resumption needs WHICH pieces are durable, and a handler can be given 17, commit 3 and fail. No cheaper mechanism exists (the caller knows what it sent, only the peer knows what was read), so the behaviour stands and round 375 makes sure nobody arrives at it by accident: `addClientStreamMethod` now states the measured numbers, says the early answer is deliberate, and routes resumable transfers to `addBidirectionalMethod` with a per-message ack. The `droppedRequests` half closed by measurement — **four paths across two rounds, none arrived**, and the comment claiming the pipeline reads it now says what is true. *(Original text below.)* A client-stream handler that stops reading answers `OK`, **byte-identical to a full read**: measured at **17 sent / 3 given to the handler / caller told OK**, against a `fullRead` control at 17/17/OK and a `throws` control that does return `RpcStatusException`. No log line on either path — the later frames die as trailing frames of a torn-down stream. **Server-stream does not have it.** The owner's use is resumable upload of CDC-chunked files, so a caller told OK over 3 of 17 records the file as stored — round 366's failure class from the other direction. The design, argued in the record: *resumption is the application's and its place already exists* — the library knows what it HANDED OVER, never what the handler COMMITTED (17 given, 3 written, then a crash), so a library-side "accepted" count would look authoritative and not be, and `nextOffset` belongs in the response message; *honesty of the status is the library's and is the missing half*, but an ERROR is the wrong shape because an early answer is USEFUL mid-upload, so what is asked for is a SIGNAL (`x-rpc-requests-consumed` in the trailer) rather than a refusal. Second half, separable and needing no decision: **`droppedRequests` documents itself as read by the pipeline and has no reader** — declaration plus two increments, zero readers, and neither measured path reached it at all, so the promise is false and the counter may be dead
- [x] ~~**[B-49](B-49-request-sink-has-no-backpressure.md)**~~ **closed (round 370)** — the number this record refused to borrow, taken on the API in question (P-61): a stalled handler, a 1 MB window, 16 KiB messages, counted inside the application's own producer. **`requestSink` pulled 2000 of 2000 (31.3 MB) where the sibling pulled 66 (1.0 MB, exactly the window); 2000 -> 68 after.** 2000 was the probe's ceiling, not a plateau. The sibling's pause was copied, behind a `finished` flag so a resume cannot land on a cancelled subscription. Canary gives a number rather than a timeout: `Actual: <2000>`. Narrower than it sounds and said so: a direct `sink.add()` is still unbounded, because that is the `StreamSink` contract — the bound reaches `addStream` producers, which is what an upload uses
- [x] ~~**[B-48](B-48-the-isolate-chrome-arm-never-connects.md)**~~ closed (round 379) — **the file does not exist.** `rpc_dart_isolate/test/` holds no `web_smoke_test.dart` and the `test:web` script named it anyway; `dart test` answers a missing path by starting a browser that has nothing to connect to, so the failure surfaces as `BrowserManager._start` timing out — **exactly the cold-start flake the comment three lines above it describes**. Every retry and every raised timeout read as confirmation of the wrong theory, which is why it survived its own filing, round 377's work in the same package, and two deliberate re-runs at 3x and 6x. Both theories in the record were wrong: not Chrome (websocket's arm passes on the same machine) and not round 378's IPv4 trap. The "not established — does it pre-date round 368" is answered by the cause: the path was wrong for as long as it has been in the script. Path removed, `test:web` green end to end. Left open deliberately: no smoke test was WRITTEN for the package, because adding coverage is a different job from repairing a gate. *(Original text below.)* Open, reason "bench" (round 368) — `melos run test:web` is red on one arm: `rpc_dart_isolate`'s `-p chrome` smoke fails at `BrowserManager._start`, i.e. Chrome never connects to the test channel, **identically at `--timeout 3x` and `6x`** so it is not the cold start the script's own comment predicts, and before a line of Dart runs. Three causes ruled out: not Chrome in general (`rpc_dart_websocket`'s Chrome smoke, same machine, same locally-resolved core, **4 passed**), not the node arm (green inside melos; a bare `fvm dart test -p node` dies `ENETDOWN` on all 115 files only because the repo's own script is what sets `NODE_OPTIONS=--dns-result-order=ipv4first`), and not round 368's diff. **Not established: whether it pre-dates round 368** — `git stash` is forbidden and no earlier-tree control was taken. Closes by running the same arm on a tree without 368's commit; note it is the only package whose Chrome invocation compiles a Web Worker entry point first
- [x] ~~**[B-47](B-47-the-initial-send-window-is-smaller-than-a-message.md)**~~ open, owner decision (round 366) — `initialSendWindowBytes` defaults to **64 KiB**, arrived in 6.0.0, and a downstream consumer chunks blobs at **256 KiB** — four times the window. **The park buys nothing that can be named**: the gate admits on `credit > 0` rather than on whether the message fits, so a 256 KiB frame passes a 64 KiB window and drives the balance negative anyway; the data goes out regardless, one round trip later. What it does buy is a state that did not exist in 5.0.1 — a parked sender on the second frame of every stream, on any link with an RTT — and round 366 fixed the defect that lived in that state without removing the state. Measured in [P-58](../probes/P-58-does-a-sender-park-over-a-real-socket.md): 2, 3 and 8 frames all park ~1xRTT on 6.0.0 defaults and never on the 5.0.1 shape. Three candidate fixes are written out (raise the default above any plausible message; derive it from `maxMessageSize`; admit on "does it fit"), all three change a shipped default for every peer, and the config's standing requirement is to ASK before a trade of that kind
- [x] ~~**[B-44](B-44-client-stream-first-frame-loses-its-metadata.md)**~~ open, reason "bench" (2026-09-15) — **the first intake from a consumer's production incident rather than from a round, and the first pass eliminated four candidates without reproducing it.** On dart2js + browser WebSocket a client-stream message reaches the responder with its decoded fields NULL; 86 occurrences in 3.6 days on one consumer's two replicas, never on the VM. [P-51](../probes/P-51-browser-client-stream-delivery.md) now covers the gap the suite never had — real browser, real `WebSocketChannel.connect`, real dart:io server — and **36 calls across five arms, including four concurrent 256 KiB calls and ten cold connections, are clean**. So are the VM and dart2js-over-memory arms. Out too: the lenient send after close, which drops the bytes but fails the CALL, [C-38](../checked/C-38-the-lenient-send-is-not-a-lost-message.md); and `7a3c66d5`, whose commit message reads like the answer but covers a reconnect path this consumer does not use. **Next step is cheap and named**: pin both sides at their ref `55159adf` (26 commits behind the bench) and re-run P-51
- [x] ~~**[B-45](B-45-two-owners-start-the-same-endpoint.md)**~~ **closed (2026-09-15)** — `RpcWebSocketServer` calls `onEndpointCreated` and then `endpoint.start()`; the framework's callback registers the contracts and starts it too, so every connection warned `Already listening for incoming requests` — **704 and 698 in 24 h on two replicas, against 6 and 34 lines of real error**, the ratio that made a live incident take a `grep -v` to read. The redundant call is now `internal`; a second call asking for a DIFFERENT `messageFilter` still warns, because there the first filter silently stays in effect. Lifecycle untouched, so "who owns starting" stays open and now costs nothing. Four witnesses, both halves canaried, and the GUARD that matters passes on both sides: a redundant start still runs the handler exactly once
- [x] ~~**[B-46](B-46-an-empty-blob-id-is-silently-generated.md)**~~ **decided (round 415): refuse it.** `putBytes(id: '')` throws `ArgumentError`. The API already has a way to ask for a generated id and it is `null`; with `null` present, an empty string is not a second way to ask but a caller whose id-building produced nothing, being answered as though it had made a request. **The wire level does NOT change** — `BlobUploadChunk.blobId` is non-nullable, so empty-means-generate is the only encoding available there and stays deliberate. In scope via B-10's narrowing. Check `rpc_blob_sqlite` and `rpc_blob_webdav` while there, or it becomes B-33. Original text: open, MEASURED (2026-09-15) — `putBytes(id: '')` stores the blob under a GENERATED id and returns it, with no error: `asked="" got=18df18eedb93057e`. At the wire level empty-means-generate is deliberate (`BlobUploadChunk.blobId` is non-nullable), but `putBytes` already has `null` for that, so an empty string there is a broken caller being answered as if it were a request. For a content-addressed caller — which is what the blob packages are for — a substituted id reads as "not stored" while the bytes are orphaned under a name nothing references. **Not the consumer defect it was found looking for**: their ids are HMAC hex and their codec reads an absent id as null
- [x] ~~**[B-23](B-23-pre-201-knowledge-outside-the-journal.md)**~~ **closed (round 415)** — **the expensive half is done and the remaining half is a different bargain.** Discharged: two lenses in the curate after 234, six records plus RPC-08's owner qualifier, six more in 239, and the whole architecture group across 271-281 — which shipped `docs/transports/grpc-compat.md`, repaired `docs/guides/diagnostics.md` and merged the three-layer model into `architecture.md`. The `core_types` docs gap is closed too (`RpcPeerEndpoint` is in `core-concepts.md`). What remains is 1653 lines of dossier whose numbers predate the flow-control work, and under this lead's own rule — re-measure every claim or drop it — importing costs what measuring from scratch costs and buys a negative rather than a fix. They stay in memory as reference the loop does not route to; `MEMORY.md` now says so instead of calling itself a queue. Original text: open, reason "cost" (curate after 234) *(stale, aba26aa3 — 94 commits)* — 52,203 words of pre-201 knowledge sit outside the journal, where nothing routes to or ages them. NOT duplication: `checked/` imported the negatives, the SHAPES and METHODS stayed out. Two lenses recovered (RPC-16, RPC-17); ~30 dossiers left, and the four METHOD entries are the highest value
- [x] ~~**[B-26](B-26-timeout-bounds-the-wait-not-the-body-read.md)**~~ closed (round 273), **REFUTED** — the prediction was that `.timeout()` on `readBody()` leaves its `await for` consuming; it does not. 384 KiB accepted after the 408 against 16384 KiB in 49 ms with the deadline off, because dart:io detaches the body of a finished exchange. Wrong for a reason that is not in our code; kept as C-31
- [x] ~~**[B-25](B-25-sequential-reconnect-orphans-a-connection.md)**~~ closed (round 262), PARKED by the owner — **a connection leak, measured and unexplained.** A sequential http2 reconnect leaves a DISCARDED connection open about 1.3% of the time (5 in 390 direct cycles, always ordinal 1 or 2). Six rounds eliminated three accounts — socket.destroy() as the fix, the dropped terminate() Future, the header-block guard — without finding the cause. The one untried variable, a discard racing a concurrent connect, is written up with its three-arm design. Reopen when it costs something real
- [x] ~~**[B-24](B-24-frame-channel-buffer-is-an-ordering-coincidence.md)**~~ closed (round 250) — the frame channel's inbound controller now buffers like the other six. Shipped with NO canary on the owner's decision: no construction path loses a frame today, so a witness would have had to invent the very await whose absence makes the code safe
- [+] **[B-21](B-21-reconnectable-transport-type.md)** **decided (round 415): ship it with the major already accruing.** The lead's own condition — take it alongside whatever else breaks the signature — is met by `27d22cd1 feat(rpc_dart)!: make RpcException abstract`, so it rides along instead of spending a major on a diagnostic. And `8d81b9e7 fix(rpc_dart): forward transport capabilities through the reconnecting proxy` is this lead's own subject arriving as a RUNTIME repair, one member at a time, which is what the type makes impossible to get wrong. **Third step is the one that can go wrong**: re-check "fixes nothing currently broken", the claim the curate after 327 ranked this up for — `8d81b9e7` is evidence it already decayed. Round 224's runtime refusal stays. Original text: open, next major (round 224) *(stale, ed1a54bc — **12 commits, 8 of them `fix()`**)* — make the reconnect capability a compile-time requirement; moves round 224's runtime refusal to a red squiggle, and fixes nothing currently broken. **Ranked up by the curate after 327**: its paths (`client_connection.dart`, `transport.dart`) have taken more behavioural churn than any other lead's since its number, so "fixes nothing currently broken" is the claim most likely to have decayed
- [x] ~~**[B-11](B-11-endpoint-reachability-needs-latency.md)**~~ **closed (round 415)** — **the blocker DID lift and the answer is still no.** Round 247 confirmed no latency helper existed anywhere; P-78 now carries a 50 ms TCP relay and P-85 a byte-counting one, both with the trap already paid for (`unawaited(socket.done.catchError(…))` on both sockets, without which the PROBE dies instead of the library), so building the link is copying rather than inventing. What it buys has not risen with the cost falling: an extension to a defect round 206 already fixed, on a path three shapes could not make leak — and the producer-overrun arm read 0 BEFORE any cancels, because a saturated stream is already parked on its window at the moment of the pause. Original text: open, reason "bench" (round 206) *(stale, c48a14d8 — 7 commits, 2 behavioural)* — does an endpoint client reach the connection-pool wedge? three benches could not see it; the gap is made of latency
- [+] **[B-09](B-09-unfiled-grpc-compat-items.md)** **decided (round 415): read out, verified against the code, and answered item by item.** The original objection — filing would invent content with no number — **was wrong about its own subject**: the memory note declares its list empty as of round 139 and the three survivors each carry a commit. (1) **Split, and widen the delimiter.** The standard settles it: RFC 9110 s5.3 permits combining only "without changing the semantics", and gRPC's PROTOCOL-HTTP2 defines Custom-Metadata as exactly that field ("joined with ',' … semantically equivalent"), so splitting is the provided-for recombination and NOT splitting would be the non-conformant choice. But gRPC's delimiter is `","` while we split on `", "` alone — and the same narrowness reaches a spec **MUST**: *"Implementations must split Binary-Headers on ',' before decoding"*, and `metadata.dart:214` never splits, so a joined `-bin` fails `base64.normalize` and the error's structured details vanish silently. READ, not measured. (2) **Fix the 504 row only** — `RpcRetryInterceptor` retries UNAVAILABLE and RESOURCE_EXHAUSTED, so a gateway timeout is retried over http2 (`unavailable`) and NOT over HTTP/1.1 (`deadlineExceeded`); the other rows differ in meaning alone. (3) **Measure before fixing**: the round-89 revert's stated blocker is DEAD — `endpoint_ping_exchange_errors_test` pins UNAVAILABLE today, not the `StateError` the deferral rested on — which is this note's own lesson applied to itself. Original text: open *(stale, 5bf4d34e — 29 commits)* — unfiled "documented, not fixed" items from private memory
- [+] **[B-03](B-03-wasm-no-package-swift.md)** **decided (round 415): add it now**, not when SPM becomes the Flutter default — the cost is the same either way and nothing in a round's ordinary work detects the day the default flips. **A DEVICE round, not a source edit**: the privacy manifest goes in BOTH manifests or the `resource_bundles` defect already caught once comes back, and the evidence is a BUILT app — a manifest in the repository proves nothing and `pod install` must be re-run. So `melos run test:wasm:device` on iOS twice: under CocoaPods as the control, and with SPM enabled. Original text: open, not urgent (round 182) — wasm: no `Package.swift`, and under SPM there is no plugin at all

## Whole layers, deferred as a group

- [ ] **[B-10](B-10-layers-without-lenses.md)** deferred (round 223), **NARROWED in round 415: this defers LOOKING, not FIXING.** The deferral covers the lens work — enumerating surfaces in packages nobody has swept, which is open-ended and competes with core and transport for attention. It does NOT cover a defect already measured in one of those packages and written down with a number: that fix costs what it costs, and leaving it unapplied saves the loop nothing, because the finding is already paid for. **B-33 and B-46 are unblocked by this**, both in `packages/blob`, both measured, both of which had "out of scope, see B-10" as their only reason. A round still may not go LOOKING in data, notify or blob. Original text: — data, notify and blob have no lens at all: 234 files. **Not to be taken up while core and transport still have work**, however loudly `loop.py stale` names those three directories

## Archive — kept because the reasoning still reads

- [x] ~~**[B-28](B-28-metadata-is-exempt-from-flow-control.md)**~~ closed (round 350) — the owner took it at the cap, and the decision it had been waiting for since 282 turned out not to be one. **Charging metadata against the send window is the wrong tool and HTTP/2 says so**: flow control there applies to DATA only, HEADERS are exempt by design, because a control frame that cannot be sent deadlocks the stream it is trying to end. The other candidate is also how the protocol really bounds headers — by SIZE. `RpcChannelTransport` now charges every frame queued into a per-stream controller against `effectiveMaxBufferedBytes`, weighed by the `bufferedBytes` round 279 wrote for the other queue; over the bound the STREAM fails with `RESOURCE_EXHAUSTED` and the connection survives. `none → RESOURCE_EXHAUSTED` on 200 frames of ~8 KiB against a 256 KiB bound, ablation `Expected: non-empty Actual: []`, and the third GUARD is what makes it a buffer bound rather than a throughput cap: **a consumer that keeps up takes all 200.** P-31 cannot see it (it measures the sender, and its consumer is paused) — recorded in the round rather than re-scoped
- [x] ~~**[B-34](B-34-http2-does-not-check-outbound-metadata.md)**~~ closed (round 340) — filed measured in 339 and fixed in 340 with the two lines `rpc_dart_http` already had. RPC-10 applied to `RpcChannelTransport.sendMetadata`: websocket/wasm/isolate inherit its outbound policy check and http ported it, while **http2's only `validateMetadata` was inbound**. Not unchecked — `_headerValue` rejects non-printable-ASCII — but a hardcoded charset check is not a policy: under `maxHeaders: 32, maxHeaderValueBytes: 64`, **64 headers, a 200-char value and a header name with a space were all ACCEPTED** where the shared layer threw. `+206 → +210`, one canary per half. **Keeper from the measuring:** a first pass used `maxHeaders: 4`, below what an ordinary request carries, so the peer refused every call including the control — and it read exactly like the violating frame had poisoned the connection. *(Sat under "Awaiting an owner decision" until the curate after 348 moved it — the same bookkeeping defect the round-287 audit note at the top of this file describes.)*
- [x] ~~**[B-27](B-27-a-tcp-syn-builds-an-endpoint.md)**~~ closed (round 287) — a TCP SYN built the transport, the endpoint and the application's contracts before a byte arrived: 200 silent sockets gave 200 endpoints, against 0 on websocket. Closed in two halves, because it needed two mechanisms: `prefaceTimeout` (275) bounds a peer that never speaks HTTP/2, and `pingInterval` defaulting to 30s (287) bounds one that speaks the preface and then goes silent — 24 preface bytes buy past the first. Fix 1, deferring the construction itself, is left and de-urgentised
- [x] ~~**[B-22](B-22-paused-consumer-never-repays-the-pool.md)**~~ closed (round 266) — **a wedged connection, fixed in one line.** A PAUSED consumer never receives `done`, so its `onCancel` never ran and `_fcForget` skipped the repay because a listener existed: the bytes stayed owed forever. Now repaid unconditionally, 1024 -> 3072 KiB. The double credit that guard prevented is absorbed by the receive-side clamp — established after five witnesses failed to observe it (253, 254, 262-264) and one read explained why (265)
- [x] ~~**[B-06](B-06-websocket-lead-list-is-stale.md)**~~ closed (round 234) — the websocket rescan is finished. 233 did a third and named the unread file; 234 read it and it held a third RPC-03 instance: on a drop the PEER starts, the id cursor was rewound by the very close that reports the drop
- [x] ~~**[B-01](B-01-response-metadata-dropped.md)**~~ closed (round 223) — response metadata dropped wholesale; a missing feature, not a defect, so it leaves the loop and becomes ordinary roadmap work. The measurement and the API shape stay on the page
- [x] ~~**[B-02](B-02-wasm-android-promise-rejection.md)**~~ closed (round 223) — wasm guest promise rejection on Android, accepted as [C-23](../checked/C-23-wasm-guest-promise-rejection-accepted.md)
- [x] ~~**[B-04](B-04-isolate-future-timeout-unaudited.md)**~~ closed (round 223) — isolate `Future.timeout` sites swept, all four guarded; the guards are untested, which is [L-04](../lessons/L-04-a-guard-with-no-witness.md)
- [x] ~~**[B-07](B-07-decision-close-on-protocol-error.md)**~~ decided by owner (round 190) — `closeOnProtocolError` defaults to `false`, plus a cap on the violation count
- [x] ~~**[B-08](B-08-decision-closed-transport-error-split.md)**~~ closed (round 201) — the error-type split on a closed transport
- [x] ~~**[B-12](B-12-http2-cancel-kills-the-connection.md)**~~ closed (round 208) — one cancelled stalled call killed the connection; the owner chose "keep reading, fail the call". The upstream package:http2 report is still open
- [x] ~~**[B-13](B-13-parked-sender-outlives-its-call.md)**~~ closed (round 211) — measured: the wake works, 0 waiters with it and 30 without
- [x] ~~**[B-14](B-14-stale-sendcredit-per-abandoned-upload.md)**~~ closed (round 212) — the writer was a late `_fcOnGrant` after teardown; fixed
- [x] ~~**[B-15](B-15-rpc-level-grants-on-http2.md)**~~ closed (round 214) — cooperative backpressure on http2 via rpc-level grants, not built; the behaviour that stands is accepted in [C-19](../checked/C-19-http2-refuses-a-slow-consumer.md)
- [x] ~~**[B-18](B-18-web-guard-is-a-census-not-a-sweep.md)**~~ closed (round 227) — premise corrected: for the cancel class the web smoke tests are a working detector, shown by ablation ([C-25](../checked/C-25-web-smoke-catches-a-cancel-deadlock.md)); and the `async*` class it named no longer reproduces on Dart 3.10.1
- [x] ~~**[B-19](B-19-close-the-gate-over-wasm.md)**~~ closed (round 226) — `analyze`, `format:check` and `format` now cover rpc_dart_wasm; verified on four arms, a planted violation goes red in wasm and still goes red in a member
- [x] ~~**[B-05](B-05-isolate-null-credit-silent.md)**~~ closed (round 229) — a peer whose first grant was ZERO was read as pre-flow-control and got flooded: 800 KiB through a 64 KiB window, 16 KiB after. The logging half is a diagnostic and stays unfixed
- [x] ~~**[B-20](B-20-detached-guard-has-no-witness.md)**~~ closed (round 225) — the guard has no witness because nothing reaches it; 0 rejections across three scenarios, all 25 wrapped expressions internally guarded ([C-24](../checked/C-24-detached-guard-is-unreachable.md)). The isolate half stays open in B-04's closing note
- [x] ~~**[B-17](B-17-watermark-lost-through-a-decorator.md)**~~ closed (round 224) — the decorator that erased the stream-id watermark is refused at attach; `handlers ended` went `1 -> 0` with the control unchanged. The compile-time version is [B-21](B-21-reconnectable-transport-type.md)
- [x] ~~**[B-16](B-16-pre-method-byte-budget-release.md)**~~ closed (round 215) — swept, clean; the one row that looks like a leak is the reorder deferral, bounded by `halfOpenStreamTimeout` ([C-20](../checked/C-20-pre-method-budget-held-for-the-reclaim.md))
